# H题第3小题杆球模块融合与标定指南

## 范围与安全边界

`user/project_ball.uvprojx`是独立的杆球固件，只编译杆球控制、RDS3230、Maix协议、UART2、TIMG6、TIMA1 PWM、按键、OLED和系统底层。它不编译或初始化底盘、电机、编码器、LF04和IMU。第3题调试与验收期间必须物理断开小车12 V电机电源。

当前软件只完成了可移植测试和编译级验证；舵机控制方向和MaixCam正侧厘米映射点已完成单项实测，但水平中位、1400/1600 us机械安全边界、负侧映射和串级PID参数仍未经过本机验证。不能据此声称已经满足5 s或±1 cm。

## 固定资源与接线

| 功能 | MSPM0G3507资源 | 外部连接 |
| --- | --- | --- |
| RDS3230信号 | PB27 / PINCM58 / TIMA1_CCP1 / 50 Hz | 舵机信号线 |
| MaixCam接收 | PB16 / UART2 RX | MaixCam TX |
| MaixCam发送 | PB15 / UART2 TX | MaixCam RX，可选但建议接通 |
| 杆球时基 | TIMG6，1 ms | 无外部接线 |
| 唯一操作键 | PB24，低有效 | 板载SW6垂直中键 |

舵机V+使用独立6–8.4 V电源或BEC，电源应能承担约3.8 A峰值。舵机、MSPM0和MaixCam必须共地；舵机不得从开发板3.3 V或5 V取电。接线前断电，用万用表确认目标排针确实连到PB27且没有外接负载。

MaixCam通信设置为115200、8N1。报告命令为`cmd=0x02`，20字节小端body为`<IhhffB3x>`，依次包含时间戳、原始x/y、位置cm、score和valid；完整帧32字节，CRC为CRC16-IBM。MSPM0只用厘米值计算位置，原始像素仅供诊断；脚本位于`maixcam/main.py`。

## 固件模式

正常上电不按中键：OLED等待连续3个有效视觉帧。`BALL_BALANCE_ALLOW_SEQUENCE=1`，PB24有效按下可启动一次正式`+5→-5`序列；运行中再次按PB24会中止、清除两级控制状态并按斜率限制回到1500 us。一次上电最多接受一次正式启动，完成后继续保持-5 cm闭环，不自动重复。软件已经解锁不代表参数已经安全，完成下述速度PD和中心串级闭环调试前不要启动正式序列。

上电时按住PB24：进入标定模式，必须先完全松开才接受操作。中键短按依次请求`1500→1400→1500→1600→1500 us`，之后循环；上、左、下、右键不触发舵机动作。首版`BALL_CALIBRATION_SPEED_TEST=1`，中键长按1 s仅在视觉有效时进入速度目标0的速度PD测试，OLED显示`CAL SPEED PD`；再次长按退出并回中。

视觉丢失、超过150 ms未收到有效视觉、评分序列达到5 s或人工中止时，控制器清积分、停止序列并通过现有2000 us/s斜率限制回中；故障后不自动恢复控制。OLED以10 Hz显示状态、目标/位置、速度/当前脉宽、序列用时、视觉年龄和累计错误。

## 未验证起点

| 参数类 | 当前起点 | 状态 |
| --- | --- | --- |
| 舵机中位/软件范围 | 1500 us；1300–1700 us | 未验证，不得扩到500–2500 us |
| 控制方向 | `BALL_CONTROL_DIRECTION=-1` | 2026-07-30实测：1450 us使`+cm`侧下降 |
| Maix厘米映射 | `+75%=+11 cm`，按中心对称换算 | 正侧已提供，负侧未复核 |
| 置信度 | 0.40 | 未验证 |
| 位置外环P/D/I | 1.6 / 0 / 0，速度目标限幅±10 cm/s | 未验证起点 |
| 速度内环P/D | 25 / 0，控制周期10 ms | 未验证起点 |
| 观察器 | alpha=0.65、beta=0.12 | 未验证 |

每轮只调整一类参数。记录旧值、新值、修改依据、结果和明确回退值；不要同时修改舵机方向、视觉标定和控制增益。

## 软件验证

从仓库根目录执行：

```text
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\run_host_tests.ps1
python .\tests\test_ball_packet.py
python -m py_compile .\maixcam\main.py .\tests\test_ball_packet.py
```

μVision空闲时依次全量Rebuild，不得并行启动UV4：

```text
user\project_ball.uvprojx
user\project.uvprojx
user\project_track.uvprojx
```

三个工程都必须为`0 Error(s), 0 Warning(s)`。杆球工程链接映射中不得出现`chassis_init`、`ml_motor_driver_init`或其他电机控制符号。

## 实机标定顺序

### 1. 供电与PWM

1. 断开小车12 V、舵机电源和机械连杆，只给MSPM0逻辑供电。
2. 烧录杆球工程，示波器测PB27，记录周期、高电平脉宽和高低电平电压；起点应约为20 ms和1500 us。
3. 接入独立舵机电源但仍不接连杆，记录空载静态/动作电流，确认MSPM0和MaixCam不复位，舵机无异常发热或持续大电流。

记录：`周期____ms，中位____us，高电平____V，低电平____V，空载电流____A，峰值____A`。

### 2. 方向与机械限位

执行标定短按序列，分别记录：

| 请求脉宽 | 舵机臂角度 | 杆右端升/降 | 电流 | 干涉/顶死 |
| --- | --- | --- | --- | --- |
| 1400 us |  |  |  |  |
| 1500 us |  |  |  |  |
| 1600 us |  |  |  |  |

2026-07-30实测1450 us使`+cm`一侧下降，因此设置`BALL_CONTROL_DIRECTION=-1`。本轮从1450/1550扩大一级到1400/1600；任一侧出现干涉、顶死、持续大电流、异常发热或复位，立即回退1450/1550。1500 us时仍应优先机械调整舵机臂、连杆和摆杆水平；全局仍限制1300–1700 us，未经逐级检查不得使用500–2500 us全行程。

### 3. 视觉标定

固定摄像头，在杆上选择两个已知厘米刻度，每点采集至少20帧`(x,y,position_cm,score,capture_ms)`并取中位数。MaixCam屏幕以模型输入中心为原点，左/上分别为`+X/+Y`；当前正侧标定点为`+75%=+11 cm`，脚本按`1%=0.1466667 cm`中心对称换算，因此`-75%=-11 cm`只是待负侧实测复核的推算值。UART同时发送原始像素和厘米值，但MSPM0控制只采用厘米值。记录图像宽高、帧间隔、丢球率和最低稳定置信度，并在O、+5 cm、-5 cm独立复核；三点换算达到要求后才能进入闭环。

| 实际位置 | x中位数 | y中位数 | score最低/中位 | 换算位置 | 误差 |
| --- | --- | --- | --- | --- | --- |
| 负端标定点 |  |  |  |  |  |
| 正端标定点 |  |  |  |  |  |
| O |  |  |  |  |  |
| +5 cm |  |  |  |  |  |
| -5 cm |  |  |  |  |  |

### 4. 串级PID调试

先用标定模式长按进入`CAL SPEED PD`，此时速度目标固定为0且位置外环不参与。速度D保持0，只调速度P：小球被轻推后若舵机反复振荡则减小P，若制动过弱仍按原方向滚落则增大P；稳定后才单独增加速度D以缩短稳定时间并抑制超调。

速度PD确认后，下一轮只把`BALL_CALIBRATION_SPEED_TEST`改为0，使标定长按进入0 cm完整串级闭环。位置环按P→D→I调节：先保持D/I为0调整位置P，再单独加入位置D，最后才加入很小位置I。位置I仅在误差绝对值不超过1.0 cm时积累，超出分离区清零，速度目标或舵机输出同向饱和时冻结，积分限制为±8 cm·s。每轮记录旧值、新值、依据、最大超调、稳定时间、最大脉宽和回退值。

### 5. +5到-5

虽然正式序列门已设为1，仍须在速度PD和0 cm完整串级闭环稳定后才按PB24启动。每轮只改速度P、速度D、位置P、位置D、位置I、观察器或序列判定中的一类，并记录：

| 参数类 | 旧值 | 新值 | 依据 | 总时间 | +5误差/超调/稳定 | -5误差/超调/稳定 | 最大脉宽 | 回退值 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |  |

任一丢球、机构碰限、舵机电源跌落、控制振荡增大或板卡复位，都立即按PB24中止并回退。

### 6. 正式验收

使用杆上0.1 cm刻度和完整图传录像作为外部基准，不能只用同一视觉坐标自证精度。从PB24被固件接受的启动边沿计时，到-5 cm稳定判定完成停止计时。至少连续5次满足：总时间不超过5.0 s，+5 cm和-5 cm处最大误差绝对值均不超过1.0 cm，小车电机始终无动作。

实测完成后，将确认的方向、中位、安全边界、像素端点、阈值和最终控制参数同步写入本指南、README、WIRING、ROBOT_SETUP、HARDWARE_ACCEPTANCE和AGENTS；AGENTS只追加杆球已验证值，不改写已有底盘stage 3记录。
