# H题第3小题杆球模块融合与标定指南

## 范围与安全边界

`user/project_ball.uvprojx`是独立的杆球固件，只编译杆球控制、杆球遥测、RDS3230、Maix协议、UART0/2、TIMG6、TIMA1 PWM、按键、OLED和系统底层。它不编译或初始化底盘、电机、编码器、LF04和IMU。第3题调试与验收期间必须物理断开小车12 V电机电源。

当前软件只完成了可移植测试和编译级验证；舵机控制方向和MaixCam正侧厘米映射点已完成单项实测，但水平中位、1400/1600 us机械安全边界、负侧映射和串级PID参数仍未经过本机验证。不能据此声称已经满足5 s或±1 cm。

## 固定资源与接线

| 功能 | MSPM0G3507资源 | 外部连接 |
| --- | --- | --- |
| RDS3230信号 | PB27 / PINCM58 / TIMA1_CCP1 / 50 Hz | 舵机信号线 |
| MaixCam接收 | PB16 / UART2 RX | MaixCam TX |
| MaixCam发送 | PB15 / UART2 TX | MaixCam RX，可选但建议接通 |
| CSV导出 | PA10 / UART0 TX、PA11 / UART0 RX | 3.3 V TTL，115200 8N1 |
| 杆球时基 | TIMG6，1 ms | 无外部接线 |
| 安全中止键 | PB24，低有效 | 板载SW6垂直中键 |

舵机V+使用独立6–8.4 V电源或BEC，电源应能承担约3.8 A峰值。舵机、MSPM0和MaixCam必须共地；舵机不得从开发板3.3 V或5 V取电。接线前断电，用万用表确认目标排针确实连到PB27且没有外接负载。

MaixCam通信设置为115200、8N1。报告命令为`cmd=0x02`，20字节小端body为`<IhhffB3x>`，依次包含时间戳、原始x/y、位置cm、score和valid；完整帧32字节，CRC为CRC16-IBM。脚本在每个图像周期都发送一帧：识别成功发送真实字段和`valid=1`，未识别时坐标、厘米、score清零并发送`valid=0`。MSPM0只用厘米值计算位置，原始像素仅供诊断；脚本位于`maixcam/main.py`。

## 固件模式

自动入口由`BALL_AUTO_CONTROL_MODE`选择：`BALL_AUTO_CONTROL_DISABLED`为人工标定/正式入口，另外两档用于速度内环和0 cm串级闭环调试。当前默认`BALL_AUTO_CONTROL_SPEED_TEST`，且`BALL_BALANCE_ALLOW_SEQUENCE=1`。正常上电保持舵机1500 us并等待连续3帧有效视觉；视觉就绪后自动进入速度内环单独调试，目标速度固定为0 cm/s，OLED显示`CAL SPEED PD`。位置环、速度环和安全边界参数均按`code/ball_balance_config.h`当前值保持不变；本轮只切换应用模式，不修改PID。

正式序列运行中再次按PB24会标记`ABORTED`并以2000 us/s斜率回到1500 us。单个`valid=0`、低score或越界值不会立即终止：150 ms内继续使用最近有效观测控制，但立即清除当前端点稳定计时。自最后有效观测达到150 ms时保留`TO_PLUS_5`或`TO_MINUS_5`阶段和目标，清PID、关闭闭环、斜率回中并显示`BALL REACQUIRE`；连续3帧有效后重新初始化观察器和PID，自动继续原阶段。PB24在重捕获期间仍可中止，5000 ms总计时始终继续并可在等待时触发`TIMEOUT`。超时瞬间清PID、脱困和制动状态，并把目标脉宽冻结为当时的实际脉宽；目标与实际脉宽相等即结束遥测并允许`D`导出，PB24只负责随后回中。恢复时OLED第一行只更新一次当前目标，活动闭环重新冻结周期显示。完成后保持-5 cm闭环，计时锁定，并仅更新一次OLED第一行`BALL HOLD -5`；确认球稳定后再次按PB24可关闭闭环并回中，同时保留`COMPLETE`和最终用时。受限静摩擦补偿现在允许在中心调试及正式`TO_PLUS_5/TO_MINUS_5`阶段工作，但完成保持阶段禁用；正式+5端仅在`|error|>0.9 cm`触发，-5端仅在`|error|>1.0 cm`触发，避免干扰端点合格计时。

首份25列正式CSV共502条有效记录，活动段500条全部连续10 ms，视觉年龄最大42 ms且无失视。球从-0.323 cm运动到最大+3.785 cm，约3 s后卡在+3.7～+3.8 cm并于5000 ms以+3.744 cm超时；超时前积分6.516/12 cm·s、控制约+74 us、舵机约1426 us，目标速度约+2.96 cm/s而实际速度接近0，所有控制权限均未饱和。源码据此只以`BALL_SEQUENCE_BREAKAWAY_ENABLED=1`把现有300 ms静止触发、50 us/s、150 us增力、1400～1650 us窗口和500 ms故障门带入正式运行，PID、5 s门限及绝对舵机范围不变。正式脱困故障显示`BALL STICK FAULT`，CSV记录`sequence_state=ABORTED,breakaway_fault=1`并在回中后允许导出；回退时只把该开关改为0。

正式脱困首轮三次CSV均保持连续10 ms、视觉年龄不超过90 ms且无失视，但全部停在`TO_PLUS_5`并以脱困故障结束。第1/3次几乎没有位移，分别在1920/800 ms故障；第2次最多移动到+0.659 cm并在2510 ms故障。普通PID在补偿前已经请求最低1383/1398/1385 us，原正式1400 us补偿下限却在激活后把更强的指令反向夹回1400 us，随后满足边界保持500 ms而过早FAULT。用户已确认1350 us机械绝对安全，因此源码只增加正式序列专用1350 us下限：正式窗口改为1350～1650 us，中心调试继续1400～1650 us；+5故障门按实际1350 us判断，-5仍按1650 us。PID、300 ms触发、50 us/s斜坡、150 us增力、释放条件、5 s门限和1300～1700 us绝对范围均不变。

1350 us正式脱困复测两次仍确认力度不足：两份CSV均连续10 ms且视觉正常。第一次先移动到最大+1.75 cm，随后卡在约+1.62 cm并在4050 ms故障；第二次几乎未启动并在2830 ms故障。两次实际1350 us均连续保持500 ms，控制输出被正式窗口限制在150 us，积分因误差大于3 cm保持分离。用户选择把正式+5脱困下限直接降到现有1300 us全局绝对边界；中心调试仍为1400 us，正式-5仍为1650 us。只有实际1300 us保持500 ms才锁存脱困故障；若5000 ms总超时先发生则按TIMEOUT结束。PID、50 us/s斜坡、150 us增力和全部视觉逻辑不变，1300 us不得继续降低。

1300 us正式固件复测两次均在5000 ms以`TIMEOUT`结束且没有触发FAULT：第一次最低1329 us，补偿受反向像素速度尖峰影响被切成4段；第二次最低1327 us，在约+2.15 cm再次卡住后增力到107/150 us即超时。两次都未实际到达1300 us，因此下限和PID保持不变。当前只把正式`TO_PLUS_5/TO_MINUS_5`斜坡从50提高到100 us/s，10 ms周期每次增加1.0 us；中心0 cm调试仍为50 us/s和每周期0.5 us。补偿激活后，反向清除必须同时满足相对触发起点背离目标至少0.15 cm、反向速度绝对值超过0.3 cm/s；单像素速度尖峰但累计背离不足0.15 cm时继续增力并冻结积分。朝目标释放仍要求位移至少0.15 cm、速度至少0.5 cm/s连续60 ms。正式斜坡回退值为50 us/s。

随后正式序列改为静止300 ms后直接使用150 us最大脱困增力，并曾试验0.5/0.1 cm/s迟滞的全程极限制动。三份26列CSV的活动段均连续10 ms，视觉年龄最大60 ms且无失视，但最大位置仅为+3.421/+2.997/+3.314 cm，全部停在`TO_PLUS_5`并于5000 ms超时。独立制动在位置仅+0.15～+0.92 cm、距离目标仍约4 cm时便请求1650 us并持续约0.52～0.58 s，使球到+3.0～+3.4 cm后反向至-0.73～-1.36 cm，再由1300 us拉回，形成约1.5 s周期的极限往返。直接150 us脱困本身已成功启动小球，因此当前保留该脱困策略，只把`BALL_SEQUENCE_OVERSPEED_BRAKE_ENABLED`默认改为0；运动后由原位置D和速度P环连续制动。强制制动代码、`brake_active`字段和26列CSV均保留，旧开关值1仅作为诊断回退。

后续曾对正式+5脱困临时开放1000 us。三份新26列CSV均在`TO_PLUS_5`于5000 ms超时，位置峰值达到+8.372/+12.392/+11.961 cm，且三次实际脉宽都到达1000 us；释放后普通速度环已经长期饱和制动，说明主要问题是脱困冲量过强而非PID增益不足。当前已撤销1000 us例外，舵机驱动和正式脱困均恢复1300 us绝对下限，1700 us绝对上限不变；位置P/D/I=0.8/0.40/0.30、速度P/D=27/3和5秒门均不变。

恢复1300 us后的三份CSV中，第1/3次分别到达+5.322/+4.495 cm，已经进入题目允许的+5±1 cm范围，但进入时速度为8.12/6.20 cm/s，旧状态机因共用`|速度|≤1 cm/s`和200 ms稳定门而拒绝折返；第2次最高+3.419 cm。当前按题意把+5改为±1 cm内连续30 ms位置确认后立即折返，不检查速度；只有最终-5继续要求±1 cm、`|速度|≤1 cm/s`连续500 ms。任一越界、失视或无效帧都会清零当前确认计时，1300–1700 us和全部PID保持不变。

最新两份CSV分别在2870/4520 ms锁存正式+5脱困故障，最大位置仅+2.144/+0.859 cm；视觉和10 ms控制周期正常。第一份在故障前已经启动但原60 ms释放确认尚未完成，第二份则在1300 us下真实卡住。用户据此明确授权把驱动绝对下限改为1200 us，但普通PID继续限制在1300 us以上。正式序列改用300 ms内朝目标位移不足0.15 cm触发：先在实际1300 us保持最多250 ms，仍未启动才在实际1200 us保持最多250 ms；有效位移和速度连续30 ms即释放，确认期间暂停故障计时。中心调试仍为1400～1650 us，正式-5仍只使用1650 us方向的脱困。

1200 us二级脱困后的三份CSV均顺利进入`TO_MINUS_5`，但第一次正向运动只到+3.09～+3.53 cm便被普通串级环反向拉回，第二次往返才进入+5允许带；`brake_active`全程为0。速度D最早在+0.26～+0.47 cm使舵机越过1500 us，位置D又在约+2 cm把速度目标变为负数，因此三个+5阶段耗时2280～2530 ms，最终全部在-5阶段5000 ms超时。当前正式序列改用惯性停止距离：6 cm/s巡航、实测制动减速度45 cm/s²、0.15 cm余量和制动区`4×位置误差`速度目标；端点±1 cm内目标速度为0，正式速度D改为0。+5必须同时满足±1 cm和`|速度|≤1 cm/s`连续30 ms才折返。4.2 g质量在`½mv²=mad`中抵消，不作为独立增益写入。

惯性规划后的三份CSV均在+4.96～+5.06 cm、低于1 cm/s时折返，但负向首次进入允许带仍有5.14～6.22 cm/s，越过至-6.11～-6.41 cm后，旧规划在`+6/0/-6 cm/s`之间切换并产生最高10.81 cm/s回冲。上一轮只为`TO_MINUS_5`增加独立捕获规划：5 cm/s巡航、有效减速度8 cm/s²、0.30 cm制动余量、`2×位置误差`且最大4 cm/s的首次接近；制动在进入允许带前锁存，首次进入±1 cm后双向纠偏均限1.5 cm/s，不再恢复巡航。负向制动锁存后启用速度D=3；+5参数及速度D=0保持不变。负向脱困的300 ms进展门新增`|速度|≤1 cm/s`条件，高速回冲时重新计时，进入最终允许带立即清除脱困。

负向独立规划后的三份CSV保持+5阶段约1.50～1.56秒完成，负向运动也明显稳定，但仍在5000 ms超时。三次首次进入-6～-4 cm时速度为5.56～7.39 cm/s，程序随即把速度目标置0；虽然在3790～3980 ms首次达到允许带内低速，却又回弹至-2.52～-2.98 cm，最长连续合格仅70～100 ms。上一轮删除负向±1 cm宽零速死区：高速进入允许带时继续用`2×位置误差`平滑减速；允许带内低速或越过-5中心后锁存捕获，改用`1×位置误差`和±1 cm/s限幅持续保持，仅中心±0.15 cm内目标速度为0。500 ms完成判定、5秒总门和其余参数不变。

连续位置保持后的三份CSV中，第1次从4530到4990 ms已在-4.33～-4.20 cm低速保持，距离500 ms完成门很近；第2/3次却在-4.40/-4.70 cm过早切换到弱捕获，回弹后停在-3.34/-3.53 cm，直到4850/4890 ms才触发1650 us脱困。上一轮把低速捕获位置门收紧为目标中心±0.4 cm，捕获前继续`2×误差`接近；捕获后同样使用`2×误差`但限幅±2 cm/s，仅±0.15 cm内零速。若已经捕获后再次越出最终±1 cm，低速脱困门由300 ms缩短为100 ms；捕获前、+5和中心模式仍保持300 ms。

内层捕获后的三份CSV最终均到达题目允许带，位置为-4.33/-4.44/-5.07 cm；第3次经过-5时速度仍有-4.54 cm/s，证明1650 us上限并非不足。三次首次低速到达时误差为0.62/0.72/0.64 cm，均因±0.4 cm门过窄而未锁存捕获，100 ms快速恢复没有启用。上一轮把低速捕获门放宽至±0.8 cm，同时把制动区速度限幅从4降至3 cm/s以降低入带动能；捕获后继续`2×误差`并把恢复限幅设为±3 cm/s，静止卡滞仍由1650 us快速脱困负责。

放宽捕获门后的三份最新CSV在5000 ms分别位于-3.79/-5.30/-4.01 cm，但速度仍为+3.26/-3.16/+2.12 cm/s。第1/3次因3 cm/s接近限幅在约-3 cm处触发了约0.3秒静摩擦脱困，首次低速入带推迟至4670/4740 ms；第2次虽在3840 ms低速入带，捕获后约1.2 cm/s的位置参考仍不足以及时抵消杆体回弹。当前恢复4 cm/s首次接近限幅，将捕获增益提高到`4×误差`且继续限幅±3 cm/s，并把100 ms脱困门扩展到整个正式负向阶段。1650 us上限、500 ms稳定门和TIMEOUT冻结均不变。

开机保持PB24仍进入人工标定模式，松开后才接受1500/1400/1500/1600/1500 us短按序列或长按调试操作；该启动方式不会运行正式序列。正常正式测试必须上电时不按PB24。

UART0遥测在正式序列成功启动时清空并开始，每10 ms保存一次，共600条约6秒。每条仍为44字节，记录区约26.4 KB并全部位于MSPM0G3507唯一的32 KB SRAM内；CSV为26列，在原阶段诊断基础上增加`brake_active`，默认关闭独立制动时该列应始终为0。序列值为`0=IDLE,1=TO_PLUS_5,2=TO_MINUS_5,3=COMPLETE,4=TIMEOUT,5=ABORTED,6=VISION_LOST兼容终态`。短失视、150 ms回中和三帧重捕获均延续同一遥测会话；人工中止后继续记录自动斜率回中，TIMEOUT冻结且目标/实际脉宽相等时立即结束遥测并允许导出。正常完成时继续记录-5 cm保持，直到PB24关闭闭环且目标/实际脉宽均回到1500 us才结束。OLED第四行显示非零`CSV Nxxx D=OUT`后，发送单个ASCII `D`可重复导出，核对保存后发送`C`清空；运行、重捕获、完成保持或回中期间只回复限频`BUSY`。RAM易失，导出前不得复位、重新烧录或断开5 V。

当前固件把遥测启动集中绑定到正式序列实际进入`TO_PLUS_5/TO_MINUS_5`，同一轮询立即保存首条。终态回中后OLED显示`CSV Nxxx D=OUT`，`xxx`是RAM中的实际记录数且必须大于0；发送`D`后的数字数据行数必须与之相等，每行26列。若OLED显示`CSV EMPTY REBOOT`，或串口回复`EMPTY`，本次记录无效，必须重新上电重跑，禁止保存空表头或据此调整PID。重复`D`保持同一数据，确认文件完整后才发送`C`。

曾尝试把后200条放入`0x20300000`，错误固件虽能链接且显示`ZI-data=36560`，但两次实机导出都恰好在第600条后变成固定重复垃圾。TI原厂MSPM0G3507链接文件只定义上述32 KB SRAM，因此已删除第二执行区并恢复600条；错误800条构建不得继续烧录或作为RAM基线。

600条单SRAM回退版本已全量Rebuild为`Code=14928, RO-data=2256, RW-data=896, ZI-data=27760`且0错误0警告。map只有`RW_IRAM2=0x6ff0/0x8000`，`g_records=26400`字节，不含`0x20300000`或第二遥测段，且无底盘控制符号，可作为当前烧录基线。

2026-07-31位置P=0.8首份CSV只有135条完整记录：175～4995 ms内位置停在+2.475 cm，视觉年龄不超过20 ms，控制量稳定约-49.5 us且舵机约1550 us；但相邻记录出现90次10 ms、43次80 ms和1次480 ms。该模式原先每100 ms通过软件I2C刷新四行OLED，阻塞时间与80 ms缺口一致，因此旧CSV不能作为PID调参基线。当前源码已冻结活动闭环期间的OLED写入；重新构建和烧录后，约5秒新CSV应接近500条，绝大多数间隔为10 ms且不再周期性出现80 ms缺口，达到该条件后再分析位置P。

OLED冻结固件的后续CSV得到594条完整记录，其中591个间隔为10 ms、2个为20 ms，全程视觉有效且帧间隔最大55 ms，证明活动闭环时序已恢复。P/D/I=0.8/0/0时位置从+2.38 cm稳定到+2.29 cm，控制约-45.8 us、舵机约1546 us且无振荡。用户确认机构本身存在不可避免的倾斜，1500 us只作为安全机械中位而非绝对水平；因此下一轮不猜测固定中位补偿，只启用位置积分消除静差。

位置I=0.20的后续CSV有540条完整记录，活动闭环内538个间隔为10 ms、视觉年龄最大90 ms且无失视。球从+2.66 cm开始，约2.3 s后舵机达到1580 us并克服静摩擦，最低到+0.893 cm，最终稳定约+1.01 cm；最大速度约-2.86 cm/s、最大舵机1582 us，没有过零或发散。按目标速度反推，积分约在3930 ms达到-8 cm·s上限；最终约1560 us仍不足以克服剩余结构偏置。因此本轮只把I从0.20提高到0.30，积分最大速度贡献从1.6提高到2.4 cm/s、对应舵机控制从约40提高到约60 us，回退值为I=0.20。

位置I=0.30的新CSV有420条活动记录，其中419个活动间隔为10 ms，视觉全程有效。球始终位于+2.748～+2.848 cm；积分约在3.06 s达到-8 cm·s上限，目标速度达到约-4.67 cm/s，舵机达到约1619 us且杆已倾斜，但球没有有效移动。该结果说明动态积分增益方向正确，当前受限于结构坡度/摩擦所需的稳态补偿权限。本轮只把积分限幅从±8扩大到±12 cm·s，I仍为0.30；最大积分贡献为±3.6 cm/s和约±90 us，在3 cm分离边界的总舵机偏移不超过约150 us。回退值为±8 cm·s。

±12 cm·s积分限幅固件的三次CSV均为600条连续10 ms活动记录且视觉有效。第一次从+2.75 cm移动到最低+0.979 cm并停在约+1.01 cm，没有过零；后两次因静摩擦直到积分接近-12 cm·s才启动，峰值速度分别约-5.70/-5.74 cm/s，各过零一次并停在约-1.39/-1.50 cm。该差异来自启动时积累的外环积分储能不同，运动阶段缺少位置D制动。本轮只把位置D从0提高到0.20；在-5.7 cm/s时预计额外产生约+1.14 cm/s制动速度目标和约28.5 us反向舵机控制，回退值为D=0。

位置D=0.20的三次320×224复测同样保持连续10 ms且视觉有效，但启动仍不一致：第一次约5 s后启动，峰值速度-6.08 cm/s并过零到-1.45 cm，最低脉宽1402 us；第二次积分达到-12 cm·s、舵机1630 us仍停在+2.017 cm；第三次峰值速度-3.40 cm/s并稳定到+0.183 cm，脉宽1470～1608 us。D在静止时为0，继续增大D不能解决卡住，因此保持全部PID不变并加入首版中心调试静摩擦补偿：`|误差|≥0.5 cm`且`|速度|≤0.3 cm/s`连续300 ms后，冻结位置积分并以50 us/s沿目标方向增加临时控制，最大95 us；首版仅按朝目标速度达到0.5 cm/s撤销。补偿期间目标脉宽限制1400～1650 us；到最大值后500 ms仍不动则锁存`BREAKAWAY_FAULT`并回中。回退方式是删除/禁用该补偿，PID回退值仍为0.8/0.20/0.30和25/0。

首版静摩擦补偿的三次CSV均为600条、599个连续10 ms间隔且视觉有效。第一次约2.66 s启动，峰值-3.45 cm/s、最低+0.697 cm、最大脉宽1610 us，最终约+1.10 cm；第二次约2.04 s启动，峰值-3.73 cm/s、最低+0.417 cm、最大脉宽1638 us，最终约+0.825 cm；第三次未产生有效位移，原始球心只在x=133/134间跳动，1像素约0.0917 cm却产生最低-0.51 cm/s的虚假速度，使补偿反复提前撤销，最大增力仅67 us并最终停在约+2.42 cm。当时源码据此只修复补偿释放判定：激活时锁存位置和目标方向，必须同时满足朝目标位移≥0.15 cm、速度≥0.5 cm/s并连续保持60 ms才撤销；任一条件中断时确认计时清零但补偿及积分冻结继续。明显背离目标且速度超过0.3 cm/s仍立即清除补偿；当时PID、补偿斜坡/上限、故障门、23列CSV和600×44字节RAM记录均不变。

释放判定抗抖修复后的三次CSV确认补偿没有再被单像素抖动提前撤销。错误800条固件中只有前600条真实有效：第一次从+2.658 cm稳定到+0.183 cm，最低+0.101 cm、峰值速度-3.01 cm/s；第二次从+2.845 cm移动到最后有效+0.090 cm，峰值-3.05 cm/s，随后一次157 ms有效帧间隔触发150 ms失视安全门。第三次在第359条前已完整记录故障：球从+2.475 cm移动后卡在约+2.017 cm，补偿3429 ms达到95 us，3919 ms锁存`BREAKAWAY_FAULT`，最大舵机1642 us，3993 ms回到1500 us且视觉全程有效。释放、故障和回中逻辑均符合设计，本轮不修改PID、0.15 cm位移门、60 ms确认、95 us上限或150 ms视觉门。

随后完成五次舵机动力断开的视觉隔离测试，最大帧间隔依次为95/44/48/100/89 ms，均未达到150 ms且没有失视；此前157 ms按孤立事件处理，Maix显示频率和MSP超时门不改。机械复查确认球总在固定范围内停住，轻触不能启动而增大杆角可以启动，说明该区域存在较高静摩擦或局部坡度。按本轮软件脱困选择，只把临时增力内部上限从95提高到150 us，并将500 ms故障计时起点改为舵机实际到达1400/1650 us边界；50 us/s斜坡、300 ms触发、释放条件、PID和全部绝对边界不变。明确回退值为95 us；若1650 us保持500 ms仍不能启动，不再扩大范围，必须处理杆面、连杆或球体。

150 us补偿固件的三次CSV起点均已接近中心。第一次从+0.550 cm启动，在1613 us开始运动并过零，停在约-0.825 cm后反向补偿到1400 us仍不动，于5689 ms锁存故障；第二次在2379 ms因Maix间歇有框/LOST触发视觉超时，不能用于控制判断；第三次约5049 ms在1621 us启动，峰值速度-4.13 cm/s并过零，用户观察最终回到中心附近。三次初始视觉位置均为+0.55～+0.79 cm，原0.5 cm门限会在已经接近题目允许误差时强制脱困。当前只把静摩擦补偿进入门限改为0.9 cm，`|误差|<0.9 cm`时不启用临时增力，普通PID仍运行；回退值为0.5 cm。0.9 cm仅是视觉软件门限，厘米映射尚未完成外部精度验收。

0.9 cm门限固件已由用户在μVision全量Rebuild：`Code=14984, RO-data=2256, RW-data=896, ZI-data=27760`，`0 Error(s), 0 Warning(s)`。Map确认600条遥测数组`g_records=26400`字节位于唯一`RW_IRAM2`，该区占用`0x6ff0/0x8000`，未链接底盘控制符号。该结果仅放行烧录和中心复测，不代表正式性能验收。

0.9 cm门限构建后的四次CSV中，第一份在位置最大仅+0.553 cm时仍于549 ms触发补偿，行为符合旧0.5 cm固件；用户不确定首次测试前是否已重新Download，因此该份不能作为新门限验收。第二、三份均在约+0.458 cm保持`breakaway_active=0`，但该位置同时低于新旧门限，只能证明控制稳定，不能区分烧录版本。第四份使用新的`5.8%=1 cm`映射，从+1.401 cm启动：约2799 ms有效运动，最大脉宽1638 us，2899 ms首次进入±0.9 cm，最大负侧超调-0.922 cm，3459 ms后持续保持±0.9 cm并最终稳定在0 cm附近；全程10 ms连续、无失视和故障。普通PID制动阶段最低到1346 us，该动态记录不能替代1350 us机械安全复核。确认新门限必须明确重新Download后，把球固定在原始`x≈154`即新映射约+0.647 cm运行至少1秒；新固件增力必须保持0，若300 ms后触发则停止调参并检查下载目标。

从本轮起`5.8%/cm`作为正式后续映射基线：`position_cm=(160-x)×0.1077586`，因此`x=147/160/173`分别约为`+1.401/0/-1.401 cm`，`±75%`对应约`±12.931 cm`。此前使用旧比例形成的厘米CSV只保留历史意义，不得与新数据直接比较；负侧和O/±5 cm仍需外部刻度独立复核。

明确重新Download后的门限判别CSV已确认0.9 cm固件实际运行：原始球心固定在`x=153`，新映射位置全程为+0.754 cm，连续2450 ms内246条活动记录均保持10 ms周期，`breakaway_active=0`、`breakaway_boost_us=0`且无失视/FAULT。普通PID仍按设计累积积分到-1.848 cm·s，控制量到-28.9 us、舵机1529 us，但球保持静止；PB24中止后回到1500 us。该结果排除旧0.5 cm固件，因为旧门限会在约300 ms后触发补偿。0.9 cm进入门限现已完成软件与实板烧录确认，回退值仍为0.5 cm。

## 未验证起点

| 参数类 | 当前起点 | 状态 |
| --- | --- | --- |
| 舵机中位/软件范围 | 1500 us；绝对1200–1700 us | 普通PID最低1300 us；1200 us仅正式+5二级脱困 |
| 控制方向 | `BALL_CONTROL_DIRECTION=-1` | 2026-07-30实测：1450 us使`+cm`侧下降 |
| Maix厘米映射 | `5.8%=1 cm`，按中心对称换算 | 用户新实测，负侧未复核 |
| 置信度 | 0.20 | ROI限定调参阈值，最终视觉验收未确认；回退0.40 |
| 位置外环P/D/I | 0.8 / 0.40 / 0.30，积分限幅±12 cm·s，速度目标限幅±10 cm/s | 本轮只恢复舵机安全边界，PID不变 |
| 速度内环P/D | 27 / 3，控制周期10 ms | 本轮不调整 |
| 观察器 | alpha=0.65、beta=0.12 | 未验证 |

每轮只调整一类参数。记录旧值、新值、修改依据、结果和明确回退值；不要同时修改舵机方向、视觉标定和控制增益。

## 软件验证

从仓库根目录执行：

```text
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\run_host_tests.ps1
python .\tests\test_ball_packet.py
python -m py_compile .\maixcam\main.py .\tests\test_ball_packet.py
```

μVision空闲时依次全量Rebuild，不得并行启动UV4：

```text
user\project_ball.uvprojx
user\project.uvprojx
user\project_track.uvprojx
```

三个工程都必须为`0 Error(s), 0 Warning(s)`。杆球工程链接映射中不得出现`chassis_init`、`ml_motor_driver_init`或其他电机控制符号。

## 实机标定顺序

### 1. 供电与PWM

1. 断开小车12 V、舵机电源和机械连杆，只给MSPM0逻辑供电。
2. 烧录杆球工程，示波器测PB27，记录周期、高电平脉宽和高低电平电压；起点应约为20 ms和1500 us。
3. 接入独立舵机电源但仍不接连杆，记录空载静态/动作电流，确认MSPM0和MaixCam不复位，舵机无异常发热或持续大电流。

记录：`周期____ms，中位____us，高电平____V，低电平____V，空载电流____A，峰值____A`。

### 2. 方向与机械限位

当前正式固件开机保持PB24即可进入人工脉宽标定；松开PB24后短按中键依次执行下表序列。标定上电与正式序列上电必须分开，不要在同一轮修改PID：

| 请求脉宽 | 舵机臂角度 | 杆右端升/降 | 电流 | 干涉/顶死 |
| --- | --- | --- | --- | --- |
| 1400 us |  |  |  |  |
| 1500 us |  |  |  |  |
| 1600 us |  |  |  |  |

2026-07-30实测1450 us使`+cm`一侧下降，因此设置`BALL_CONTROL_DIRECTION=-1`。本轮从1450/1550扩大一级到1400/1600；任一侧出现干涉、顶死、持续大电流、异常发热或复位，立即回退1450/1550。用户确认现有结构无法在1500 us达到绝对水平，因此1500 us仅保留为安全机械中位，不加入未经测量的固定软件偏置；静差先通过位置积分补偿。用户在最新正式测试后明确把驱动绝对下限授权到1200 us，但该值只允许正式+5二级脱困短时使用；普通PID仍不低于1300 us，上限保持1700 us，不得使用500–2500 us全行程。

### 3. 视觉标定

固定摄像头，在杆上选择两个已知厘米刻度，每点采集至少20帧`(x,y,position_cm,score,capture_ms)`并取中位数。MaixCam屏幕以模型输入中心为原点，左/上分别为`+X/+Y`；当前用户新实测标定为球心横向`5.8%=1 cm`，脚本按`position_cm=x_percent/5.8`中心对称换算，因此负侧厘米值仍需实测复核。UART同时发送原始像素和厘米值，但MSPM0控制只采用厘米值。记录图像宽高、帧间隔、丢球率和最低稳定置信度，并在O、+5 cm、-5 cm独立复核；三点换算达到要求后才能进入闭环。

| 实际位置 | x中位数 | y中位数 | score最低/中位 | 换算位置 | 误差 |
| --- | --- | --- | --- | --- | --- |
| 负端标定点 |  |  |  |  |  |
| 正端标定点 |  |  |  |  |  |
| O |  |  |  |  |  |
| +5 cm |  |  |  |  |  |
| -5 cm |  |  |  |  |  |

### 4. 串级PID调试

2026-07-31速度内环首轮实机观察：速度P/D=25/0时，小球可明显减速并停止，舵机没有持续往复；球停在+2～+4 cm是速度目标0并不约束位置的正常结果，因此速度P方向和量级暂定基本正确，但尚不能代替完整动态验收。本轮保持速度参数不变。

2026-07-31位置P=1.6首轮实测中，坐标符号与实际一致且小球反复穿过0 cm，振荡持续增大；OLED观察到`P+090`即+9.0 cm、`U1700`即舵机达到软件上限。按单类参数纪律只把位置P降至0.8。冻结OLED后的有效CSV确认10 ms时序稳定，随后按P-D-I顺序得到当前位置P/D/I=0.8/0.20/0.30、积分分离3.0 cm、积分限幅±12 cm·s，速度P/D保持25/0。最新三次复测已有两次进入中心附近，第三次正确锁存机械卡滞故障。机械检查进一步确认球总在固定范围内停住，轻触不能启动而更大杆角可以启动。当前位置PID保持不变；临时增力以50 us/s继续推进，实际脉宽到达1400/1650 us后才开始500 ms故障计时。若1650 us仍无法启动，立即停止软件扩限并处理杆面、连杆或球体。

机械检查后单独验证视觉：断开舵机动力并固定球在画面中心附近，连续5次运行约5.5秒并导出CSV。五次均无`frame_interval_ms>150`则把157 ms视为孤立事件；仅一次超限则再增加5次样本；至少两次超限时才单独把Maix屏幕刷新降到10 Hz，检测和UART仍每帧执行，且不得提高MSP的150 ms安全门。串口软件必须关闭时间戳，避免把一条CSV拆成两行。

### 5. +5到-5

正式`+5→-5`入口当前已启用，`BALL_AUTO_CONTROL_MODE=BALL_AUTO_CONTROL_DISABLED`。正常上电不按住PB24，等待`BALL READY C=GO`，把球放在O点后单击PB24启动。运行时再次按PB24立即中止；完成并保持-5 cm后再按一次PB24回中并导出CSV。每轮只改速度P、速度D、位置P、位置D、位置I、观察器或序列判定中的一类，并记录：

| 参数类 | 旧值 | 新值 | 依据 | 总时间 | +5误差/超调/稳定 | -5误差/超调/稳定 | 最大脉宽 | 回退值 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |  |

任一丢球、机构碰限、舵机电源跌落、控制振荡增大或板卡复位，都立即按PB24中止并回退。

### 6. 正式验收

使用杆上0.1 cm刻度和完整图传录像作为外部基准，不能只用同一视觉坐标自证精度。从PB24被固件接受的启动边沿计时，到-5 cm稳定判定完成停止计时。至少连续5次满足：总时间不超过5.0 s，+5 cm和-5 cm处最大误差绝对值均不超过1.0 cm，小车电机始终无动作。

实测完成后，将确认的方向、中位、安全边界、像素端点、阈值和最终控制参数同步写入本指南、README、WIRING、ROBOT_SETUP、HARDWARE_ACCEPTANCE和AGENTS；AGENTS只追加杆球已验证值，不改写已有底盘stage 3记录。
