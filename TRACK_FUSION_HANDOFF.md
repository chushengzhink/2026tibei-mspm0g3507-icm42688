# LF04、编码器与IMU正式融合竞速交接

更新日期：2026-07-30

## 1. 本轮目标与实施状态

当前正式竞速的LF04、编码器和IMU巡线已经由实车确认基本完美；停车测试点已改为车尾，并根据最新一圈约125 mm的车尾越线修正停车坐标。正式竞速保持stage 0的360/360 mm/s和40% PWM安全基线，不启用提速档。最新27列CSV没有任何`target_center_mm_s=0`或非零`lap_total_ms`记录，末端进度约6036 mm、融合航向约389.77°，说明该圈正常双门仍未制动，车辆随后会在原故障门锁停。

根因分为两项：理论路线6141.6 mm不是车尾测试点重回A线时的实测编码器圈长；最新一圈融合航向在约5584–5618 mm已经经过355°–365°，旧逻辑到5804 mm才允许确认，因此错过窗口。按上一轮5994 mm参考、50 mm故障超程、约13 mm低速滑行和本次125 mm车尾越线估算，新参考为`5994+50+13-125≈5932 mm`。

当前实现保持已验证的LF04方向优先、B0无限保持、路线分段和R500前馈不变。停车改为`finish_reference_progress_mm=5932`，减速区从5742 mm开始，距离门为5919 mm，超程故障门为5982 mm；航向从DA起点约4570.8 mm开始允许确认，连续3周期进入355°–365°后锁存。红外位型和A点横线均不参与停车。中键启动时锁存融合方位；最新实车CSV显示首次停稳约387.06°，旧逻辑回到360°产生约28°错误左转，因此终点回正新增`finish_alignment_heading_bias_deg=+37°`，按`wrap(start+37°-current)`向约397°回正。回正期间LF04权重为0；补偿误差±2°且再次连续3周期停稳后才结束计时并显示OLED `TIME`。

2026-07-30首次竞速工程Rebuild暴露SRAM溢出`L6407E: 0xC10 bytes`。随后保留52字节公开记录、27列CSV和600条逻辑容量，把RAM内部记录压缩为44字节：tick与航向保持原精度，x/y以0.25 mm分辨率存储。修改后主机测试和ARMCC再次通过；两个Keil工程的最终链接结果及实车验收仍待完成。

## 2. 仓库与工作区状态

- 仓库：`E:\电赛`
- 分支：`main`
- 修改前提交：`ead59be 红外+iMU +编码器，巡线更稳了，就差停车距离了`
- 当前工作区包含本轮车尾停车修改，不得回滚或覆盖：
  - `HARDWARE_ACCEPTANCE.md`
  - `README.md`
  - `ROBOT_SETUP.md`
  - `TRACK_FUSION_HANDOFF.md`
  - `WIRING.md`
  - `code/chassis_track_mission.c`
  - `code/chassis_track_mission.h`
  - `tests/chassis_track_mission_test.c`
- 当前未检测到μVision进程；后续打开工程时仍不得并行启动多个UV4构建进程。
- 本轮提速和PWM扩容改动已撤回；`18/18`主机测试、`git diff --check`以及stage 0/1/2任务和受影响文件的ARM Compiler 5单文件编译均已通过。
- 两个工程仍需在μVision中分别执行全量Rebuild，并确认`0 Error(s), 0 Warning(s)`。

新对话开始后必须先执行`git status --short`并阅读`AGENTS.md`、`README.md`、`WIRING.md`、`ROBOT_SETUP.md`和本文，不得使用`git reset --hard`或`git checkout --`清理当前修改。

## 3. 两种竞速固件模式

同一个`project_track.uvprojx`固件包含两种入口，必须先确认进入了哪一种：

| 模式 | 上电操作 | READY显示 | 任务速度与停车 | 控制职责 |
| --- | --- | --- | --- | --- |
| 纯LF04诊断 | 上电前按住上键 | `LF ONLY READY` | 60/120/200/280/350 mm/s，无距离终点，人工中键停车 | LF04决定转向；IMU只记录；编码器负责轮速、里程和堵转 |
| 正式融合竞速 | 上电时不要按上键 | `RACE FUSION` | 默认stage 0直线/弯道均360 mm/s，5932 mm车尾参考/锁存航向双门自动停车 | LF04、编码器路线和IMU融合航向共同巡线；停车不读取LF04位型 |

恒定350 mm/s、越过6141.6 mm后仍继续运行、最后由中键停车的CSV属于纯LF04诊断。本文分析的最新数据是正式融合模式：目标为360 mm/s，但正常停车未触发并最终故障锁停。

## 4. 最新CSV来源与有效数据范围

原始文件：

```text
C:\Users\MECHREVO\.codex\attachments\be6a63e2-aef8-4b64-bcd2-1f6003789135\pasted-text.txt
```

去除串口工具的“发→/收←”和时间标签后，共得到185条完整27列记录。末端没有目标速度归零或正常停稳记录。CSV末六列已经分别包含任务进度、期望航向、路线前馈、航向反馈、LF04权重和最终轮差。

## 5. 最新一圈量化结果

中心行程使用当前左右标定值估算：

```text
left_mm_per_tick  = 0.1413727
right_mm_per_tick = 0.1434926

progress ≈ 首条记录x方向进度4.345 mm
         + 0.5 × (左tick增量×0.1413727 + 右tick增量×0.1434926)
```

该估算用于分析相对行程；最终A线位置仍必须用外部基准实测，不能直接把CSV的`x_mm/y_mm`当作A线偏差。

| 指标 | 最新结果 | 结论 |
| --- | ---: | --- |
| 完整记录时长 | 约18.5 s | 记录结束于故障门附近，不是正常完赛 |
| 正常停车命令 | 0次 | `target_center_mm_s`从未归零 |
| 正常完赛时间 | 0 ms | `lap_total_ms`全部为0 |
| 末条任务进度 | 6036 mm | 已越过旧距离门并接近旧故障门 |
| 末条融合航向 | 389.77° | 距离门前已经错过355°–365°窗口 |
| 末条目标/实际速度 | 100/约97 mm/s | 故障前仍在前进 |
| 外部停车前冲 | 约125 mm | 车尾约定测试点越过A线，需重标终点坐标 |
| 推算车尾A线进度 | 约5932 mm | 作为新`finish_reference_progress_mm` |

融合航向在约5584–5618 mm经过355°–365°，但旧门到5804 mm才开始计数；末条已到389.77°。这不是单纯的IMU符号错误，航向确认必须从DA段开始并锁存，不能要求距离门到达时仍处于窗口。

## 6. 上一轮第二弯与终点关键时序（历史分析）

第二弯理论起点为`3000 + π×500 ≈ 4570.8 mm`。下表中的进度由左右tick估算：

| elapsed_ms | 进度 mm | 融合航向 | LF04 | 红外纠偏 mm/s | 现象 |
| ---: | ---: | ---: | ---: | ---: | --- |
| 14020 | 4711.8 | 212.46° | B2 | +17 | 已进入第二弯，方向正确 |
| 14520 | 4894.2 | 224.38° | B1 | +120 | 第一次长时间外侧增强开始 |
| 15320 | 5174.7 | 257.08° | B1 | +120 | 再次进入满增强 |
| 16720 | 5661.9 | 320.40° | B1 | +120 | 末弯第三次满增强开始 |
| 17320 | 5867.4 | 351.08° | B1 | +120 | 航向已越过终点最低350°门 |
| 17520 | 5938.1 | 361.25° | B1 | +120 | 距离终点仍约204 mm时已超过360° |
| 17720 | 6008.3 | 371.51° | B0 | +120 | B0继续保持B1满增强，路线无法拉回 |
| 17820 | 6041.2 | 376.58° | B2 | +3 | 外侧增强刚释放 |
| 17920 | 6068.4 | 382.20° | B4 | 0 | 反向首周期抑制，航向达到峰值 |
| 18020 | 6090.4 | 380.34° | B0 | -10 | 反向确认后才开始纠偏回来 |
| 18220 | 6127.9 | 376.52° | B0 | 0 | 双门通过并下达停车，实际速度172.1 mm/s |
| 18700 | 6164.1 | 374.99° | B0 | 0 | 完全停稳，仍多转约15° |

## 7. 上一轮控制逻辑为何会产生终点歪斜（历史分析）

本轮修改前的正式竞速遵循“红外方向优先”：

- LF04非零纠偏时，只允许编码器/IMU路线辅助同方向叠加。
- 如果路线辅助与LF04方向相反，路线辅助被直接丢弃。
- B0无限期保持最后确认的LF04误差和B1/B8外侧增强状态。
- 普通总偏置上限90 mm/s；B1/B8及其B0保持阶段上限120 mm/s。

这一策略解决了早期“融合反向覆盖红外”的问题，但在最新末弯暴露出另一端的缺陷：当车辆航向已经领先路线、融合层需要负向阻尼时，B1或其后的B0仍持续请求`+120 mm/s`。因为LF04优先级是绝对的，负向路线修正被丢弃，车辆直到检测到对侧B4并完成反向确认后才开始回正。

此外，修改前的终点航向门只检查：

```text
heading_progress_deg >= 350°
```

它能阻止第二弯没转够时提前停车，却不能阻止375°这种过转姿态通过。本轮已用360°±5°连续3周期窗口替换该单边门。

## 8. 当前已实现参数与不可破坏基线

### LF04控制

- 四探头位置：`-40.25/-7.25/+7.25/+40.25 mm`
- 黑线低电平，`B1-B15`有效，`B0`丢线
- 质心误差：`error=-average(active_position)/40.25`
- `Kp=1.2、Ki=0、Kd=0.20`
- 普通纠偏：22%，最大90 mm/s
- B1/B8增强：35%，最大120 mm/s
- 反向确认：2个20 ms周期
- 纯LF04的B0：无限保持最后确认误差，不自动减速或停车
- 正式竞速的B0：与纯LF04一致，无限保持最后确认误差和增强状态
- 正式B1/B8：立即允许120 mm/s，不随持续时间降额
- LF04非零时：只接受同向路线/航向辅助，反向辅助不得抵消或反转LF04
- 实验性弯道方向记忆：纯LF04和正式竞速均关闭

### 正式路线融合

- 直线1500 mm、R500半圆、直线1500 mm、R500半圆
- 总长：6141.6 mm
- 默认stage 0直线/弯道速度：360/360 mm/s，加减速度400 mm/s²
- stage 1/2保留为未启用可选档：380/360、400/360 mm/s
- R500前馈：`omega_ff=v/500`，360 mm/s弯道为0.72 rad/s
- 航向反馈：`Kp=4.0`，限幅`±0.35 rad/s`
- 实测终点参考：5932 mm（车尾约定测试点重新到达A线的编码器进度）
- 当前终点提前量：13 mm，制动门5919 mm
- 接近区：190 mm
- 当前航向门：从DA起点约4570.8 mm开始累计，连续3周期进入355°–365°后锁存
- 到5982 mm仍未满足双门时锁停
- B15只记录，不作为启停或停车门

### 安全限制

- 轮速上限500 mm/s
- PWM绝对硬限幅20000/50000，即40%
- 正式竞速PID输出/积分限幅为14000/7000，继续叠加6000前馈
- 堵转门8个20 ms周期，约160 ms
- 中键运行中锁定急停
- 编码器继续使用3.3 V和GPIO软件4×解码
- 不得绕过现有急停、堵转、轮速和PWM限制

## 9. 本轮实现摘要

1. 正式任务输出已拆为`route_feedforward_rad_s`、`heading_feedback_rad_s`和`heading_error_deg`，下层融合角速度闭环保持原样。
2. 新正式接口保留路线前馈、航向反馈和LF04三个分量的显式遥测，但最终仲裁恢复LF04方向优先；旧更新函数继续作为纯LF04兼容入口。
3. 最新第一半圆失控回放、B0无限保持、同向叠加、反向LF04优先、左右镜像和旧纯LF04行为均有主机测试。
4. 理论6141.6 mm保持路线几何用途；停车改用5932 mm车尾参考、5919 mm制动门和5982 mm锁停。航向从DA开始允许确认，连续3周期进入355°–365°即锁存，随后389°仍可正常停车；红外位型不参与停车。
5. CSV扩为27列，新增`mission_progress_mm`、`expected_heading_deg`、`route_feedforward_bias_mm_s`、`heading_feedback_bias_mm_s`、`line_weight_pct`和`final_steering_bias_mm_s`。
6. 600条RAM缓冲保持不变。52字节公开结构通过44字节内部紧凑记录实现，避免首次Rebuild出现的3,088字节SRAM溢出。
7. 提速和PWM扩容已经撤回：默认竞速恢复360/360 mm/s、400 mm/s²和20000绝对PWM硬限制，stage 1/2仅保留为未启用可选档。

不应把路线几何、低速滑行和固定停车偏差用单一参数掩盖。先保持三源巡线仲裁不变，再依据至少3圈同一车尾测试点相对A线的外部有符号测量修正5932 mm终点参考；只有滑行证据变化时才调整13 mm提前量。

## 10. 剩余验收指标

- 正式READY必须显示`RACE FUSION`，不得误进`LF ONLY READY`。
- 使用默认stage 0和20000验证新停车点；本轮不启用stage 1/2，纯LF04参数和入口不变。
- 第一半圆B8/B0阶段不得再出现LF04负向回线而最终轮差为正；车辆必须先完成四分之一圈和半圈验收，再允许整圈。
- DA段内融合航向连续3周期进入355°–365°后必须锁存，随后超过365°不得清除。
- 自动停车命令前终点实际速度为100±20 mm/s。
- 完全停稳车身航向目标为360°±5°。
- 外部测得A线停车偏差绝对值目标不超过20 mm。
- 编码器航向与融合航向保持连续；IMU失效时仍能平滑退化到编码器。
- 中键急停、160 ms堵转、500 mm/s轮速和PWM 20000硬限制全部保留。
- PC主机测试保持`18/18 passed`；默认与竞速工程全量Rebuild均须为`0 Error(s), 0 Warning(s)`。
- 27列CSV重复导出必须完整，空缓存表头精确为435字节；正式巡线阶段`line_weight_pct`为100，终点原地回正和纯LF04正式融合诊断列为0。
- 默认stage 0整圈时间≤20.00 s，车尾A线停车偏差绝对值≤20 mm。

## 11. 可复制给新对话的剩余任务说明

```text
请先阅读E:\电赛\AGENTS.md、README.md、WIRING.md、ROBOT_SETUP.md和TRACK_FUSION_HANDOFF.md，并执行git status --short，保留当前全部未提交修改。

最新巡线稳定但车尾停车前冲约125 mm的实车CSV位于：
C:\Users\MECHREVO\.codex\attachments\be6a63e2-aef8-4b64-bcd2-1f6003789135\pasted-text.txt

当前保留ead59be已验证稳定的LF04方向优先、B0无限保持和路线/航向辅助；停车使用5932 mm车尾参考、5919 mm距离门、DA段内锁存的355°–365°航向门和27列CSV，红外位型不参与停车。首次停稳后按`wrap(start+37°-current)`执行终点专用偏置回正，当前启动零点的完成窗口为395°–399°；初始补偿误差超过45°或3秒未完成锁停。回正完成后才写入`lap_total_ms`并在OLED显示最终用时。正式竞速保持默认stage 0的360/360 mm/s、400 mm/s²和20000绝对PWM硬限制；stage 1/2只保留为未启用可选档。52字节公开结构使用44字节内部紧凑记录，容量仍为600。

请在当前μVision窗口中分别全量Rebuild user/project_track.uvprojx 和 user/project.uvprojx，均确认0 Error、0 Warning。随后严格按HARDWARE_ACCEPTANCE.md先架空检查B1/B2/B4/B8/B0、同向叠加、反向LF04优先、非零起点偏置回正双向控制和20000安全限幅，再用默认stage 0依次完成四分之一圈、半圈和整圈。保存27列CSV并核对约5919 mm触发制动、首次停稳约387°后显示`ALIGN TO START`并向右转至约397°、补偿误差±2°进入`LAP COMPLETE`、OLED用时与`lap_total_ms`一致、整圈≤20 s且车尾误差≤20 mm。至少3圈后分别按平均有符号车尾距离误差修正5932 mm参考、按`新偏置=旧偏置-平均有符号物理角度误差`修正+37°回正偏置；只有滑行证据变化时才调整13 mm提前量。
```
