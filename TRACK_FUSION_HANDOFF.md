# LF04、编码器与IMU正式融合竞速交接

更新日期：2026-07-30

## 1. 本轮目标与实施状态

当前车辆已经能够使用正式竞速模式完成整圈并自动停车，但最新实车仍有两个明确问题：

1. 停车后车身相对起点方向顺时针偏转约15°，终点姿态不正。
2. 编码器估算的中心行程约6164.1 mm，相对理论圈长6141.6 mm多约22.5 mm。

本轮第一次尝试把正式模式改为“路线前馈先占预算、B0在120 ms归零、冲突时削弱LF04”，实车在第一半圆尚未走完时即严重脱线。最新27列CSV证明：B8已经请求`-120 mm/s`回线时，路线前馈约`+77 mm/s`和航向反馈约`+37 mm/s`仍把最终轮差推成`+65～+84 mm/s`；第一半圆末期的融合航向仅约138°，期望约174°。该策略已撤销。

当前正式模式恢复`22d672e`已验证的LF04方向优先和B0无限保持，只保留路线/航向拆分遥测、360°±5°三周期终点窗口和36 mm停车提前量。最新失控条件已加入回归测试，要求B8 `-120`面对相反的`+77/+37`辅助时最终仍为`-120 mm/s`；主机测试为`18/18 passed`。

2026-07-30首次竞速工程Rebuild暴露SRAM溢出`L6407E: 0xC10 bytes`。随后保留52字节公开记录、27列CSV和600条逻辑容量，把RAM内部记录压缩为44字节：tick与航向保持原精度，x/y以0.25 mm分辨率存储。修改后主机测试和ARMCC再次通过；两个Keil工程的最终链接结果及实车验收仍待完成。

## 2. 仓库与工作区状态

- 仓库：`E:\电赛`
- 分支：`main`
- 当前提交：`22d672e`
- `origin/main`：`22d672e`
- 当前工作区并非干净状态，以下修改属于上一轮“第二弯启用正式融合”任务，不得回滚或覆盖：
  - `HARDWARE_ACCEPTANCE.md`
  - `README.md`
  - `ROBOT_SETUP.md`
  - `code/chassis_track_app.c`
  - `code/chassis_track_mission.c`
  - `tests/chassis_track_line_control_test.c`
  - `tests/chassis_track_mission_test.c`
- 当前μVision窗口打开的是`user/project_track.uvprojx`，不要并行启动新的UV4进程。
- 上一轮可移植验证已经通过：PC主机测试`18/18 passed`，四个受影响源码通过ARM Compiler 5单文件编译，两个Keil工程XML和`git diff --check`通过。
- 两个工程仍需在现有μVision窗口分别执行全量Rebuild，并确认`0 Error(s), 0 Warning(s)`。

新对话开始后必须先执行`git status --short`并阅读`AGENTS.md`、`README.md`、`WIRING.md`、`ROBOT_SETUP.md`和本文，不得使用`git reset --hard`或`git checkout --`清理当前修改。

## 3. 两种竞速固件模式

同一个`project_track.uvprojx`固件包含两种入口，必须先确认进入了哪一种：

| 模式 | 上电操作 | READY显示 | 任务速度与停车 | 控制职责 |
| --- | --- | --- | --- | --- |
| 纯LF04诊断 | 上电前按住上键 | `LF ONLY READY` | 60/120/200/280/350 mm/s，无距离终点，人工中键停车 | LF04决定转向；IMU只记录；编码器负责轮速、里程和堵转 |
| 正式融合竞速 | 上电时不要按上键 | `RACE FUSION` | stage 0直线/弯道均为360 mm/s，6141.6 mm/航向双门自动停车 | LF04、编码器路线和IMU融合航向共同控制 |

恒定350 mm/s、越过6141.6 mm后仍继续运行、最后由中键停车的CSV属于纯LF04诊断。本文分析的最新数据是正式融合模式：目标达到360 mm/s，并在18.22秒自动下达停车命令。

## 4. 最新CSV来源与有效数据范围

原始文件：

```text
C:\Users\MECHREVO\.codex\attachments\d230affc-50aa-4317-aa33-b1cb18dd9ef4\pasted-text.txt
```

串口工具多次发送了`D`，文件内含重复导出和少量串口损坏片段：

- CSV表头出现2次。
- 共解析到398条格式完整的21列记录，但其中包含重复数据。
- 第一段严格递增的完整会话为权威数据，共187条，`elapsed_ms=100…18700`。
- 第一段所有187条记录均为`fusion_active=1`。
- 后续重复段和末尾截断行不得再次计入统计。

CSV仍为21列、每条44字节。`line_correction_mm_s`只记录LF04红外分量，不包含路线前馈和航向反馈产生的最终轮间偏置。判断正式融合的最终效果必须同时查看融合角速度、左右实际速度和PWM。

## 5. 最新一圈量化结果

中心行程使用当前左右标定值估算：

```text
left_mm_per_tick  = 0.1413727
right_mm_per_tick = 0.1434926

progress ≈ 首条记录x方向进度4.345 mm
         + 0.5 × (左tick增量×0.1413727 + 右tick增量×0.1434926)
```

该估算用于分析相对行程；最终A线位置仍必须用外部基准实测，不能直接把CSV的`x_mm/y_mm`当作A线偏差。

| 指标 | 最新结果 | 结论 |
| --- | ---: | --- |
| 完整会话时间 | 18.70 s | 满足当前20 s目标 |
| 自动停车命令时刻 | 18.22 s | 正式任务自动停车有效 |
| 自动停车命令时估算进度 | 6127.9 mm | 接近当前6126.6 mm距离门 |
| 自动停车命令时实际中心速度 | 172.1 mm/s | 停车时仍有明显动能 |
| 完全停稳估算进度 | 6164.1 mm | 相对理论6141.6 mm超出约22.5 mm |
| 停车命令后滑行 | 约36.2 mm | 当前15 mm提前量明显不足 |
| 完全停稳编码器航向 | 375.06° | 相对360°多约15.1° |
| 完全停稳融合航向 | 374.99° | 相对360°多约15.0° |
| 全圈最大融合航向 | 382.20° | 出现在17.92 s、约6068.4 mm |
| 第二弯B0占比 | 约49.0% | LF04在弯中接近一半时间丢线 |
| 第二弯`+120 mm/s`占比 | 约56.9% | 外侧单探头增强使用时间过长 |
| 最后1000 mm的`+120 mm/s`占比 | 约65.7% | 终点姿态偏转的主要直接信号 |
| 最后1000 mm右轮PWM=20000占比 | 约25.7% | 右轮多次达到40%硬限幅 |

编码器航向与融合航向最终只差0.07°，因此终点歪斜不是简单的IMU符号错误或IMU长期漂移；编码器和IMU都认为车辆确实多转了约15°。

## 6. 第二弯与终点关键时序

第二弯理论起点为`3000 + π×500 ≈ 4570.8 mm`。下表中的进度由左右tick估算：

| elapsed_ms | 进度 mm | 融合航向 | LF04 | 红外纠偏 mm/s | 现象 |
| ---: | ---: | ---: | ---: | ---: | --- |
| 14020 | 4711.8 | 212.46° | B2 | +17 | 已进入第二弯，方向正确 |
| 14520 | 4894.2 | 224.38° | B1 | +120 | 第一次长时间外侧增强开始 |
| 15320 | 5174.7 | 257.08° | B1 | +120 | 再次进入满增强 |
| 16720 | 5661.9 | 320.40° | B1 | +120 | 末弯第三次满增强开始 |
| 17320 | 5867.4 | 351.08° | B1 | +120 | 航向已越过终点最低350°门 |
| 17520 | 5938.1 | 361.25° | B1 | +120 | 距离终点仍约204 mm时已超过360° |
| 17720 | 6008.3 | 371.51° | B0 | +120 | B0继续保持B1满增强，路线无法拉回 |
| 17820 | 6041.2 | 376.58° | B2 | +3 | 外侧增强刚释放 |
| 17920 | 6068.4 | 382.20° | B4 | 0 | 反向首周期抑制，航向达到峰值 |
| 18020 | 6090.4 | 380.34° | B0 | -10 | 反向确认后才开始纠偏回来 |
| 18220 | 6127.9 | 376.52° | B0 | 0 | 双门通过并下达停车，实际速度172.1 mm/s |
| 18700 | 6164.1 | 374.99° | B0 | 0 | 完全停稳，仍多转约15° |

## 7. 实施前控制逻辑为何会产生终点歪斜

本轮修改前的正式竞速遵循“红外方向优先”：

- LF04非零纠偏时，只允许编码器/IMU路线辅助同方向叠加。
- 如果路线辅助与LF04方向相反，路线辅助被直接丢弃。
- B0无限期保持最后确认的LF04误差和B1/B8外侧增强状态。
- 普通总偏置上限90 mm/s；B1/B8及其B0保持阶段上限120 mm/s。

这一策略解决了早期“融合反向覆盖红外”的问题，但在最新末弯暴露出另一端的缺陷：当车辆航向已经领先路线、融合层需要负向阻尼时，B1或其后的B0仍持续请求`+120 mm/s`。因为LF04优先级是绝对的，负向路线修正被丢弃，车辆直到检测到对侧B4并完成反向确认后才开始回正。

此外，修改前的终点航向门只检查：

```text
heading_progress_deg >= 350°
```

它能阻止第二弯没转够时提前停车，却不能阻止375°这种过转姿态通过。本轮已用360°±5°连续3周期窗口替换该单边门。

## 8. 当前已实现参数与不可破坏基线

### LF04控制

- 四探头位置：`-40.25/-7.25/+7.25/+40.25 mm`
- 黑线低电平，`B1-B15`有效，`B0`丢线
- 质心误差：`error=-average(active_position)/40.25`
- `Kp=1.2、Ki=0、Kd=0.20`
- 普通纠偏：22%，最大90 mm/s
- B1/B8增强：35%，最大120 mm/s
- 反向确认：2个20 ms周期
- 纯LF04的B0：无限保持最后确认误差，不自动减速或停车
- 正式竞速的B0：与纯LF04一致，无限保持最后确认误差和增强状态
- 正式B1/B8：立即允许120 mm/s，不随持续时间降额
- LF04非零时：只接受同向路线/航向辅助，反向辅助不得抵消或反转LF04
- 实验性弯道方向记忆：纯LF04和正式竞速均关闭

### 正式路线融合

- 直线1500 mm、R500半圆、直线1500 mm、R500半圆
- 总长：6141.6 mm
- stage 0直线/弯道速度：360 mm/s
- R500前馈：`omega_ff=v/500`，360 mm/s时为0.72 rad/s
- 航向反馈：`Kp=4.0`，限幅`±0.35 rad/s`
- 当前终点提前量：36 mm
- 接近区：190 mm
- 当前航向门：累计融合航向连续3周期进入360°±5°
- 到`route+50 mm`仍未满足双门时锁停
- B15只记录，不作为启停或停车门

### 安全限制

- 轮速上限500 mm/s
- PWM绝对硬限幅20000/50000，即40%
- 堵转门8个20 ms周期，约160 ms
- 中键运行中锁定急停
- 编码器继续使用3.3 V和GPIO软件4×解码
- 不得绕过现有急停、堵转、轮速和PWM限制

## 9. 本轮实现摘要

1. 正式任务输出已拆为`route_feedforward_rad_s`、`heading_feedback_rad_s`和`heading_error_deg`，下层融合角速度闭环保持原样。
2. 新正式接口保留路线前馈、航向反馈和LF04三个分量的显式遥测，但最终仲裁恢复LF04方向优先；旧更新函数继续作为纯LF04兼容入口。
3. 最新第一半圆失控回放、B0无限保持、同向叠加、反向LF04优先、左右镜像和旧纯LF04行为均有主机测试。
4. 终点距离门改为`route-36 mm`，航向门改为360°±5°连续3周期；376°拒绝制动，`route+50 mm`锁停保留。
5. CSV扩为27列，新增`mission_progress_mm`、`expected_heading_deg`、`route_feedforward_bias_mm_s`、`heading_feedback_bias_mm_s`、`line_weight_pct`和`final_steering_bias_mm_s`。
6. 600条RAM缓冲保持不变。52字节公开结构通过44字节内部紧凑记录实现，避免首次Rebuild出现的3,088字节SRAM溢出。

不应把终点姿态和停车距离用单一P参数一起掩盖。先验证三源仲裁能够把姿态收敛到窗口，再依据至少3圈外部A线测量微调36 mm提前量。

## 10. 剩余验收指标

- 正式READY必须显示`RACE FUSION`，不得误进`LF ONLY READY`。
- stage 0整圈目标保持360 mm/s，纯LF04参数和入口不变。
- 第一半圆B8/B0阶段不得再出现LF04负向回线而最终轮差为正；车辆必须先完成四分之一圈和半圈验收，再允许整圈。
- 距离终点约190 mm时，航向不得已经超过365°。
- 自动停车命令前融合航向进入360°±5°目标窗口。
- 完全停稳车身航向目标为360°±5°。
- 外部测得A线停车偏差绝对值目标不超过20 mm。
- 编码器航向与融合航向保持连续；IMU失效时仍能平滑退化到编码器。
- 中键急停、160 ms堵转、500 mm/s轮速和PWM 20000硬限制全部保留。
- PC主机测试保持`18/18 passed`；默认与竞速工程全量Rebuild均须为`0 Error(s), 0 Warning(s)`。
- 27列CSV重复导出必须完整，空缓存表头精确为435字节；正式模式`line_weight_pct`固定为100，纯LF04为0。
- 整圈时间≤20.00 s，外部A线停车偏差绝对值≤20 mm。

## 11. 可复制给新对话的剩余任务说明

```text
请先阅读E:\电赛\AGENTS.md、README.md、WIRING.md、ROBOT_SETUP.md和TRACK_FUSION_HANDOFF.md，并执行git status --short，保留当前全部未提交修改。

失败的前馈优先实车CSV位于：
C:\Users\MECHREVO\.codex\attachments\e115e7cd-145a-4902-8148-8018182dc35a\pasted-text.txt

前馈优先、B0归零和冲突削弱LF04的方案已被实车否决并撤销。当前恢复22d672e的LF04方向优先与B0无限保持；保留360°±5°三周期终点门、36 mm提前量和27列CSV。52字节公开结构使用44字节内部紧凑记录，容量仍为600。最新第一半圆B8=-120、路线/航向=+77/+37的回放要求最终=-120，主机测试18/18通过，等待ARMCC和两个Keil工程全量Rebuild。

请在现有μVision窗口分别全量Rebuild user/project_track.uvprojx 和 user/project.uvprojx，均确认0 Error、0 Warning。随后严格按HARDWARE_ACCEPTANCE.md先架空检查B1/B2/B4/B8/B0、同向叠加、反向LF04优先和安全限幅，再按60/120/200 mm/s依次验证直线与第一半圆；四分之一圈和半圈均不偏线后才允许360 mm/s整圈。保存27列CSV并核对终点制动航向355–365°、完全停稳360°±5°、整圈≤20 s和外部A线偏差≤20 mm。至少3圈姿态稳定后才微调36 mm提前量。
```
