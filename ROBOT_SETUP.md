# 通用双轮底盘自检、标定与API说明

本文件说明自检行为、标定原理和公共 API；实际烧录、架空、地面测试和数据签字请逐项填写 [HARDWARE_ACCEPTANCE.md](HARDWARE_ACCEPTANCE.md)。

## 默认自检流程

SW6动作映射已经实测确认，当前构建设置为 `CHASSIS_SW6_MAPPING_DIAGNOSTIC=0`，恢复完整底盘自检。垂直中键对应PB24，方向键不会启动运动；开机保持右键PB25只会进入永久停车的UART诊断页。

### 中键操作

一次有效中键操作是垂直按下0.1–0.3秒后完全松开。程序使用60 ms时间消抖：过快轻触可能被过滤，长按只产生一次事件，下一次操作前必须先松开。IMU校准期间不会接受运动确认；唯一例外是开机前持续按住中键，会在校准完成后进入不驱动电机的滚动标定页。

### OLED提示与操作

| OLED提示 | 用户操作 | 程序动作 |
| --- | --- | --- |
| `KEEP STILL` | 不触碰小车 | IMU采集300个静止样本 |
| `ROLL C=ZERO` | 释放中键后手推标定；中键可重新清零 | 显示左右相对tick和相对BAD，不发送运动命令 |
| `LIFT+PRESS KEY` | 架空两轮，垂直按中键 | 左正、左反、右正、右反、双轮闭环各约1秒 |
| `FUSION F/S STOP` | 12 V断开时手动转动车体；串口发送`S`退出 | 10 Hz记录编码器、IMU与融合结果，不发送运动命令 |
| `GROUND+PRESS KEY` | 放到平地后按中键 | 执行500 mm直线 |
| `PRESS FOR +90` | 确认周围安全后按中键 | 执行+90°原地转向 |
| `PRESS FOR -90` | 按中键 | 执行−90°原地转向 |
| `TEST COMPLETE` | 不再按键 | 自检完成，电机保持停止 |
| 任一电机运行页面 | 按中键 | 立即锁定急停，必须重新上电 |
| `SELF TEST ERROR` | 断开12 V后排查 | 保持停车，不继续后续步骤 |

首次运动前应能够立即断开12 V，架空时不要手持车轮或让线缆靠近轮胎。

### OLED滚动标定模式

1. 物理断开12 V，只保留5 V逻辑供电；将车辆放在样本起点。
2. 开机前按住垂直中键，并在整个IMU校准期间保持车体静止和中键按下。
3. 出现 `ROLL C=ZERO` 后松开中键。首次进入已自动建立零基线。
4. 第二、三行分别显示 `L +0000000`、`R +0000000` 格式的有符号相对tick；第四行显示自基线以来的 `BAD` 增量。
5. 每完成一个样本后记录屏幕；需要下一个样本时按住中键超过60 ms再松开，确认tick与BAD重新从0开始。
6. 标定页内中键不会启动电机，上、左、下、右也无功能。退出只能断电，然后不按中键重新开机。

显示值是相对最近一次清零的增量，不会清除底层累计计数。七位tick超过范围时饱和显示为 `±9999999`。

### 校准与AB状态显示说明

第二行 `L AxBx R AxBx` 显示PB23、PB12、PB4、PB5的实时GPIO电平，不再显示解码器内部缓存。编码器插头全部拔掉时，内部上拉应使其稳定显示 `L A1B1 R A1B1`。固件将每一行先组成最终16字符内容，再通过批量软件I²C写入；不会先显示占位数字0再覆盖真实数字，因此正常情况下不应出现0/1假闪烁。

出现 `MOVE RETRY` 时，第四行显示类似 `G:034 A:101`：

- `G`是陀螺仪模长的0.1 dps单位，`034`表示3.4 dps。
- `A`是加速度模长的0.01 g单位，`101`表示1.01 g。
- 静止校准要求陀螺仪总模长小于3.0 dps、加速度模长处于0.85–1.15 g。

若校准能够正常采样，第四行必须从 `CAL:000/300`连续增加到300。它不会再永久用`CAL:000`掩盖读取错误或主循环超时。

校准结束后，第四行按实测物理通道顺序显示：

```text
K U1L1D1C1R1
```

`U/L/D/C/R`依次表示上、左、下、中、右，对应PA14、PA15、PA24、PB24、PB25。未操作时均为1，操作对应方向时只有自己的位变为0。只有`C`参与确认和急停。

### ICM42688安装方向

当前模块元件面朝下并水平安装。传感器 `+X` 指向车尾；站在车头正前方、面向车尾观察时，传感器 `+Y` 指向观察者右侧，也就是车辆自身左侧；传感器 `+Z` 指向地面。车体姿态坐标保持 `+X` 向前、`+Y` 向左、`+Z` 向上，因此配置为：

```text
source_axis = {X, Y, Z}
axis_sign   = {-1, +1, -1}
```

从上方观察，Fusion库输出仍遵循逆时针正、顺时针负。当前实车运动接口和编码器航向沿用既有约定：`+90°`实测为顺时针，所以融合入口使用独立的`imu_heading_sign=-1`将IMU角速度转换到编码器正方向；该符号转换不改变三维安装映射。IMU每次上电仍必须静止完成300样本校准。

20 ms底盘周期使用陀螺仪短时预测、编码器低频约束形成融合航向；时间常数为1.0 s，融合角速度采用75% IMU和25%编码器。IMU超过100 ms未更新或单周期变化超过25°时，系统连续退化为编码器航向，恢复时不跳变。单轮PID仍只使用编码器，IMU只在底盘运动层参与直线航向、转向/圆弧进度及角速度反馈。

### 按键没有反应时的排查顺序

1. 查看第四行 `K UxLxDxCxRx`，未操作时五位都应为1。
2. 若对应动作不能把自己的位变为0，断开5 V和12 V，检查开关触点、公共GND、焊点和对应GPIO走线。
3. 若一个动作出现多个0，检查信号间短路。
4. `C0`能正常显示但没有启动时，确认页面已经进入`LIFT+PRESS KEY`且按住超过60 ms，并先完全松开后重试。
5. 方向键只用于电平诊断，按上、左、下或右不会启动自检。

更完整的TM-2027六脚定义、内部上拉原理和万用表通断测试见 [WIRING.md](WIRING.md)。

## 编码器验收

软件4×解码在PB23/PB12/PB4/PB5四根线上均使用双边沿中断。AB状态表只接受相邻转换；`00→11`、`01→10`等两位同时变化不增加位置，只增加非法转换计数。

静止时某轮若在 `A0B0` 与 `A1B1` 之间反复切换，表示A、B两位正在同时变化，不是正常正交相位。固件已对双边沿输入启用GPIO硬件迟滞以抑制窄噪声，但不会把这种非法转换计入位置。若BAD仍持续增加，应分别测量两相信号：检查A/B是否误并联、编码器3.3 V与GND是否稳定、插头针序是否正确；长线或开漏输出可在A、B各增加约4.7 kΩ到3.3 V的外部上拉，不能靠软件忽略错误来替代电气修复。

1. 用万用表或示波器确认四路高电平约3.3 V，不超过3.6 V。
2. 拔掉两只编码器插头时，确认四路实测约3.3 V且OLED稳定显示 `L A1B1 R A1B1`。
3. 如需逐脚验证，可将PB23、PB12、PB4、PB5分别通过1 kΩ电阻接GND；对应A/B位应稳定变为0，断开后恢复1。
4. 进入OLED滚动标定页，缓慢手转左轮输出轴一整圈，相对计数绝对值应接近1456。
5. 中键清零后对右轮重复一次，相对计数绝对值也应接近1456。当前实测左轮1456、右轮绝对值1498，BAD增量均为0。
6. 正向推动车体时左右累计计数都应为正。
7. 分别正反转至少10圈，非法转换率应低于0.1%。

方向不一致时优先核对A/B接线。确认接线无误后，只修改 `code/chassis_config.c` 中对应轮的 `encoder_sign` 或 `motor_sign`，不要同时随意交换多组线。

## 轮距与每tick距离标定

默认值：

```text
left mm_per_tick   = 0.1413727
right mm_per_tick  = 0.1434926
effective_track_mm = 214.2
```

`CHASSIS_DEFAULT_MM_PER_TICK=0.1402497` 仍保留为65 mm轮径的理论参考值，不再作为当前左右轮的实车配置。当前 `214.2 mm` 是两轮接地点中心距的首轮实测值。旧值 `128.6 mm` 下，双向 90°命令均只转到约 54°；按 `90 × 128.6 / 214.2 ≈ 54.0°` 计算，两者相互吻合。

### 左右轮独立标定

1. 断开12 V并进入OLED滚动标定页，在实际比赛轮胎、载荷和地面条件下让车辆直线滚动1–2 m。
2. 每次在起点用中键清零显示基线；每个方向至少做5次，记录实际距离和左右相对tick。
3. 对每轮计算 `mm_per_tick = 实际距离 / |相对tick|`。
4. 去掉明显打滑数据，取稳定样本平均值，分别写入左、右轮配置。
5. 再执行1 m直线，基线目标是距离误差不超过1%。

当前用户选择先采用一组正反各1000 mm样本：前进 `L+7037/R+6951`，后退 `L-7110/R-6987`。按合并距离除以合并绝对计数得到：

```text
left  = 2000 / (7037 + 7110) = 0.1413727 mm/tick
right = 2000 / (6951 + 6987) = 0.1434926 mm/tick
```

这是一组正反方向的暂定值，不等同于已经完成多次重复性验证。写入后必须重新验证500 mm直线和双向90°；若距离超差或偏移明显，应增加滚动样本，不先调整PID。

### 有效轮距标定

1. 先重复执行双向90°，再执行双向360°。
2. 使用外部角度基准记录实际转角，按 `新轮距 = 旧轮距 × |命令角度| / |实际角度|` 计算修正值。
3. 实际转角小于命令角度时增大 `effective_track_mm`；实际转角大于命令角度时减小。
4. 每次只调整一个参数并重复至少5次。
5. 基线目标是90°转向误差不超过2°。

有效轮距包含轮胎侧滑等综合影响，通常不等于尺量的几何轮距。更换地面、轮胎或载荷后应重新标定。

## 轮速参数

默认20 ms控制周期下：

| 参数 | 默认值 |
| --- | --- |
| KP | 700 |
| KI | 55 |
| KD | 0 |
| 前馈 | 6000 |
| PID输出限制 | ±11500 |
| PID积分限制 | ±5750 |
| 默认自检最大合成输出 | 约±17500（35%） |
| 电机驱动绝对PWM硬限制 | ±20000（40%，仅竞速配置可用满） |
| 速度滤波系数 | 0.35 |

先标定机械方向和编码器比例，再调整PID。推荐从低速架空测试开始，观察目标轮速、滤波轮速和PWM，不要用提高PWM限幅掩盖接线、摩擦或供电问题。

## 非阻塞API

初始化并持续轮询：

```c
if (chassis_init(&g_chassis_default_config) == ML_STATUS_OK) {
    while (1) {
        chassis_poll();
        status = chassis_get_status();
    }
}
```

运动命令：

```c
chassis_set_wheel_speed(left_mm_s, right_mm_s);
chassis_update_wheel_speed(left_mm_s, right_mm_s);
chassis_set_velocity(linear_mm_s, angular_rad_s);
chassis_update_velocity(linear_mm_s, angular_rad_s);
chassis_move_mm(distance_mm, max_speed_mm_s);
chassis_rotate_deg(angle_deg, max_angular_deg_s);
chassis_arc(radius_mm, angle_deg, max_speed_mm_s);
chassis_stop();
chassis_emergency_stop();
chassis_reset_pose(x_mm, y_mm, heading_deg);
```

- 定距允许正负距离。
- 当前实车约定中，转向角正值为从车顶向下看的顺时针，负值为逆时针；编码器航向和融合航向均使用该约定。
- `chassis_update_wheel_speed()`只更新已运行的直接左右轮目标，不重置PID或堵转计数，并始终保持`CHASSIS_MODE_WHEEL_SPEED`，不会叠加IMU角速度反馈；首个命令仍调用`chassis_set_wheel_speed()`。
- `chassis_update_velocity()`只更新已运行的`VELOCITY`命令，不重置PID、堵转计数或融合基线；赛题20 ms循环必须使用它，首个命令仍调用`chassis_set_velocity()`。
- 圆弧半径必须大于半轮距，角度正负决定转向方向，车辆默认向前沿弧线运动。
- 新的定距/转向/圆弧命令在另一个轨迹命令运行时返回 `ML_STATUS_BUSY`。
- 急停会锁定后续运动，必须重新初始化或重新上电。
- 默认自检通过`chassis_set_imu_sample()`同步输入原始Yaw、映射且扣除零偏后的车体Z轴角速度和时间戳；旧`chassis_set_imu_yaw()`仅保留为不启用融合的兼容接口。

## 可选LF04调用

```c
line_sample_t sample = line_sensor_read();
if (!sample.io_fault && !sample.lost) {
    uint8_t active_sensors = sample.black_bits;
}
```

竞速工程由`chassis_track_line_control`按`-40.25/-7.25/+7.25/+40.25 mm`计算四路有效探头的物理质心；`error=-average(position)/40.25`。红外环使用`Kp=1.2、Ki=0、Kd=0.20`的PD控制，首个有效样本不产生D冲击，PID输出限制`±1`。普通位型使用22%纠偏和90 mm/s总偏置上限，B1/B8最外侧单探头继续立即使用35%和120 mm/s。直接左右反向首周期抑制为0，连续第二周期或随后B0确认；居中和同侧变化立即接受。B0无限期复用最后确认误差且保持当前任务速度及外侧增强，首次B0按误差0且不增强处理。

纯LF04模式使用同一PD，但额外启用弯道记忆配置：确认B1/B8后锁存方向，质心PD作为弯道基础偏置的横向残差，B6/B9保持`min(速度×0.31,110)`，B15清除记忆。记忆态B0在前600 ms保持`min(速度×0.35,120)`，600–1200 ms线性过渡到保持值，之后无限保持；350 mm/s下为120、约114.25、108.5 mm/s。弯道退出使用请求速度积分行程门：前1200 mm不允许对侧内探头清除记忆，达到门限后还须连续5个20 ms周期确认对侧非外探头；B0不能确认退出。首个反向确认周期仍为零纠偏，正式竞速不启用这组记忆参数。

## RAM遥测与UART0

### UART0安全诊断模式

1. 物理断开12 V，开机前按住右键PB25，并保持车体静止完成IMU的300样本校准。
2. 出现`UART DIAG STOP`后松开右键。该状态不会执行任何运动命令，中键和其他方向键也无效；退出只能断电后不按右键重启。
3. OLED第二行正常应为`BRD I0002 F11`，表示4 MHz MFCLK、16×过采样和115200波特率的实际硬件分频。
4. 固件每秒自动发送`UART0,115200,TX,nnnn\r\n`，无需先发送命令。第三行显示横幅次数和TX超时数。
5. 发送一个ASCII `D`时，第四行应显示`RX44 E00 O0000`，串口回复`RX,44,ERR,00\r\n`。诊断模式把所有输入都当作测试字节，不会执行`D/C`、导出CSV或清空RAM。
6. 横幅乱码但OLED分频正确且TX次数增加时，检查PA10到适配器RX、共地、3.3 V TTL和约`8.68 us/bit`波形；横幅正确但不是`RX44`时，检查适配器TX到PA11。`E`非0表示overrun/break/parity/framing/noise，`O`为软件队列溢出。

诊断通过后必须断电、不按右键正常启动，再进行空缓存CSV验证。自动横幅只存在于诊断模式，不会混入正常CSV。

运动时每100 ms记录一次；`LIFT+PRESS KEY`页面发送`F`后，停车状态也按相同周期记录。竞速制动开始后会自动继续被动采样，直到两轮连续3个20 ms周期低于20 mm/s。共600条，缓冲满后停止新增并置溢出标志，不覆盖旧数据。立即采样若与上一条时间戳相同会覆盖上一条，较小时间戳会被拒绝，因此CSV时间戳严格递增。每条44字节：

- 时间戳、左右累计tick：32位整数。
- x/y：32位浮点毫米。
- 编码器/融合航向：有符号32位0.01°累计值，可完整显示180°、270°和约360°；原始IMU Yaw仍为有符号16位回绕诊断值。
- 融合角速度：有符号0.01°/s定点数，超过范围时饱和。
- 融合有效位：`0/1`，表示该样本中IMU是否实际参与融合。
- 目标/实际中心速度：有符号0.1 mm/s定点数。
- 左右PWM：驱动端实际施加值的绝对计数，范围0–20000。
- LF04位型、纠偏和状态：低4位十进制输出、有符号整数`line_correction_mm_s`，以及`line_usable/line_recovering/line_pattern_invalid`三个标志。

`chassis_init()`会在每次上电初始化时清空 RAM。默认自检的架空五段和地面运动都会记录；未启用`F`调试时，等待页面和完全停车不写入，所以仍可利用等待页面产生的时间戳间隔区分运动段。五段架空、500 mm和双向90°的总运动时间远低于约60秒容量，不需要在地面连接电脑或发送 `C`。

UART驱动显式启用硬件FIFO，并把RX中断阈值设为1字节；`uart_try_read()` 在软件队列为空时还会直接轮询硬件RX FIFO，避免单字节RX中断未到达软件队列时丢失命令。单独发送一个ASCII `D`或`C`必须立即处理，不需要附加 `0`、连续字符或换行。TX每个字节在写入前后都等待UART `BUSY`清零，成功返回即表示该字节已经发送完成；任何超时都会立即终止本次CSV导出。

每次重新构建和烧录后，先保持12 V断开：静止完成IMU校准并停在 `LIFT+PRESS KEY`，通过COM3发送单个 `D`。空缓存也必须返回且只返回完整表头：

```text
time_ms,left_ticks,right_ticks,x_mm,y_mm,encoder_heading_deg,fused_heading_deg,imu_yaw_deg,fused_yaw_rate_dps,fusion_active,elapsed_ms,lap_total_ms,target_center_mm_s,actual_center_mm_s,left_pwm_count,right_pwm_count,line_bits,line_correction_mm_s,line_usable,line_recovering,line_pattern_invalid
```

首次复验建议开启接收十六进制显示。空缓存表头必须恰好为298字节，以`74 69 6D 65 5F 6D 73`开头、以`69 64 0D 0A`结尾；文本模式应显示上述完整21列表头。重复发送 `D`应重复得到同一结果。若无输出或出现 `~`、`}k`等乱码，不得接通12 V或重跑地面测试；保存第一次导出的原始十六进制，并测量PA10空闲电平及约`8.68 us/bit`的实际位宽。

### 无电机航向融合调试

1. 物理断开12 V，只保留5 V，正常开机并静止完成IMU校准，停在`LIFT+PRESS KEY`。
2. 发送`C`显式清空旧记录，再发送`F`；`F`不会自行清空RAM，也不会发送电机命令。
3. OLED显示`FUSION F/S STOP`。第二行`E/F`为编码器/融合航向，第三行`I/R`为原始IMU Yaw/融合角速度，第四行`IMU ON/OFF`和`N`表示融合有效性与记录数。
4. 从车顶向下顺时针手动转动约90°：原始`I`应为负变化，`F/R`应为正变化；反向转动时符号相反。轮子不计数时，保持静止后融合航向会按约1 s时间常数平滑回到编码器约束。
5. 发送`S`停止采样并返回`LIFT+PRESS KEY`，再发送`D`导出。采样期间`D/C`返回忙，中键和方向键都不会启动运动。

`F/f`只允许从`LIFT+PRESS KEY`进入，`S/s`只在融合采样页生效。任何运动入口、普通停车或急停都会防御性关闭空闲采样。右键开机进入的`UART DIAG STOP`仍把所有字节仅作为RX诊断，不执行`F/S/D/C`。

离线现场流程：

1. 保持独立5 V逻辑供电，按正常顺序完成IMU校准和架空五段自检。
2. 将车放到地面，在 `GROUND+PRESS KEY`、`PRESS FOR +90` 和 `PRESS FOR -90` 等待页面各停留数秒，再按中键执行500 mm、`+90°`和`-90°`并完成外部测量。
3. `TEST COMPLETE` 完全停车后先断开12 V，但持续保持5 V；不要复位、重新烧录或切换可能短暂掉电的电源。
4. 保持5 V将车搬回电脑旁。停车搬运不会新增记录，也不会改变已经存入RAM的样本。
5. 使用3.3 V TTL USB转串口，适配器RX接PA10/UART0 TX、TX接PA11/UART0 RX，并连接GND；适配器VCC悬空，不得反向给开发板供电。
6. 串口设为115200、8N1、无流控，先发送ASCII `D`并保存CSV。核对表头和数据行完整后，才允许发送ASCII `C`清空记录和溢出标志。

RAM是易失存储器。测试完成后若断开5 V、复位或重新烧录，尚未导出的数据无法恢复。不要在导出前发送 `C`。

UART0命令：

- 发送ASCII `D`：导出带表头CSV。
- 发送ASCII `C`：清空记录和溢出标志。
- 在`LIFT+PRESS KEY`发送ASCII `F`：开始无电机融合采样，不清空RAM。
- 在`FUSION F/S STOP`发送ASCII `S`：停止融合采样并返回等待页。

运动状态下不会执行导出或清空命令。分析时按时间戳间隔和左右轮运动特征分出架空五段、500 mm、`+90°`和`-90°`；分别比较`encoder_heading_deg`、`fused_heading_deg`和原始`imu_yaw_deg`。原始IMU Yaw与当前软件航向正号相反，比较时先乘以-1；融合航向应连续并接近外部实测角度。没有磁力计，因此长期打滑仍必须依靠外部角度基准判断。

## 测试与构建

PC主机测试覆盖：

- 16种AB转换、正反完整相位和非法跳变。
- 直线、原地旋转、圆弧和双航向里程计。
- 编码器–陀螺仪互补融合、符号转换、超时回退和恢复连续性。
- 加减速、三周期完成、堵转与急停。
- 左右速度滤波、换向PID复位。
- 遥测容量、溢出、44字节布局、累计航向、重复时间戳覆盖、21列CSV、纠偏量正负/饱和、停止态10 Hz采样和运行中`BUSY`。
- LF04全部16个位型的四路物理质心、单周期响应、普通22%/90 mm/s与B1/B8增强35%/120 mm/s、红外方向优先仲裁、B0状态记忆与无限保持，以及终点速度上限。
- 竞速A点零位快照、四赛段期望航向、航向纠偏限幅、6126.6 mm/350°双门和`route+50 mm`锁停。
- 自检最终16字符格式，确保实时AB、CAL、MOVE和STATUS不会通过占位数字二次覆盖。

从仓库根目录运行全部18组 PC 主机测试（需要 `gcc`）：

```text
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\run_host_tests.ps1
```

主机测试不能替代 ARMCC 全量构建、烧录或实车验收。

Keil完整构建：

```text
D:\Keil_v5\UV4\UV4.exe -b user\project.uvprojx -j0
```

提交和烧录基线为 `0 Error(s), 0 Warning(s)`。算法能提高分辨率和重复性，但无法消除轮胎打滑、减速箱回差、轮径变化和地面差异。

## 顺时针胶囊赛道竞速任务

竞速入口位于`user/project_track.uvprojx`，默认`user/project.uvprojx`仍是安全自检。竞速配置使用PID输出`±14000`、积分`±7000`和前馈6000，底层最终饱和在`±20000`；最大轮速500 mm/s、160 ms堵转和中键锁定急停均不变。

### 纯LF04循迹诊断

1. 仍烧录`project_track.uvprojx`，上电前按住上键；车辆先放白底并保持静止，等待白底稳定门和IMU校准完成，再完全松开上键。普通开机不按上键仍进入整圈竞速。
2. `LF ONLY READY`页用上/下键选60、120、200、280或350 mm/s，默认60。方向键仅在READY页生效，运行中不会切速或启动。
3. 准备好后直接按中键启动；READY页不检查当前B0-B15位型。若从B0启动且尚无有效样本，四路误差按0处理并直行。中键仍是唯一启动键，运行中按下会锁定急停。
4. 模式只用LF04决定左右轮差速；编码器继续执行轮速PID、累计里程和约160 ms堵转保护，IMU只写入遥测而不参与转向。目标速度以400 mm/s²升降，不再设置自动停车距离，可连续运行完整一圈。
5. `B0`不改变当前任务速度，持续复用最后有效四路误差，直到任意探头重新识别黑线；不会因持续时间锁停。所有`B1-B15`均按物理质心计算。若先见B1/B8，纯LF04会按600 ms全增强、600 ms线性衰减到保持值；前1200 mm请求行程内即使确认对侧内探头也只叠加PD残差，不能改变弯道方向，随后B0仍按原方向恢复。达到1200 mm后，对侧非外探头连续100 ms或B15才清除记忆。B0期间高速转向可能持续偏离赛道，测试人员必须随时准备按中键或断12 V。
6. 回到起点后按中键执行锁定急停；固件结束遥测会话并写入`line_correction_mm_s=0`的最终快照。保持5 V并发送单个`D`导出21列CSV，确认后才可发送`C`；随后断电重启并重新进入纯LF04模式。依次按60→120→200→280→350 mm/s验证，先短距离确认方向，再逐级完成整圈；任一档出现堵转、振荡或完全脱线都停止升级。

RAM遥测约保存前60秒且缓冲满后不覆盖。60 mm/s跑完整圈约需102秒，因此该档CSV可能只含前60秒，但缓冲满不会停止车辆；120 mm/s及以上的标称整圈时间在容量范围内。

默认自检工程的开机右键UART安全诊断入口没有改变；纯LF04入口只存在于竞速工程且只使用开机上键选择。

上电时把车辆放在白底且保持静止。LF04固定采用白底高、黑线低逻辑，不再自适应记录开机极性；原始`RF`必须连续稳定10个20 ms样本，并与IMU校准同时完成，才会出现`READY PRESS C`。IMU已完成但白底未稳定时显示`LF WHITE WAIT`、当前原始位型和稳定计数，电机保持锁定。进入READY后再把车移到A点、车头对准AB顺时针方向；程序不识别A点，当前LF04位型也不参与启动许可。按中键会把当前编码器里程和融合航向定义为竞速零点并立即启动，状态依次为AB、BC、CD、DA和终点低速检查。

路线长度为`2×1500+2π×500=6141.6 mm`。基线直线/弯道均为360 mm/s；验证后把编译宏`CHASSIS_TRACK_SPEED_STAGE`设为1或2，可把直线依次提高到380或400 mm/s，弯道始终保持360 mm/s。中心速度以400 mm/s²变化；终点层按剩余制动距离计算速度上限，保证双门前达到100 mm/s，LF04恢复层只能进一步降速，不能把终点请求抬回200 mm/s。编码器里程生成连续期望航向：AB为0°，BC从0°升至180°，CD为180°，DA从180°升至360°。AB/CD角速度前馈为0，BC/DA为`+v/500`；期望航向与融合航向误差使用`Kp=4.0`和`±0.35 rad/s`限幅修正，底盘速度模式继续执行融合角速度反馈。

终点确认同时要求累计中心里程达到`6141.6-15 mm`且相对启动零点的融合顺时针航向达到350°。航向先满足时等待距离；距离先满足时保持100 mm/s等待航向。到`route+50 mm`仍未同时满足会立即停车并显示`FAULT LAP CHECK`。两轮实测速度连续3周期低于20 mm/s才停止计时。`finish_stop_lead_mm=15`是100 mm/s初始制动提前量，必须结合实车停车数据调整；完成页偏差仍需用外部A线复核。

LF04从左到右为PA31/PA12/PB8/PA27，物理中心位置为`-40.25/-7.25/+7.25/+40.25 mm`。所有低电平探头参与质心平均，归一化误差为`-average(position)/40.25`；左侧为正、右侧为负，B6/B9/B15为0。所有`B1-B15`均有效，只有`B0`表示丢线。控制器每20 ms响应，使用`Kp=1.2、Ki=0、Kd=0.20`的PD，PID输出限制`±1`且不使用I项、中心死区、全局低通或轮差斜坡。普通位型使用22%纠偏和90 mm/s总偏置上限，B1/B8最外侧单探头立即使用35%和120 mm/s。直接左右反向首周期有效误差为0，连续第二周期或随后B0确认；居中与同侧变化立即接受。B0保持最后确认误差、位型和当前任务速度，B1/B8时继续增强。编码器/IMU辅助不得反转非零保持方向；首次B0按居中误差且不增强处理。

三类传感器按职责仲裁：编码器先生成里程、赛段和路线期望航向，编码器+IMU融合再给出航向/角速度辅助偏置，LF04质心最终决定横向转向方向。红外误差非零时，只接受同向的融合偏置，反向量直接丢弃；普通位型同向叠加后不超过90 mm/s，B1/B8及其随后B0增强状态不超过120 mm/s。质心为0时保留路线融合修正。LF04不覆盖编码器里程或终点双门；B0保持最后误差和增强模式且不产生超时故障；IMU失效时融合层无跳变退化为编码器。中键急停、堵转、500 mm/s轮速和PWM 20000限幅始终高于三者。

通用GPIO输入初始化会先关闭输出使能，竞速初始化、READY到RUN的启动边沿和每次20 ms读取前还会重新配置四路上拉输入。RUN页第四行每秒交替显示`Rr Ww Bb`和`LFb Ln DnHn`：`R/W/B`依次为原始GPIO、固定白底期望和归一化黑线位，`L`为B0保持状态，`D/H`为距离门和航向门。正常白底必须显示`RF WF B0`；从左到右逐路覆盖时原始值为`RE/RD/RB/R7`，归一化位依次为`B1/B2/B4/B8`。输入重申失败显示`LF GPIO FAULT`；B0只累计并显示保持时间，不会触发`LF LOST STOP`。电机运行时四块LF04的检测指示灯可能微亮，不能据此判定GPIO持续低电平，必须以OLED原始/归一化位型为准。

竞速和纯LF04模式都按有效中键边沿启动遥测会话，`elapsed_ms`以该边沿为零点；竞速`lap_total_ms`取启动到正常停稳，纯LF04取启动到中键锁停。CSV共21列，`line_bits`后的`line_correction_mm_s`为有符号红外纠偏量；末三列为`line_usable,line_recovering,line_pattern_invalid`。B1-B15时`line_usable=1`，B0时`line_recovering=1`，兼容列`line_pattern_invalid`固定为0。运行与制动期间`D/C`不导出、不清空，每秒最多回复一次短`BUSY`；完全停车后保持5 V、断开12 V，SSCOM关闭方向标记、时间标签、定时发送和自动换行，只发一次ASCII `D`或十六进制`44`并等待完整导出；串口工具显示的“发→/收←”和时间标签不是固件内容，不能保存进CSV。
