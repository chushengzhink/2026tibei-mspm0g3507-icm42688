# 双轮差速底盘接线表

本表对应立创·天猛星 MSPM0G3507 开发板、现有扩展板、TB6612和两只MG513X霍尔编码器电机。所有电源负极与信号地必须共地。

## 电源

| 电源轨 | 扩展板位置 | 用途 |
| --- | --- | --- |
| 12 V | H2或H5的`+12V` | TB6612 `VM`和电机功率 |
| 5 V | H3的`+5V` | 核心板输入、OLED、TB6612 `VCC/STBY` |
| 3.3 V | H4的`3V3` | 两路编码器、LF04、ICM42688 |
| GND | H1或其他GND | 所有模块公共地 |

TB6612使用 `VM=12V`、`VCC=5V`、`STBY=5V`。编码器VCC必须接H4的3.3 V，不得接U9/U12原有的5 V脚。核心板从5 V输入后自行稳压到3.3 V，并不表示普通I/O可以接收5 V编码器方波。

上电后先测量：

- H3对GND约5.0 V。
- H4对GND约3.3 V。
- 四路编码器高电平约3.3 V且不得超过3.6 V。
- 12 V不能进入5 V或3.3 V网络。

## 左轮：电机A与编码器A

左轮使用U12。

| 功能 | MCU/驱动 | MG513X线束 |
| --- | --- | --- |
| PWM | PA28 / TIMG7_CH0 | TB6612 PWMA |
| 方向1 | PA13 | TB6612 AIN1 |
| 方向2 | PB26 | TB6612 AIN2 |
| 电机输出 | U12.1 AO1、U12.6 AO2 | 电机+、电机− |
| 编码器A相 | PB23 / E1A / U12.4 | MG513X脚3 |
| 编码器B相 | PB12 / E1B / U12.3 | MG513X脚4 |
| 编码器GND | U12.2 | MG513X脚5 |
| 编码器VCC | H4 3V3飞线 | MG513X脚2 |

U12.5是5 V，编码器VCC导线必须从该位置退针并绝缘，再飞线到H4 3V3。MG513X的脚1/脚6是电机线，不能误接编码器电源。

## 右轮：电机B与编码器B

右轮使用U9。

| 功能 | MCU/驱动 | MG513X线束 |
| --- | --- | --- |
| PWM | PB20 / TIMG12_CH0 | TB6612 PWMB |
| 方向1 | PB9 | TB6612 BIN1 |
| 方向2 | PB7 | TB6612 BIN2 |
| 电机输出 | U9.1 BO1、U9.6 BO2 | 电机+、电机− |
| 编码器A相 | PB4 / E2A / U9.4 | MG513X脚3 |
| 编码器B相 | PB5 / E2B / U9.3 | MG513X脚4 |
| 编码器GND | U9.2 | MG513X脚5 |
| 编码器VCC | H4 3V3飞线 | MG513X脚2 |

U9.5同样是5 V，处理方式与U12.5相同。

## 板载与调试接口

| 功能 | MCU引脚 | 说明 |
| --- | --- | --- |
| OLED SCL/SDA | PB2 / PB3 | SSD1306软件I²C，地址0x3C |
| SW6五向键 | PA14 / PA15 / PA24 / PB24 / PB25 / GND | 上/左/下/中/右，内部上拉、低有效 |
| UART0 TX/RX | PA10 / PA11 | 115200、8N1、无流控；停车后CSV导出 |
| LED1 | PB14 | 低电平点亮 |

### SW6五向按键实测映射

SW6有五个GPIO信号和一个公共GND。下表是2026-07-28在已焊接实物上确认的映射，不再使用未经验证的按钮本体脚号。

| 原始通道 | MCU引脚 | 机械动作 | 按下时原始显示 |
| --- | --- | --- | --- |
| 1 | PA14 | 上 | `01111` |
| 2 | PA15 | 左 | `10111` |
| 3 | PA24 | 下 | `11011` |
| 4 | PB24 | 垂直中键 | `11101` |
| 5 | PB25 | 右 | `11110` |

| 公共端 | 连接 |
| --- | --- |
| GND | 五个触点共用的系统地 |

五个GPIO不需要外接下拉，也不得连接5 V。正常按键动作应当把其中一个GPIO通过SW6公共端接到GND，使该通道从约3.3 V变为约0 V。

#### 万用表检查

断电检查：

1. 切断5 V和12 V，万用表调到通断档或低阻档。
2. 先找出PCB上直接连接GND的SW6焊盘，测量该焊盘与任一GND测试点，必须持续导通或接近0 Ω。
3. 黑表笔保持在该GND焊盘，红表笔依次测另外五个信号焊盘；执行五种机械动作时记录哪一个焊盘接近0 Ω。
4. 若任何动作都不导通，检查公共GND、开关封装方向、虚焊和器件损坏；不要根据旧文档中的SW6脚号飞线。
5. 分别检查五个信号焊盘到PA14、PA15、PA24、PB24、PB25的通断，只修复确认存在的断路。

上电检查：

1. 暂时断开12 V电机电源，只给核心板和逻辑部分供5 V。
2. IMU校准完成后，第四行未操作时应显示 `K U1L1D1C1R1`。
3. 依次操作五个方向，只有对应字母后的数字应变为0；例如垂直按压时显示`C0`。
4. 若五种动作都保持1，优先检查SW6公共GND；若一次出现多个0，检查信号短路或封装焊接。
5. PB24垂直中键用于自检确认、运行中急停，以及12 V断开时进入OLED滚动标定页和清零显示基线。正常运行中方向键只显示电平且不触发运动；开机保持右键PB25会在IMU校准后进入永久停车的`UART DIAG STOP`，退出只能断电重启。

UART0连接3.3 V TTL USB转串口时必须交叉TX/RX并共地，适配器VCC不连接。串口工具发送单个ASCII字符：`D`导出CSV，确认保存后用`C`清空RAM记录；正常启动并停在`LIFT+PRESS KEY`时，`F`进入无电机融合采样、`S`退出。`F`调试前必须物理断开12 V；电机运行或竞速制动期间`D/C`均不会执行。

## ICM42688（仅记录）

| ICM42688 | 扩展板/MCU |
| --- | --- |
| VCC | H4 3V3 |
| GND | 公共GND |
| AD0/MISO | GND，地址0x68 |
| SDA/MOSI | PA0 |
| SCL/SCLK | PA1 |
| CS、INT1、INT2 | 不接 |

模块使用软件I²C。SDA、SCL空闲电平应接近3.3 V；若模块没有上拉，分别增加约4.7 kΩ到3.3 V。映射且扣除零偏后的Z轴角速度参与航向融合和运动层角速度反馈；原始Yaw保留作遥测诊断。

## 可选LF04

LF04必须使用3.3 V，H6.9是5 V，不能用作LF04电源。
四路数字输入启用MCU内部上拉，可兼容常见开漏输出；模块若已有3.3 V上拉不会冲突。任一输出高电平仍不得超过3.6 V。

| LF04 | 扩展板 | MCU |
| --- | --- | --- |
| VCC | H4 3V3 | — |
| GND | H6.10 | GND |
| O1最左 | H6.8 / C1 | PA31 |
| O2左中 | H6.7 / C2 | PA12 |
| O3右中 | H6.6 / C3 | PB8 |
| O4最右 | H6.1 / C8 | PA27（PINCM60普通GPIO输入） |

## 安全上电顺序

1. 断开12 V和两个电机插头，检查5 V、3.3 V、GND无短路。
2. 只接5 V，确认H3和H4电压正确。
3. 接入编码器、IMU和可选LF04，确认所有信号高电平均不超过3.6 V。
4. 手动慢转左右轮，用示波器观察四路AB相均有0–3.3 V方波。
5. 架空车轮后再接12 V电机电源。
6. 上电保持小车静止约3秒；程序不会自动驱动电机。
7. 按 `ROBOT_SETUP.md`逐步完成架空和落地测试。

MG513X堵转电流可能超过TB6612持续能力。底层40%绝对软件限幅和约160 ms堵转保护只能降低风险，不能替代正确的驱动选型、散热和保险措施。默认安全自检配置仍限制在原35%；40%只供通过架空验收后的独立竞速工程使用。

LF04四路在竞速工程中是横向转向的优先控制源。模块必须横向垂直于车身中心线固定。GPIO输入配置会先关闭遗留输出使能，并在初始化、启动边沿和每次20 ms采样前把PA31/PA12/PB8/PA27重新确认为普通GPIO上拉输入。本车模块固定为白底高、黑线低，白底期望始终为`WF`；只有原始`RF`连续稳定10个20 ms样本且IMU校准完成才进入READY。READY页和RUN诊断页的`R/W/B`分别是原始GPIO、固定白底期望和归一化黑线位；红外不参与A点识别或终点判定。任一轮目标仍限制在500 mm/s以内。

竞速固件开机采样上键PA14：上电前保持上键会选择纯LF04诊断模式，完成白底/IMU校准并松开上键后，READY页不检查当前红外位型，按中键即可启动；普通开机不按上键仍是整圈竞速。上/下键只在诊断READY页选择60/120/200/280/350 mm/s，运行中不切换控制源或速度。该模式不按距离自动停车，回到起点后由中键锁定急停；仍保留编码器、3.3 V供电、堵转、500 mm/s轮速和20000 PWM硬限制，不能为了“纯红外”断开编码器。

按“从车头看向车尾”，PA31/PA12/PB8/PA27依次位于`-40.25/-7.25/+7.25/+40.25 mm`。软件不再合并左右组，而是对所有低电平探头取位置平均，并用`error=-average(position)/40.25`得到归一化误差。左侧黑线使左轮减速、右轮加速，右侧完全对称；B6、B9、B15居中。所有`B1-B15`均为有效输入，只有`B0`表示丢线。控制器每20 ms响应，采用`Kp=1.2、Ki=0、Kd=0.20`；普通位型使用22%纠偏和90 mm/s总偏置上限，B1/B8最外侧单探头仍立即使用35%和120 mm/s。左右方向直接反转时首周期抑制为0，第二个同方向样本或随后的B0才确认；居中与同侧位型立即接受。正式竞速的编码器/IMU偏置只能同向增强非零红外误差，并服从当前90或120 mm/s动态上限。B0按当前任务速度无限期保持最后确认误差及外侧增强状态。启动后尚无有效误差时按居中处理。电机运行时指示灯微亮不等于GPIO持续低电平，必须以OLED原始/归一化位型为准。

纯LF04和正式竞速当前都不启用实验性弯道方向记忆。纯LF04只执行质心PD、B1/B8外侧增强和B0最后误差保持；新有效位型经反向确认后立即替换旧方向。原31%/110 mm/s保持曲线和1200 mm退出门曾在实测中跨入直线继续输出大轮差，现仅保留为关闭的实验配置。正式竞速额外使用编码器/IMU路线辅助，但辅助仍不得抵消或反转非零红外命令。

竞速按中键把当前编码器里程和融合航向定义为A点零点，终点使用整圈编码器里程和累计融合航向判断，因此不再配置`sensor_to_axle_mm`。停车误差通过左右`mm_per_tick`、有效轮距、`finish_stop_lead_mm`和实测整圈数据调整。
