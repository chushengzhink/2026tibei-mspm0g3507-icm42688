# MSPM0G3507 双轮底盘项目指引

本文件适用于仓库根目录及其所有子目录。后续 Agent 开始工作前，应先阅读本文件，并结合 `README.md`、`WIRING.md` 和 `ROBOT_SETUP.md` 核对当前实现与实车状态。

## 项目概况

- 核心板：立创·天猛星 MSPM0G3507 开发板。
- 电机驱动：TB6612 双路驱动。
- 电机：两只 MG513X 霍尔编码器减速电机。
- 编码器 VCC 使用 3.3 V，禁止改接 5 V。
- 默认工程是与具体赛题无关的双轮差速底盘模板。

当前底盘软件包含：

- 四路 GPIO 双边沿中断的软件 4× 正交编码器解码。
- 左右轮独立 PID、前馈和速度滤波。
- 差速里程计、定距、原地转向、圆弧及轮速控制。
- 安全自检、堵转保护、急停、OLED 状态显示。
- ICM42688 姿态记录和 RAM 遥测，停车后可通过 UART0 导出。

## 已确认的硬件映射

SW6 五向键曾因焊接问题全部无响应，焊接现已修复。以下映射均由实板测量确认，不要再根据开关封装脚号猜测：

| 动作 | MCU 引脚 | OLED 原始通道值 |
| --- | --- | --- |
| 上 | PA14 | `01111` |
| 左 | PA15 | `10111` |
| 下 | PA24 | `11011` |
| 中键（垂直按压） | PB24 | `11101` |
| 右 | PB25 | `11110` |

- OLED 正式显示顺序为 `K U1L1D1C1R1`。
- 五路输入均使用内部上拉、低电平有效。
- 只有 PB24 垂直中键用于等待页面确认、电机运行中的锁定急停，以及滚动标定模式的开机进入和基线清零。
- 上、左、下、右不得启动运动；唯一例外是开机保持右键PB25，在IMU校准完成后进入永久停车的UART诊断页。
- `CHASSIS_SW6_MAPPING_DIAGNOSTIC` 当前应保持为 `0`，以启用完整自检。

编码器引脚保持为：

| 车轮 | A 相 | B 相 |
| --- | --- | --- |
| 左轮 | PB23 | PB12 |
| 右轮 | PB4 | PB5 |

## 不得随意改变的安全与算法基线

- MG513X 默认参数为 13 PPR、1:28 减速比，软件 4× 后理论值为 `1456 tick/轮`。
- 65 mm 轮径的理论值为 `0.1402497 mm/tick`；该值只用于初次测试，最终应分别标定左右轮。
- PWM计数满量程为50000，底层绝对硬限制为`±20000`即40%；默认安全自检仍通过PID与前馈配置限制在约35%，40%只用于独立竞速目标。
- 正式竞速PID输出限制为`±14000`、积分限制为`±7000`，继续叠加原6000前馈并受20000绝对上限约束。
- 堵转阈值为 8 个 20 ms 控制周期，约 160 ms。
- 继续使用 GPIO 软件 4× 解码，不切换到硬件 QEI 或单边沿解码。
- ICM42688三维安装映射保持`{X,Y,Z}/{-1,+1,-1}`；映射且扣除零偏后的Z轴角速度与编码器航向互补融合，并在底盘运动层参与航向/角速度反馈。单轮速度PID仍只使用编码器。
- 未完成架空测试前，不得提高 PWM 限幅或绕过中键确认、急停和堵转保护。

## 修改代码时的工作约束

- 当前工作区包含大量尚未提交的底盘重构改动。开始修改前必须执行 `git status --short`，不得回滚、覆盖或清理不属于当前任务的改动。
- 不得使用 `git reset --hard`、`git checkout --` 等方式丢弃现有工作。
- 未经用户明确要求，不修改编码器 3.3 V 供电、PCB 引脚映射、电机驱动供电方式或软件 4× 解码方案。
- 修改前先阅读相关接口和三份项目文档；代码、接线和操作说明发生变化时同步更新文档。
- 保留40%底层绝对硬限幅；默认安全自检必须继续使用原35%输出配置，旧接口不得绕过40%绝对限制。
- 默认自检中的所有运动必须等待中键确认；电机运行期间按中键必须立即进入锁定急停。

主要代码分层：

- `ml_libs/ml_motor_driver.*`：TB6612 方向、PWM 和底层硬限幅。
- `ml_libs/ml_encoder.*`、`ml_libs/ml_quadrature.*`：GPIO 采集和软件 4× 解码。
- `code/motor_velocity.*`：左右轮 PID、前馈和速度滤波。
- `code/chassis_odometry.*`：差速里程计。
- `code/chassis_motion.*`、`code/chassis.*`：轨迹、堵转、急停和公共非阻塞 API。
- `code/chassis_self_test.*`：默认安全自检流程。

## 当前验证状态

- 2026-07-30杆球舵机方向单项实测：1450 us会使杆的`+cm`侧下降，源码据此使用`BALL_CONTROL_DIRECTION=-1`。人工标定已从1450/1550扩大一级到1400/1600 us，但新范围机械余量尚未实测；异常必须回退1450/1550，全局仍限1300–1700 us。
- 2026-07-31 MaixCam横向映射已由旧`5.8%/cm`单比例改为实测`x_percent→实际cm`分段线性表：`-68.8%=-10 cm`、`-33.8%=-5 cm`、`0%=0 cm`、`+34.4%=+5 cm`、`+61.9%=+9 cm`等标定点直接命中，并沿末段外推/夹紧到±12 cm。屏幕同时显示旧比例`RAWCM`和修正后发送给MSPM0的`FIXCM`；MSPM0位置控制只采用修正厘米值，UART帧仍为32字节`<IhhffB3x>`。当前Maix脚本只在`X ±84%`、`Y ±20%` ROI内以0.20置信度阈值检测目标；MSPM0保留score≥0.20、valid、连续3帧恢复和150 ms超时。该低阈值只用于本轮ROI限定调参，误检时回退0.40。
- 2026-07-31速度内环首轮实机观察：速度P/D=25/0时，小球可明显减速并停在+2～+4 cm，舵机没有持续往复，速度P方向和量级暂定基本正确；该结果符合速度目标0不约束位置，尚不是完整动态验收。随后0 cm位置P=1.6实测中坐标符号正确且球反复过零，但振荡持续增大，OLED观察到`P+090/U1700`；源码据此只把位置P降至0.8，位置D/I与速度P/D保持0/0和25/0。若0.8仍发散，下一轮只回退到0.4。
- 杆球工程使用独立UART0离线遥测，不链接底盘遥测：PA10/PA11、115200 8N1。当前记录为600条×44字节、25列CSV，按10 ms覆盖约6秒；新增`sequence_state,sequence_elapsed_ms`时把控制状态和序列状态压入同一字节并复用原对齐空间，因此`g_records`仍为26400字节。MSPM0G3507只有TI原厂链接文件定义的`0x20200000～0x20207fff`共32 KB SRAM，禁止再把`0x20300000`当作第二RAM。失视重捕获不清空，PB24中止或补偿故障后继续记录到目标/实际脉宽均为1500 us；OLED显示`CSV READY D=OUT`后才允许`D`重复导出或`C`清空。错误800条固件虽Rebuild为`Code=14984, RO-data=2272, RW-data=896, ZI-data=36560`且0错误0警告，但实机导出在第600条后出现固定重复垃圾，不能作为有效基线。2026-07-31用户对回退源码全量Rebuild为`Code=14928, RO-data=2256, RW-data=896, ZI-data=27760`且0错误0警告；map确认只有`RW_IRAM2=0x6ff0/0x8000`、当时`g_records=26400`字节，不含`0x20300000`或第二遥测段，且无底盘、电机、编码器控制符号；25列增强源码仍待新Rebuild确认。
- 2026-07-31位置P=0.8首份CSV得到135条完整记录：175–4995 ms内位置为+2.107～+2.486 cm、最终+2.475 cm，控制量稳定约-49.5 us且舵机约1550 us，视觉年龄不超过20 ms。相邻记录却只有90次10 ms，另有43次80 ms和1次480 ms；代码检查确认自动闭环期间四行软件I2C OLED刷新会阻塞主循环。当前源码已改为每次进入自动闭环前最后显示一次`CAL CENTER P`，`BALL_BALANCE_ACTIVE`期间完全冻结OLED写入，失视、等待和PB24中止后恢复10 Hz显示；PID和限幅未改。新的OLED冻结应用测试与全部PC回归为`24/24 passed`，但该源码仍须重新Rebuild、烧录并用约500条新CSV确认10 ms连续性，旧CSV不得用于继续调PID。
- 2026-07-31 OLED冻结固件Rebuild结果为`Code=14048, RO-data=2184, RW-data=896, ZI-data=22928`且0错误0警告。随后有效CSV含594条完整记录、591个10 ms间隔和2个20 ms间隔，全程`BALL_BALANCE_ACTIVE`，视觉年龄最大50 ms、帧间隔最大55 ms，确认活动期OLED阻塞已消除。位置P/D/I=0.8/0/0时球从+2.38 cm稳定到+2.29 cm，控制约-45.8 us、舵机约1546 us且无振荡；用户确认1500 us只是安全机械中位，结构无法保证绝对水平。当前源码据此只启用位置积分：P/D/I=0.8/0/0.20，积分分离3.0 cm、积分限幅±8 cm·s；速度P/D仍为25/0，舵机中位、斜率和1300–1700 us限幅不变。积分新参数与分离、限幅、饱和冻结、停用/失视清零已通过全部PC主机测试`24/24 passed`，仍须重新Rebuild和实机CSV验证。
- 2026-07-31位置I=0.20固件Rebuild结果为`Code=14124, RO-data=2188, RW-data=896, ZI-data=22928`且0错误0警告。540条完整CSV中活动闭环有538个连续10 ms间隔、视觉年龄最大90 ms且无失视；球从+2.66 cm开始，积分约在3930 ms达到-8 cm·s上限，位置最低+0.893 cm并最终稳定约+1.01 cm，没有过零或发散。最大速度约-2.86 cm/s、最大舵机1582 us，说明速度内环能制动且I方向正确，但I=0.20最大约40 us积分控制不足以消除剩余静差。当前源码只把位置I提高到0.30，P/D=0.8/0、速度P/D=25/0、积分分离3.0 cm、积分限幅±8 cm·s和全部安全边界不变；最大积分贡献约60 us，明确回退值为I=0.20，仍须重新Rebuild和实机CSV验证。
- 2026-07-31位置I=0.30固件Rebuild结果为`Code=14132, RO-data=2188, RW-data=896, ZI-data=22928`且0错误0警告。新CSV含420条活动记录，其中419个活动间隔为10 ms，视觉全程有效；球始终位于+2.748～+2.848 cm，积分约在3.06 s达到原-8 cm·s上限，目标速度约-4.67 cm/s，舵机达到约1619 us且杆已倾斜，但球没有有效移动。当前源码据此只把位置积分限幅从±8扩大到±12 cm·s；位置P/D/I=0.8/0/0.30、速度P/D=25/0、积分分离3.0 cm、1500 us中位、1300–1700 us边界和全部视觉安全门不变。最大积分贡献约±90 us，在3 cm分离边界的总舵机偏移不超过约150 us，明确回退值为±8 cm·s；仍须重新Rebuild和实机CSV验证，达到约1650 us仍不移动时不得继续扩大积分或舵机范围。
- 2026-07-31积分限幅±12 cm·s固件Rebuild结果为`Code=14136, RO-data=2184, RW-data=896, ZI-data=22928`且0错误0警告。三次CSV均含600条连续10 ms活动记录且视觉有效：第一次从+2.75 cm移动到最低+0.979 cm并停在约+1.01 cm，没有过零；后两次积分接近-12 cm·s才克服静摩擦，峰值速度约-5.70/-5.74 cm/s，各过零一次并停在约-1.39/-1.50 cm。当前源码据此只把位置D从0加入到0.20；位置P/I=0.8/0.30、速度P/D=25/0、积分分离3.0 cm、积分限幅±12 cm·s、舵机边界和视觉安全门不变。在-5.7 cm/s时预计增加约+1.14 cm/s制动速度目标和约28.5 us反向舵机控制，明确回退值为D=0；仍须重新Rebuild并连续三次实测，当前不得启动正式序列。
- 2026-07-31位置D=0.20的三次有效320×224 CSV均保持连续10 ms且无失视：第一次约5 s后启动，峰值速度-6.08 cm/s、过零至-1.45 cm、最低脉宽1402 us；第二次积分达到-12 cm·s且舵机1630 us仍停在+2.017 cm；第三次峰值速度-3.40 cm/s并稳定到+0.183 cm，脉宽1470～1608 us。视觉、D方向和时序正常，静摩擦启动阈值不一致是主要问题。首版补偿源码保持位置P/D/I=0.8/0.20/0.30、速度P/D=25/0和积分限幅±12 cm·s不变，只在目标0 cm且非评分序列时加入静摩擦临时增力：误差≥0.5 cm、速度≤0.3 cm/s连续300 ms后冻结积分并以50 us/s增至最多95 us；首版仅按朝目标速度达到0.5 cm/s撤销；活动期脉宽限1400～1650 us；最大增力保持500 ms仍不动则锁存`BREAKAWAY_FAULT`并回中，本次上电禁止恢复。该首版已通过当时24/24主机测试，当前不得启动正式序列或声称满足5 s/±1 cm。
- 2026-07-31首版静摩擦补偿三次CSV均为600条、599个连续10 ms间隔且视觉有效：第一次约2.66 s启动，峰值-3.45 cm/s、最低+0.697 cm、最大脉宽1610 us、最终约+1.10 cm；第二次约2.04 s启动，峰值-3.73 cm/s、最低+0.417 cm、最大脉宽1638 us、最终约+0.825 cm；第三次没有有效位移，原始x仅在133/134间变化，单像素约0.0917 cm却产生最低-0.51 cm/s虚假速度，使补偿反复提前撤销，最大增力仅67 us并最终停在约+2.42 cm。当时源码只修复释放判定：补偿激活时锁存位置与方向，朝目标位移≥0.15 cm、速度≥0.5 cm/s连续60 ms后才撤销；条件中断只清确认计时，补偿和积分冻结继续；明显背离目标且速度超过0.3 cm/s仍立即清除。当时PID、300 ms触发、50 us/s斜坡、95 us上限、1400～1650 us窗口、500 ms故障门以及23列/600×44字节遥测均不变；该轮不得启动正式序列或声称满足5 s/±1 cm。
- 2026-07-31释放判定抗抖修复后三次CSV确认单像素抖动未再提前撤销补偿。错误800条固件的前600条仍是有效单SRAM数据：第一次从+2.658 cm稳定到+0.183 cm，最低+0.101 cm、峰值速度-3.01 cm/s；第二次从+2.845 cm移动到最后有效+0.090 cm，峰值-3.05 cm/s，随后一次157 ms有效帧间隔触发150 ms失视安全门并正常回中。第三次在第359条完整记录到真实故障：球从+2.475 cm移动后卡在约+2.017 cm，补偿于3429 ms达到95 us，3919 ms满足最大值保持500 ms并锁存`BREAKAWAY_FAULT`，最大舵机1642 us，3993 ms回到1500 us，视觉全程正常。PID与补偿逻辑不改；先检查+2 cm杆面、球体、连杆回差和供电，再用固定球/断开舵机动力的五次视觉测试判断157 ms是否重复，当前不得启动正式序列或声称满足5 s/±1 cm。
- 2026-07-31五次舵机动力断开的视觉隔离CSV最大帧间隔为95/44/48/100/89 ms，均未达到150 ms且无失视；此前157 ms按孤立事件处理，不修改Maix显示频率或MSP安全门。机械检查确认球总在固定范围内停住，轻触不能启动而增大杆角可以启动，定位为局部静摩擦/坡度阈值。当前位置P/D/I=0.8/0.20/0.30、速度P/D=25/0保持不变；源码只把中心调试临时增力内部上限从95提高到150 us，并将500 ms故障计时改为舵机实际到达1400/1650 us边界后开始。300 ms触发、50 us/s斜坡、0.15 cm与0.5 cm/s连续60 ms释放、1300～1700 us绝对边界均不变；明确回退值95 us。若1650 us保持500 ms仍不能启动，不得继续扩限，必须处理杆面、连杆或球体。
- 2026-07-31静摩擦增力150 us固件三次CSV：第一次从+0.550 cm在1613 us启动并过零，停在-0.825 cm后反向补偿到1400 us仍不动，于5689 ms正确FAULT；第二次2379 ms因Maix间歇有框/LOST触发视觉超时，不能用于控制判断；第三次约5049 ms在1621 us启动，峰值速度-4.13 cm/s并过零，用户确认最终回到中心附近。三次起点均为视觉+0.55～+0.79 cm，原0.5 cm补偿门会在接近题目允许误差时强制脱困。当前源码只把`BALL_BREAKAWAY_ERROR_MINIMUM_CM`改为0.9 cm，±0.89 cm不触发而±0.91 cm触发；普通PID、150 us增力、1400～1650 us窗口和全部安全门不变，回退值0.5 cm。1350 us尚未单独复核，不得扩大补偿下限；0.9 cm是视觉软件门限，不能等同于外部精度验收。
- 2026-07-31用户对0.9 cm门限固件全量Rebuild：`Code=14984, RO-data=2256, RW-data=896, ZI-data=27760`，`0 Error(s), 0 Warning(s)`，耗时11秒。Map确认`g_records=26400`字节、唯一`RW_IRAM2=0x6ff0/0x8000`且无底盘控制符号；全部主机测试`24/24 passed`、Maix协议/语法、ARMCC单编及`git diff --check`均通过。该构建可用于0.9 cm中心复测，尚不能启动正式`+5→-5`或声明满足5 s/±1 cm。
- 2026-07-31用户将Maix正式映射更新为`5.8%=1 cm`，320像素下约0.1077586 cm/px，`x=147/160/173`对应约`+1.401/0/-1.401 cm`，`±75%`对应约`±12.931 cm`；旧映射CSV不得与新厘米数据直接比较。四次中心CSV中第一份在+0.553 cm仍触发补偿，符合旧0.5 cm固件且用户不确定是否已Download，不能验证0.9门限；第二、三份约+0.458 cm无补偿但不能区分版本；第四份使用新映射从+1.401 cm启动，2799 ms有效运动、最大1638 us，2899 ms首次进入±0.9 cm，最大超调-0.922 cm，3459 ms后持续保持并最终回到0 cm，全程无失视/FAULT。普通PID阶段最低1346 us不能替代1350 us机械复核。下一步必须明确重新Download，再以原始`x≈154`（新映射+0.647 cm）确认运行至少1秒时增力始终为0；当前不改PID或安全边界。
- 2026-07-31明确重新Download后的判别CSV确认0.9 cm固件已在实板运行：球心固定`x=153`，按新映射为+0.754 cm；246条活动记录覆盖2450 ms且全部10 ms连续，补偿活动和增力始终为0，无失视/FAULT。普通PID积分到-1.848 cm·s、控制-28.9 us、舵机1529 us，球仍静止，PB24中止后回到1500 us。该位置高于旧0.5 cm且低于新0.9 cm，已排除旧固件；0.9 cm补偿进入门限的软件、构建和烧录确认完成。正式`+5→-5`仍需负侧映射、1350 us机械安全和正式轨迹实测。
- 2026-07-31当前杆球源码已切换为正式`+5→-5`首轮入口：`BALL_AUTO_CONTROL_MODE=BALL_AUTO_CONTROL_DISABLED`、`BALL_BALANCE_ALLOW_SEQUENCE=1`。正常上电等待连续3帧视觉后显示`BALL READY C=GO`，PB24只接受一次启动；运行中PB24立即中止，完成后保持-5 cm并仅更新一次OLED第一行，PB24可关闭闭环并斜率回中。正式序列成功启动时启用原600条×44字节UART0遥测，活动/完成保持期间冻结周期OLED写入，目标及实际脉宽均回到1500 us后才允许`D/C`。受限静摩擦补偿现由`BALL_SEQUENCE_BREAKAWAY_ENABLED=1`允许在正式`TO_PLUS_5/TO_MINUS_5`阶段运行，完成保持阶段禁用；回退时只把该开关改为0。全部主机回归为`25/25 passed`；该源码仍需用户全量Rebuild/Download和实机验证，不能声明满足5 s或±1 cm。
- 2026-07-31正式序列失视逻辑已改为可恢复：Maix在每个320×224图像周期发送固定32字节帧，未识别时发送字段清零的`valid=0`；MSPM0对单帧无效、低score或越界数据只清端点稳定计时，150 ms内继续最近有效观测。有效观测年龄达到150 ms时保留当前`TO_PLUS_5/TO_MINUS_5`阶段和目标，清PID、关闭控制并斜率回中，OLED显示`BALL REACQUIRE`；连续3帧有效后重建观察器并自动恢复原目标，遥测会话和5000 ms总计时均不暂停。重捕获期间PB24仍可中止，等待达到5秒仍锁存`TIMEOUT`；`BALL_SEQUENCE_VISION_LOST`枚举仅保留兼容。PID、厘米映射、舵机边界和静摩擦补偿均未改变。失视版本当时全部主机回归`25/25 passed`，Maix协议/语法、ARMCC 5.06双文件单编、三工程XML、杆球源隔离和`git diff --check`均通过。
- 2026-07-31正式+5首轮实测OLED显示`BALL TIMEOUT`，Maix最远仅识别到约+3.6 cm；本轮不据此修改PID、5秒门、映射或舵机范围，只增强正式CSV诊断。CSV从23列扩展为25列，在`state`后加入`sequence_state,sequence_elapsed_ms`；内部记录仍为44字节、600条。终态回中完成后OLED第四行显示`CSV READY D=OUT`，此前`D`继续返回限频`BUSY`。全部主机回归`25/25 passed`，Maix协议/语法、ARMCC 5.06遥测与应用单编、三工程XML、杆球源隔离和`git diff --check`均通过；用户仍需在现有`project_ball`窗口Rebuild/Download并取得新CSV后再单独决定下一类参数修改。
- 2026-07-31首份25列正式CSV含502条有效记录，活动段500条全部连续10 ms，视觉年龄最大42 ms且无失视。球从-0.323 cm到最大+3.785 cm，约3 s后卡在+3.7～+3.8 cm，5000 ms时以+3.744 cm超时；超时前积分6.516/12 cm·s、控制约+74 us、舵机约1426 us、目标速度约+2.96 cm/s而实际速度接近0，说明视觉、时序和控制权限均未饱和，主要是正式序列禁用脱困导致局部静摩擦无法在5 s内克服。当前源码保持PID与5 s门限不变，正式+5端仅在误差严格大于0.9 cm、-5端严格大于1.0 cm时允许原300 ms/50 us/s/150 us/1400～1650 us补偿；故障记为`ABORTED + breakaway_fault=1`、显示`BALL STICK FAULT`并回中。新增正负方向、端点禁用、释放、完成保持隔离和故障导出测试后全部主机回归`25/25 passed`，仍待用户Rebuild/Download及三次实测。
- 2026-07-31正式脱困首轮三次CSV均连续10 ms、视觉年龄最大90 ms且无失视，却全部停在`TO_PLUS_5`并以`ABORTED + breakaway_fault=1`结束：第1/3次几乎不动并在1920/800 ms故障，第2次最多到+0.659 cm并在2510 ms故障。补偿前普通PID最低已请求1383/1398/1385 us，原1400 us脱困下限激活后反而把指令夹弱到1400 us并过早满足500 ms故障门。用户明确确认1350 us机械绝对安全；当前源码新增`BALL_SEQUENCE_BREAKAWAY_SERVO_MINIMUM_US=1350`，只把正式窗口改为1350～1650 us和+5故障边界改为1350 us，中心调试仍为1400～1650 us、-5仍为1650 us。PID、触发/释放、5 s和1300～1700 us绝对边界不变；新增不反向夹弱、1350/1650故障及中心隔离回归后主机测试`25/25 passed`，仍待Rebuild/Download和三次实测。
- 2026-07-31正式1350 us脱困复测两次均连续10 ms且视觉正常：第一次移动到最大+1.75 cm后卡在约+1.62 cm，实际1350 us保持500 ms并在4050 ms故障；第二次几乎未启动，1350 us保持500 ms并在2830 ms故障。用户选择直接使用现有1300 us全局绝对下限；当前源码只把`BALL_SEQUENCE_BREAKAWAY_SERVO_MINIMUM_US`改为1300，使正式窗口为1300～1650 us并按实际1300 us保持500 ms判定+5故障。中心调试仍为1400～1650 us、正式-5仍为1650 us，PID、50 us/s斜坡、150 us增力和5 s门限不变；1300 us不得继续降低。
- 2026-07-31正式1300 us脱困复测两次均连续10 ms、视觉正常并在5000 ms以`TIMEOUT`结束，没有FAULT：第一次最低1329 us，补偿受反向像素速度尖峰影响被切成4段；第二次最低1327 us，在约+2.15 cm再次卡住后增力到107/150 us即超时。两次均未实际达到1300 us，不能继续降低下限。当前源码只新增正式序列专用100 us/s斜坡，中心调试仍为50 us/s；激活后仅在相对触发起点背离目标至少0.15 cm且反向速度超过0.3 cm/s时清除补偿，单像素反向速度尖峰不再重置斜坡且积分继续冻结。PID、150 us增力上限、1300～1650 us正式窗口、500 ms故障门、5 s、Maix和25列CSV不变；正式斜坡回退值为50 us/s。
- 2026-07-31正式直接150 us脱困与全程极限制动复测三次：三份26列CSV活动段均连续10 ms，视觉年龄最大60 ms且无失视；最大位置仅+3.421/+2.997/+3.314 cm，全部停在`TO_PLUS_5`并于5000 ms超时。独立制动在+0.15～+0.92 cm便请求1650 us并持续约0.52～0.58 s，使球到+3.0～+3.4 cm后反向至-0.73～-1.36 cm，再由1300 us拉回，形成约1.5 s周期的极限往返。直接150 us脱困本身已成功克服起点静摩擦；当前源码保留正式静止300 ms后直接最大脱困，只把`BALL_SEQUENCE_OVERSPEED_BRAKE_ENABLED`默认改为0，运动后的连续制动恢复由位置D和速度P环完成。强制制动代码、`brake_active`和26列/44字节遥测保留，旧值1仅作诊断回退；PID、5 s、1300～1700 us、Maix及全部安全门不变。
- 2026-07-30已实现车尾A点停车重标：终点参考5932 mm、5742 mm减速、5919 mm制动、5982 mm故障门，航向从DA起点开始连续3周期进入355°–365°后锁存。正式竞速恢复stage 0的360/360 mm/s、400 mm/s²和20000绝对PWM上限；stage 1/2仅保留为未启用的380/360、400/360可选档。PC主机测试`18/18 passed`、`git diff --check`及stage 0/1/2与受影响文件ARMCC单编通过；默认与竞速工程仍待μVision全量Rebuild。
- 2026-07-29 12:24 的 Keil 全量 Rebuild 已包含UART FIFO配置和RX FIFO轮询回退，结果为 `0 Error(s), 0 Warning(s)`；但该结果早于本次ICM42688安装方向修改，已不能作为当前源码的烧录基线。
- 实车已确认最终方向配置：左轮 `encoder_sign=-1`、`motor_sign=-1`，右轮 `encoder_sign=-1`、`motor_sign=+1`。完整五段架空自检已通过并到达 `GROUND+PRESS KEY`，各阶段 `BAD L0000 R0000`。
- 理论 `mm_per_tick=0.1402497` 时，500 mm 直线实测约 500 mm且肉眼未见明显横向偏移；该结果早于本轮独立滚动标定，需在新参数烧录后复验。
- 旧 `effective_track_mm=128.6 mm` 下，`+90°/-90°` 方向均正确但实际均约 54°。两轮接地点中心距实测为 `214.2 mm`，与 `90 × 128.6 / 214.2 ≈ 54.0°` 相符；当前源码已将首轮标定值改为 `214.2 mm`。
- 12 V 断开、Keil Watch 实测单圈结果为左轮 `1456 tick`、右轮 `|tick|=1498`，两侧 `BAD` 增量均为 0，均通过 `1456 tick ±5%` 初验。
- OLED滚动标定页完成一组正反向各 1000 mm 样本：前进 `L+7037/R+6951`，后退 `L-7110/R-6987`。当前按合并计数暂定左轮 `0.1413727 mm/tick`、右轮 `0.1434926 mm/tick`；样本量不足以单独证明最终 1% 精度。
- 已新增 OLED 滚动标定页面：开机前按住中键，IMU 校准完成后显示左右相对 tick 和相对 `BAD`，中键仅清零显示基线，不发送运动命令。
- 实板在 `TEST COMPLETE` 页面发送单个 `D` 时仍只间歇返回乱码。2026-07-29 13:11已确认使用包含UART `BUSY`发送等待修复的新构建和新烧录，问题仍存在，说明不能继续仅按TX FIFO超时推断。当前源码新增开机右键UART安全诊断模式，用自动横幅、RX十六进制、硬件分频和错误统计分别验证TX/RX。
- 2026-07-29 使用J-Link在CPU运行期间发送单个 `D`，UART0中断入口断点未命中，软件队列仍为 `head=0/tail=0/overflow=0`。`uart_try_read()` 已增加硬件RX FIFO轮询回退：软件队列为空时直接读取FIFO，保留原中断接收作为主路径；修改后的10组主机测试已 `10/10 passed`，Keil全量Rebuild已通过，并于12:26完成 `Erase Done / Programming Done / Verify OK`，尚待空缓存实板复验。
- UART安全诊断模式已新增自动横幅、RX回复、OLED分频/错误/超时格式和RAM不受影响的回归；2026-07-29全部10组PC主机测试重新编译并以`10/10 passed`通过，真实UART、自检状态机、遥测UART和OLED视图也已用ARMCC单文件编译通过，`git diff --check`通过。
- 当前Keil构建和已烧录固件均早于UART安全诊断模式，已经失效；必须重新Rebuild并烧录后先完成诊断模式验证。
- ICM42688当前安装为传感器 `+X` 指向车尾、`+Y` 指向车辆左侧、`+Z` 指向地面；车体坐标保持 `+X` 向前、`+Y` 向左、`+Z` 向上，因此默认姿态映射保持 `{X,Y,Z}` / `{-1,+1,-1}`。
- 2026-07-29已加入编码器–陀螺仪航向融合和底盘运动层角速度反馈：保留当前编码器/运动正号，使用`imu_heading_sign=-1`转换原始IMU正号；IMU超时或跳变时连续退化到编码器。新增融合测试后PC主机测试为`11/11 passed`；当前交互式Keil窗口仍在运行，尚未执行本次源码的全量Rebuild和烧录。
- 地面遥测采用离线流程：五段架空和地面三段运动由车载 RAM 暂存，测试结束后只断开 12 V、持续保持 5 V，将车搬回电脑旁再通过 UART0 导出。复位、重新烧录或断开 5 V 都会丢失尚未导出的记录。
- 旧构建日志位于 `user/Objects/project.build_log.htm`；`user/Objects/` 和 `user/Listings/` 被 `.gitignore` 忽略，不纳入版本控制。
- 用户的交互式 Keil 窗口应予保留；不要在其运行时并行启动其他 UV4 构建进程。

Keil 全量构建命令为：

```text
D:\Keil_v5\UV4\UV4.exe -b user\project.uvprojx -j0
```

最终构建验收标准为 `0 Error(s), 0 Warning(s)`。若用户正在使用 Keil，由用户在现有交互式窗口点击工具栏的 `Rebuild`；当前环境中的 `Ctrl+F7` 可能只编译当前文件，不能替代全量构建。

- 2026-07-31第三问空CSV修复：实机曾在串级正式序列后发送`D`只得到26列表头。源码现将遥测启动统一绑定到实际`TO_PLUS_5/TO_MINUS_5`状态并在同轮询记录首条；终态OLED显示非零`CSV Nxxx D=OUT`，0条显示`CSV EMPTY REBOOT`，空缓存`D`回复`EMPTY`且不再输出误导表头。26列、600条×44字节、26400字节单SRAM布局及串级PID/5秒门/舵机边界均未改；真实应用+遥测联合回归和完整主机测试为`26/26 passed`，仍须在现有project_ball窗口全量Rebuild/Download后实机确认记录数与数据行一致。
- 2026-07-31第三问最新两份CSV均在`TO_PLUS_5`触发脱困故障，最大位置仅+2.144/+0.859 cm；第一份在原500 ms故障边缘已经启动，第二份在1300 us下真实卡住。用户明确覆盖原边界并授权驱动绝对下限改为1200 us，但普通PID仍限制在1300 us以上。正式+5现按300 ms朝目标位移不足0.15 cm触发二级脱困：实际1300 us最多250 ms，再按需实际1200 us最多250 ms；朝目标位移≥0.15 cm且速度≥0.5 cm/s连续30 ms即释放，确认期间暂停故障计时。中心调试仍为1400～1650 us，正式-5仍不使用1200 us。TIMEOUT冻结且目标/实际脉宽相等时立即结束遥测并允许`D`导出，PB24只负责随后回中。PID、5秒门、26列CSV和RAM布局不变；该源码尚未运行回归或构建，须用户Rebuild/Download后连续三次复测。
- 2026-07-31二级脱困后三份CSV均进入`TO_MINUS_5`，但普通串级位置D/速度D在第一次正向运动中提前反打，使小球只到+3.09～+3.53 cm便回到中心，第二次往返才以2280～2530 ms进入+5带，最终均在-5阶段超时；`brake_active`始终为0。正式序列现改为6 cm/s巡航和`v²/(2×45 cm/s²)+0.15 cm`停止距离制动，制动区以`4×位置误差`降低速度目标，端点±1 cm内目标速度为0；正式速度内环P/D=27/0，中心闭环和完成保持仍为位置P/D/I=0.8/0.40/0.30、速度P/D=27/3。+5改为误差≤1 cm且速度≤1 cm/s连续30 ms后折返，-5继续低速稳定500 ms。1200 us二级脱困、5秒门、CSV和RAM布局不变；该源码未运行回归或构建，等待三份新CSV确认首次正向运动直接到+5并在5秒内完成。
- 2026-07-31第三问1000 us正式脱困三次CSV均停留在`TO_PLUS_5`并于5000 ms超时，位置峰值为+8.372/+12.392/+11.961 cm，速度峰值约14.57/21.49/21.94 cm/s；三次均实际到达1000 us，释放后普通速度环已长期饱和制动，第2/3次还在接近+12 cm时发生约220 ms失视回中。用户据此明确撤销正式1000 us例外，源码将舵机绝对下限和正式+5脱困下限统一恢复为1300 us，1700 us上限不变；位置P/D/I=0.8/0.40/0.30、速度P/D=27/3、5秒门、+5低速200 ms、-5低速500 ms、Maix和26列遥测均不改。新源码等待Rebuild/Download及三次CSV复测，1300 us不得再次降低。

- 2026-07-31恢复1300 us后的三份第三问CSV均保持10 ms记录且舵机位于1300–1700 us：第1/3次分别到达+5.322/+4.495 cm，已进入题目允许的+5±1 cm范围，但进入时速度为8.12/6.20 cm/s，旧状态机因+5与-5共用`|速度|≤1 cm/s`和200 ms稳定门而始终不折返；第2次最高+3.419 cm。用户选择保持1300 us绝对下限，源码只把+5改为±1 cm内连续30 ms位置确认后立即切换`TO_MINUS_5`且不检查速度；最终-5仍要求±1 cm、`|速度|≤1 cm/s`连续500 ms。PID、脱困、5000 ms总门、Maix和26列遥测不变；按用户要求本轮不运行回归、编译或差异检查，等待Rebuild/Download和新CSV。

- 2026-07-31新分段`FIXCM`映射下三份501条CSV均为连续10 ms且无失视：三次以约8～9 cm/s进入+5允许带，峰值为+6.506/+7.082/+6.961 cm；只有第一份在1610 ms偶然低速后折返，另外两份因正端低速门形成1300/1700 us往返并超时。源码现按题面把+5改为±1 cm连续30 ms后立即折返且不检查速度，同时将正向巡航6.0降至4.5 cm/s、制动估计20降至18 cm/s²、制动余量0.15增至0.25 cm。最终-5捕获后在±1 cm合格带内固定0速度目标，离带才以`2×误差`、最大±2 cm/s恢复；500 ms低速完成门不变。舵机与两级正式脱困统一为1300–1700 us，两级各250 ms以保留累计500 ms故障计时，历史1200 us例外撤销。位置PID、速度PID、负向巡航/制动、5秒门、Maix协议和26列遥测不变；用户明确要求本轮不运行回归、编译、差异检查或Keil构建，源码尚未验证。

- 2026-07-31上述修订后三份附件实际只有两轮独立CSV，前两份SHA一致。第一轮在4030 ms以-5.008 cm、-2.22 cm/s高速越过目标时提前锁存，第二轮在4260 ms以-4.745 cm、-0.96 cm/s低速捕获；两轮随后均因±1 cm内目标速度突变为0而出现接近U1500的控制卸载，最长连续合格仅140 ms，最终回弹到约-3.4/-3.2 cm。源码现取消“越过-5中心无条件捕获”，只允许误差≤0.8 cm且速度≤1 cm/s时锁存；捕获后始终使用`2×位置误差`并限幅±2 cm/s，使目标速度连续趋零，不加入固定舵机偏置。+5轨迹、PID、1300–1700 us边界、脱困、5秒门、Maix和26列遥测不变；按用户要求未运行回归、编译、差异检查或Keil构建，等待重新Rebuild/Download和CSV实测。

- 2026-07-31连续捕获参考修复后四份CSV均为10 ms记录且视觉有效：第1份在4491 ms以约-4.29 cm进入`COMPLETE`并连续合格500 ms；第2/3份最长合格400/240 ms，负向首次制动均在约-4.2 cm停住并回弹到约-3.0 cm，直到4110/4200 ms才第二次低速捕获；第4份+5在+4.019 cm折返，负端最低-4.845 cm但最长合格370 ms并在5000 ms以-4.259 cm、+1.43 cm/s超时。源码现把负向制动估计20提高到24 cm/s²、制动余量0.30减至0.15 cm，使首次制动约后移0.5 cm；捕获保持由2×提高到3×误差，±2 cm/s限幅不变。软件内部+5确认带由题面±1 cm收紧为±0.8 cm，30 ms和不检查速度不变。500 ms门、PID、1300–1700 us、脱困、Maix和26列遥测均不改。OLED第四行首字段`Txxxx`显示毫秒用时，正常完成时第一行一次性显示`HOLD -5 Txxxx`，活动期继续冻结写屏；按用户要求未运行回归、编译、差异检查或Keil构建，等待Rebuild/Download和新CSV。

## 下一步实车验收流程（等待左右轮参数重新构建）

以下是交接摘要；现场操作、停止条件和数据表以 [HARDWARE_ACCEPTANCE.md](HARDWARE_ACCEPTANCE.md) 为准。

严格按以下顺序执行，不要跳过架空测试直接落地运行：

1. 在当前交互式 Keil 窗口点击工具栏 `Rebuild`，确认新的 `0 Error(s), 0 Warning(s)`；不要用可能只编译当前文件的`Ctrl+F7`代替。
2. 烧录包含最终方向符号、左/右 `mm_per_tick=0.1413727/0.1434926` 和 `effective_track_mm=214.2 mm` 的新固件。
3. 保持12 V断开，开机前按住右键PB25并静止完成IMU校准；确认进入`UART DIAG STOP`且电机始终不动作。
4. COM3设为115200、8N1、无流控，不发送内容时每秒必须收到`UART0,115200,TX,nnnn`；OLED应显示`BRD I0002 F11`且TX超时不增加。发送单个ASCII `D`后OLED应显示`RX44`，串口应回复`RX,44,ERR,00`。诊断模式内不得导出CSV或清空RAM。
5. 断电后不按右键重新启动；停在`LIFT+PRESS KEY`时发送单个ASCII `D`，空缓存必须立即、完整返回92字节融合CSV表头。重复发送仍应正常，不得使用`D0`、连续`D`或换行凑字符。
6. UART空缓存测试通过后，确认五键未操作均显示1，依次操作时只有对应位变为0。
7. 架空两轮并接通12 V。显示`LIFT+PRESS KEY`后垂直按中键，依次检查左轮正转、左轮反转、右轮正转、右轮反转和双轮闭环。
8. 确认四个单轮页面的物理方向与名称一致：`FORWARD`使轮胎顶部朝车头、`REVERSE`相反；`BOTH CLOSED LOOP`时两轮顶部都朝车头。五段必须全部通过并到达`GROUND+PRESS KEY`，同时正向累计为正且`BAD`不持续增加。
9. 只有完整架空复验通过后，才能在平地复验500 mm直线；暂定放行范围为495–505 mm，并记录横向偏移。
10. 左右轮参数会改变距离和转向换算，必须按OLED提示重新执行`+90°`和`-90°`，使用外部基准确认双向误差不超过2°。
11. 断开12 V，开机前按住中键并保持静止；进入`ROLL C=ZERO`后可直接从OLED读取单圈相对tick，左右都应接近1456 tick。
12. 当前暂按一组正反1 m样本写入左右参数；若500 mm回归超差或横向偏移明显，再回到滚动标定页增加样本，不通过调整PID掩盖机械比例误差。
13. 双向执行90°和360°转向，标定`effective_track_mm`。
14. 地面依次执行500 mm、`+90°`和`-90°`时不需要携带电脑；每个等待页面停留数秒再按中键，使各运动段在时间戳中留下明显间隔。RAM在本次上电初始化时已自动清空，导出的CSV会同时包含架空五段和地面三段记录。
15. 最后一段完全停车后先断开12 V，但必须持续保持5 V，且不得复位或重新烧录。将车搬回电脑旁后，使用3.3 V TTL USB转串口交叉连接PA10/UART0 TX、PA11/UART0 RX和GND，不连接转串口VCC；先发送`D`并保存、核验CSV，确认数据安全后才允许发送`C`清空RAM。
16. 对比各地面运动段的编码器航向、融合航向和原始IMU Yaw首末变化量（原始IMU按`-1`转换后比较），判断打滑、融合连续性与系统误差；完成机械参数标定后再从60–100 mm/s低速调整，保持所有安全限幅不变。

任一电机方向、编码器符号、非法跳变或堵转保护不符合预期时，应立即停止后续地面测试，先定位接线、机械阻力或配置问题。
