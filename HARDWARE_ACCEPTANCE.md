# MSPM0G3507 双轮底盘实车验收现场检查表

本文用于当前 MSPM0G3507、TB6612 和两只 MG513X 编码器电机底盘的首次烧录、架空自检、地面测试、机械标定、遥测分析和低速调参。接线原理见 [WIRING.md](WIRING.md)，软件行为与 API 说明见 [ROBOT_SETUP.md](ROBOT_SETUP.md)。

> **安全边界：** 任一方向错误、编码器符号错误、`BAD` 持续增加、异常振动、机械卡滞、驱动过热、冒烟、异味或堵转保护不符合预期时，立即断开 12 V，停止后续地面测试。未完成默认35%架空验收前，不得烧录或运行40%竞速工程，也不得绕过中键确认、急停和堵转保护。

## 0. 验收信息

| 项目 | 记录 |
| --- | --- |
| 验收日期与时间 |  |
| 操作人 |  |
| Git 提交/工作区说明 |  |
| 固件构建时间 |  |
| Keil 构建结果 | `0 Error(s), 0 Warning(s)` / 未通过 |
| 主机测试结果 | `18/18 passed` / 未通过 |
| 左/右 `mm_per_tick` |  /  |
| `effective_track_mm` |  |
| 轮胎与轮径 |  |
| 车体载荷 |  |
| 测试地面 |  |
| 12 V 电池静态电压 |  |
| 5 V / 3.3 V 实测 |  /  |
| UART CSV 文件目录 |  |

### 当前本车实测进度（2026-07-28）

| 项目 | 已确认结果 |
| --- | --- |
| 架空五段自检 | 左右 `FORWARD/REVERSE` 方向正确，`BOTH CLOSED LOOP` 正常，并到达 `GROUND+PRESS KEY` |
| 编码器非法转换 | 各运动阶段均显示 `BAD L0000 R0000`，表示未检测到非法正交跳变 |
| 500 mm 初始直线 | 实际约 500 mm，肉眼未见明显横向偏移 |
| 旧轮距下 `+90°/-90°` | 方向均正确，实际均约 54°，对称性比例误差明显 |
| 两轮接地点中心距 | 实测 `214.2 mm`；与 `90 × 128.6 / 214.2 ≈ 54.0°` 相符 |
| 编码器单圈初验 | 左轮 `1456 tick`，右轮 `|tick|=1498`，两侧 `BAD` 增量均为 0，均通过 ±5% 门槛 |
| 滚动标定暂定值 | 正向 `L7037/R6951`、反向 `L7110/R6987`；左/右 `mm_per_tick=0.1413727/0.1434926` |
| 当前待验证参数 | 左右 `mm_per_tick` 和 `effective_track_mm=214.2 mm`；修改后的固件必须重新 Rebuild、烧录并复验 |
| LF04四路物理质心控制、纯LF04诊断、固定`WF`白底门、GPIO输入防护、27列三源融合遥测与正式竞速LF04优先仲裁 | 新源码主机测试已通过；ARMCC、竞速/默认Rebuild和实车复验仍须执行 |

### 所需工具

- [ ] 稳固的架空支架，能让两轮完全离地且车体不会滑落。
- [ ] 可以单手立即操作的 12 V 断电开关或拔插点。
- [ ] 万用表；检查编码器时建议准备示波器或逻辑分析仪。
- [ ] 钢卷尺、直尺、胶带、细头记号笔和地面中心线。
- [ ] 角度板、转盘刻度或足够长的地面角度基准线。
- [ ] OLED滚动标定页用于读取相对tick；Keil调试器仅作为备用读取方式。
- [ ] 3.3 V TTL USB 转串口，串口工具支持保存原始 CSV。
- [ ] 防止长发、衣物和线缆卷入轮胎的固定措施。

### 总安全检查

- [ ] 已执行 `git status --short`，未覆盖或清理现有重构改动。
- [x] 当前源码已运行`tests/run_host_tests.ps1`，结果为`18/18 passed`。
- [ ] 当前固件已完成 Keil 全量 Rebuild，结果为 `0 Error(s), 0 Warning(s)`。
- [ ] 编码器 VCC 来自 H4 的 3.3 V，不是 U9/U12 原有的 5 V。
- [ ] 12 V、5 V、3.3 V 和信号地的负极已经共地。
- [ ] H3 对 GND 约 5.0 V，H4 对 GND 约 3.3 V。
- [ ] 四路编码器高电平不超过 3.6 V。
- [ ] TB6612 `VM=12 V`、`VCC=5 V`、`STBY=5 V`。
- [ ] 轮胎、齿轮和电机附近没有松动线缆或工具。
- [ ] 地面测试区域无人、无台阶，车辆冲出时不会造成损坏。
- [ ] 操作人知道垂直中键 PB24 是运行中的锁定急停。
- [ ] 操作人可以在异常发生时立即断开 12 V。

## 累计 tick 的读取方法

单圈和滚动标定默认使用OLED滚动标定页。它显示相对最近一次清零的左右有符号tick和相对 `BAD`，不修改底层累计计数；普通停车手推不会写入UART遥测，只有显式发送`F`进入融合采样页后才会记录。

1. 物理断开12 V，只保留5 V逻辑供电；在开机前按住垂直中键并保持车体静止。
2. IMU校准完成并显示 `ROLL C=ZERO` 后松开中键。首次进入已自动建立零基线。
3. 每次中键有效按压只重新建立tick和BAD显示基线，不会启动电机。
4. 记录样本后断电，不按中键重新开机即可退出标定模式。

若需要核对底层累计值，Keil Watch仍可作为备用方法：

1. 打开 `code/chassis.c`，进入 Keil Debug 会话。
2. 在 Watch 窗口加入：

   ```text
   g_chassis.status.encoder_total_left
   g_chassis.status.encoder_total_right
   g_chassis.status.encoder_invalid_left
   g_chassis.status.encoder_invalid_right
   g_chassis.status.target_left_mm_s
   g_chassis.status.target_right_mm_s
   g_chassis.status.measured_left_mm_s
   g_chassis.status.measured_right_mm_s
   ```

3. 如果静态变量无法解析，先打开 `chassis.c`，在 `chassis_poll()` 内暂停一次，再添加表达式。
4. 记录起始值后必须让程序继续运行，再转轮或推车；程序暂停时编码器状态不会被主循环同步到 `g_chassis.status`。
5. 操作完成后再次暂停并记录结束值，计算 `增量 = 结束值 - 起始值`。
6. 手动转轮、手推标定时断开 12 V，只保留逻辑电源和调试器。

## 十阶段放行总览

| 阶段 | 内容 | 进入下一阶段的最低条件 |
| --- | --- | --- |
| 1 | 烧录与首次启动 | 下载成功，逻辑电源正常，无异常发热 |
| 2 | IMU 与五向键 | 校准完成，五路按键一一对应 |
| 3 | 急停与架空运动 | 急停锁定有效，五段运动全部正确 |
| 4 | 编码器验收 | 符号正确，单圈在 ±5%，`BAD` 合格 |
| 5 | 500 mm 直线 | 正向、受控停止、无故障 |
| 6 | `+90°/-90°` | 转向方向正确、受控停止、无故障 |
| 7 | 左右轮比例 | 得到稳定的左右 `mm_per_tick` |
| 8 | 有效轮距 | 双向 90° 误差不超过 2° |
| 9 | UART 遥测 | CSV 完整，方向与变化趋势合理 |
| 10 | 低速调参回归 | 全部安全回归项通过并签字 |

---

## 阶段 1：固件烧录与首次启动

### 前置条件

- [ ] 12 V 电机电源已断开。
- [ ] 两个电机插头可保持断开；编码器、OLED、IMU 可以连接。
- [ ] Keil 最新全量构建为 `0 Error(s), 0 Warning(s)`。

### 操作

1. 在 Keil 中选择当前工程和 `Target 1`。
2. 下载当前 `user/Objects/project.axf`。
3. 下载完成后复位，只接通 5 V 逻辑电源。
4. 观察 OLED、核心板和外设是否正常启动，触摸检查是否有快速升温器件。

### 预期与通过标准

- [ ] 下载无错误，复位后 OLED 能显示自检页面。
- [ ] 未按中键时不会自动驱动电机。
- [ ] 5 V、3.3 V 电压正常，没有异味、异常电流或快速发热。

| 项目 | 结果/读数 |
| --- | --- |
| 下载结果 |  |
| OLED 首屏 |  |
| H3 电压 |  |
| H4 电压 |  |
| 异常现象 |  |

**立即停止：** 下载后黑屏、反复复位、电源电压错误或任何器件异常发热。排查完成前不要接 12 V。

---

## 阶段 2：断开 12 V 验证 IMU 与五向键

### 前置条件

- [ ] 12 V 仍然断开。
- [ ] 小车放在稳定水平面，IMU 校准期间不触碰车体。

### IMU 校准

1. 复位后确认第一行显示 `KEEP STILL`。
2. 确认第四行 `CAL:000/300` 连续增加到 `CAL:300/300`。
3. 若出现 `MOVE RETRY`，保持静止并观察：`G:034` 表示 3.4 dps，`A:101` 表示 1.01 g。
4. 静止校准要求陀螺仪模长小于 3.0 dps，加速度模长在 0.85–1.15 g。
5. 普通启动校准结束后应进入`LIFT+PRESS KEY`；若开机保持右键，则应进入永不运动的`UART DIAG STOP`。

当前IMU安装方向必须为：模块元件面朝下，传感器 `+X` 指向车尾，传感器 `+Y` 指向车辆左侧，传感器 `+Z` 指向地面。固件使用 `{X,Y,Z}` / `{-1,+1,-1}` 将其转换为车体 `+X` 向前、`+Y` 向左、`+Z` 向上的坐标。

校准完成后仍保持12 V断开，从车顶向下观察并手动顺时针缓慢转动车体约90°。Fusion原始Yaw应减小约90°；融合入口乘以`imu_heading_sign=-1`后应与当前编码器/运动接口的正航向一致。该测试只验证符号，不用原始Yaw作为绝对北向。

- [ ] 校准计数连续到 300。
- [ ] 未持续出现 `IMU READ ERR`、`IMU DATA ERR` 或 `IMU LOOP SLOW`。
- [ ] 车体保持静止时不会反复 `MOVE RETRY`。
- [ ] IMU模块朝向与上述 `+X/+Y/+Z` 安装说明一致。
- [ ] 手动顺时针转动时原始IMU Yaw为负，转换后的底盘航向为正。

### UART0安全诊断

本项必须先于空缓存CSV和任何电机动作执行：

1. 断电后按住右键PB25重新启动，保持12 V断开并静止完成IMU校准。
2. 确认OLED显示`UART DIAG STOP`、`BRD I0002 F11`，电机始终不动作。
3. COM3设为115200、8N1、无流控，不发送任何内容；每秒应收到一行`UART0,115200,TX,nnnn`，OLED的`TO`不得增加。
4. 发送一个ASCII `D`；OLED应显示`RX44 E00 O0000`，串口应回复`RX,44,ERR,00`，不得出现CSV表头或清空RAM。
5. 断电退出诊断模式；不按右键正常启动，重新静止完成IMU校准。

- [ ] 自动横幅连续两次完整、稳定且无乱码。
- [ ] 实际分频为`IBRD=2/FBRD=11`，TX超时为0。
- [ ] 单字符`D`显示为`RX44`，错误和溢出均为0。
- [ ] 所有按键在诊断页均不会启动运动。

**立即停止：** 横幅缺失或乱码、`TO`增加、`RX`不是44、`E/O`非0。保持12 V断开，根据OLED区分TX、RX和错误路径；未定位前不得进入空缓存CSV或架空测试。

### UART0空缓存冒烟测试

当前驱动同时保留RX中断队列和硬件RX FIFO轮询回退。两条接收路径对现场操作透明，仍只发送一个ASCII命令字符。

本项必须在接通12 V和执行任何电机动作前完成：

1. 保持页面为 `LIFT+PRESS KEY`，开发板继续使用独立5 V逻辑供电。
2. 使用3.3 V TTL USB转串口：适配器RX接PA10/UART0 TX、TX接PA11/UART0 RX并共地，VCC不连接。
3. COM3设置为115200、8N1、无流控；首次验证同时开启接收十六进制显示或原始字节保存。
4. 发送单个ASCII `D`，不附加 `0`、连续字符或换行。
5. 接收区必须立即出现一次完整CSV表头；此时尚无运动记录，所以表头后不应有数据行。
6. 再发送一次单个 `D`，必须重复得到相同表头且没有乱码。

```text
time_ms,left_ticks,right_ticks,x_mm,y_mm,encoder_heading_deg,fused_heading_deg,imu_yaw_deg,fused_yaw_rate_dps,fusion_active,elapsed_ms,lap_total_ms,target_center_mm_s,actual_center_mm_s,left_pwm_count,right_pwm_count,line_bits,line_correction_mm_s,line_usable,line_recovering,line_pattern_invalid,mission_progress_mm,expected_heading_deg,route_feedforward_bias_mm_s,heading_feedback_bias_mm_s,line_weight_pct,final_steering_bias_mm_s
```

空缓存表头连同末尾CRLF共435字节，共27列；十六进制以`74 69 6D 65 5F 6D 73`开头，以`5F 73 0D 0A`结尾。

- [ ] 单个 `D`能够立即触发导出。
- [ ] 两次表头均完整，不含 `~`、`}k`或其他乱码。

**立即停止：** 单个 `D`无输出、必须凑多个字符才响应、表头不是435字节或乱码。保持12 V断开，保存第一次响应的原始十六进制并检查PA10波形；PA10应空闲为约3.3 V，115200波特率的单个位宽约为`8.68 us`。本项通过前不得进入架空运动。

### 无电机融合方向调试

本项在空缓存测试之后、接通12 V之前执行：

1. 保持12 V断开且页面为`LIFT+PRESS KEY`，发送`C`清空旧记录。
2. 发送单个ASCII `F`，确认OLED变为`FUSION F/S STOP`且电机始终不动作。
3. 从车顶向下顺时针缓慢转动车体约90°；OLED原始`I`应减小，融合`F`和角速度`R`应按当前软件约定增大，第四行应显示`IMU ON`。
4. 保持静止约2 s；轮子未计数时，`F`应平滑回到编码器`E`，不得跳变。
5. 逆时针重复，符号应相反；发送`S`返回`LIFT+PRESS KEY`，再发送`D`保存CSV。
6. CSV应有数据行，`fusion_active`正常样本为1；`fused_yaw_rate_dps`与融合航向的瞬态方向一致。

- [ ] `F`没有清空RAM或启动电机。
- [ ] 采样时中键和方向键均不启动运动，`D/C`不执行。
- [ ] `S`停止采样后`D`能完整导出435字节表头和数据行。
- [ ] 顺/逆时针符号、融合有效位和静止回归趋势符合预期。

### 五向键映射

在 `LIFT+PRESS KEY` 页面先检查上、左、下、右，每次操作后完全松开；这四个方向键只做电平诊断，不应启动运动。中键必须最后检查：它通过 60 ms 消抖后会开始架空流程，但本阶段 12 V 已断开，因此电机功率侧不会动作；确认 `C0` 和页面响应后立即重新上电，再进入阶段 3。

| 操作 | MCU 引脚 | 预期第四行 | 实测 | 通过 |
| --- | --- | --- | --- | --- |
| 不操作 | — | `K U1L1D1C1R1` |  | [ ] |
| 上 | PA14 | `K U0L1D1C1R1` |  | [ ] |
| 左 | PA15 | `K U1L0D1C1R1` |  | [ ] |
| 下 | PA24 | `K U1L1D0C1R1` |  | [ ] |
| 右 | PB25 | `K U1L1D1C1R0` |  | [ ] |
| 中键（最后） | PB24 | `K U1L1D1C0R1`，随后进入架空流程 |  | [ ] |

### 通过门槛

- [ ] 每次只出现对应的一位变为 0。
- [ ] 松开后对应位恢复为 1。
- [ ] 上、左、下、右均不会启动电机自检。
- [ ] 中键按住超过 60 ms 后松开能产生一次确认；确认后已重新上电，没有在无 12 V 的错误流程中继续等待。

**立即停止：** 五路都不变、一次出现多个 0、按方向键启动运动或中键映射不在 PB24。

---

## 阶段 3：架空急停演练与五段运动

### 前置条件

- [ ] 两轮已经可靠架空，轮胎与地面、桌面、支架均无接触。
- [ ] 线缆、头发、衣物远离轮胎。
- [ ] 12 V 断电点在操作人手边。
- [ ] 已完成阶段 2，当前页面为 `LIFT+PRESS KEY`。

### 3A. 锁定急停演练

1. 接通 12 V。
2. 垂直按中键 0.1–0.3 秒并完全松开。
3. 进入 `LEFT FORWARD` 后再次垂直按中键并保持超过 60 ms。
4. 确认电机停止，第一行显示 `EMERGENCY STOP`。
5. 再按中键，确认电机不会重新启动。
6. 先断开 12 V，再断开/重启逻辑电源；急停锁定只能通过重新初始化或重新上电解除。

- [ ] 有效中键事件后电机立即停止。
- [ ] 急停页面保持锁定，后续运动不能恢复。
- [ ] 重新上电前始终保持停车。

### 3B. 完整五段架空自检

重新上电、保持静止完成 IMU 校准，再次确认车轮架空后接通 12 V并按中键。每段约 1 秒：

> 2026-07-28 后续架空复验已确认左右轮 `FORWARD/REVERSE` 均正确。当前最终方向配置为左轮 `encoder_sign=-1/motor_sign=-1`、右轮 `encoder_sign=-1/motor_sign=+1`；本轮只改变左右 `mm_per_tick`，方向配置保持不变。

| OLED 页面 | 固件目标 | 机械预期 | 实测方向 | 非目标轮 | 通过 |
| --- | --- | --- | --- | --- | --- |
| `LEFT FORWARD` | 左 +60 mm/s | 左轮车体前进方向 |  | 静止 | [ ] |
| `LEFT REVERSE` | 左 −60 mm/s | 左轮车体后退方向 |  | 静止 | [ ] |
| `RIGHT FORWARD` | 右 +60 mm/s | 右轮车体前进方向 |  | 静止 | [ ] |
| `RIGHT REVERSE` | 右 −60 mm/s | 右轮车体后退方向 |  | 静止 | [ ] |
| `BOTH CLOSED LOOP` | 左右 +80 mm/s | 两轮同时向前 |  | — | [ ] |

固件要求每个单轮阶段对应增量至少达到 20 tick，双轮阶段左右都至少达到 20 tick；未达到会停止并进入 `SELF TEST ERROR`。

### 通过门槛

- [ ] 五段顺序与 OLED 一致。
- [ ] 左右 `FORWARD` 时对应轮胎顶部都朝车头、底部都朝车尾；`REVERSE` 与之相反。
- [ ] `BOTH CLOSED LOOP` 时两轮顶部都朝车头；从左右外侧观察到的顺逆时针因镜像安装而相反。
- [ ] 正反方向正确，非目标轮不被错误驱动。
- [ ] `RIGHT FORWARD` 已继续进入 `RIGHT REVERSE`，没有再次出现 `STATUS:04`。
- [ ] 无持续抖动、摩擦、卡滞、异响或驱动明显发热。
- [ ] 五段结束后自动停车并进入 `GROUND+PRESS KEY`。

**立即停止：** 任一轮方向错误、另一轮误动作、轮速失控、机械干涉、`SELF TEST ERROR` 或堵转保护未停车。

---

## 阶段 4：编码器方向、单圈 tick 与非法跳变

### 前置条件

- [ ] 断开 12 V，只保留 5 V 逻辑供电。
- [ ] 开机前按住中键并保持车体静止，IMU完成后已进入 `ROLL C=ZERO`。
- [ ] 在轮胎和车体上制作一圈起止标记。
- [ ] 已释放中键，OLED左右相对tick和相对 `BAD` 均从0开始。

### 4A. 单圈和符号

每个样本开始前用中键清零显示基线；分别将输出轮沿车体前进方向缓慢、完整转一圈，再沿反方向转一圈。OLED显示值就是本次有符号增量。

理论值：`13 PPR × 28 × 4 = 1456 tick/轮`。初验范围采用 ±5%，即绝对值约 `1383–1529 tick`。

| 车轮 | 方向 | OLED tick增量 | 绝对值在范围内 | 符号正确 |
| --- | --- | ---: | --- | --- |
| 左 | 前进 |  | [ ] | 正 [ ] |
| 左 | 后退 |  | [ ] | 负 [ ] |
| 右 | 前进 |  | [ ] | 正 [ ] |
| 右 | 后退 |  | [ ] | 负 [ ] |

### 4B. `BAD` 非法跳变

1. 静止观察 30 秒，`BAD Lxxxx Rxxxx` 不应自行持续增加。
2. 每轮分别正转 10 圈、反转 10 圈，记录累计 tick 和 `BAD` 增量。
3. 计算：

   ```text
   非法转换率 = BAD增量 / (|tick增量| + BAD增量) × 100%
   ```

| 车轮 | 方向/圈数 | tick 增量 | BAD 起始 | BAD 结束 | 非法转换率 | 通过 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| 左 | 正转 10 圈 |  |  |  |  | [ ] |
| 左 | 反转 10 圈 |  |  |  |  | [ ] |
| 右 | 正转 10 圈 |  |  |  |  | [ ] |
| 右 | 反转 10 圈 |  |  |  |  | [ ] |

### 通过门槛

- [ ] 两轮前进累计均为正，后退累计均为负。
- [ ] 四次单圈绝对值均在 1383–1529 tick 的初验范围内。
- [ ] 静止时 `BAD` 不持续增加。
- [ ] 正反 10 圈非法转换率均低于 0.1%。

**立即停止：** 前进符号为负、A/B 同时跳变、`BAD` 快速增加或单圈计数明显接近 364/728 等错误解码倍率。先检查 A/B、3.3 V、GND、插头针序和机械连接。

---

## 阶段 5：500 mm 初始直线测试

### 前置条件

- [ ] 阶段 1–4 全部通过。
- [ ] 地面平整、清洁、空间充足，车前至少保留 1.5 m 安全距离。
- [ ] 以驱动轮轴中心或固定车体基准点作为起止测量点，禁止中途更换基准。
- [ ] 地面已画出前进中心线和 500 mm 终点线。
- [ ] 12 V 已接通，小车停在 `GROUND+PRESS KEY`。

### 操作

1. 将两轮轴线与起始线对齐，车体中心对准中心线。
2. 确认人员离开行驶区域后，垂直按中键并松开。
3. OLED 应显示 `RUN 500MM`，目标速度为 100 mm/s。
4. 车辆自动停止后不要立即按下一页面，测量实际纵向距离和横向偏移。
5. 如需重复，必须重新上电并重新完成 IMU 与架空自检；不要试图在当前等待页面重复触发 500 mm。

| 次数 | 实际距离 mm | 距离误差 mm | 距离误差 % | 横向偏移 mm | 停止是否平稳 | 备注 |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| 1 |  |  |  |  |  |  |
| 2 |  |  |  |  |  |  |
| 3 |  |  |  |  |  |  |

```text
距离误差 = 实际距离 - 500 mm
距离误差率 = |距离误差| / 500 mm × 100%
```

### 通过门槛

首次使用理论轮径时，本阶段首先验证安全和方向，不把 1% 作为未标定固件的强制门槛：

- [ ] 车辆沿车头方向前进，没有倒退或原地转向。
- [ ] 能自动减速并停止，没有持续振荡、冲击或故障。
- [ ] 三次结果已记录，可判断距离和横向偏移的重复性。
- [ ] 完成机械标定后，必须另做 1 m 验证并达到距离误差不超过 1%。

> 当前默认自检只能发出 500 mm 命令。最终 1 m 验证需要标定/任务入口在底盘空闲时调用 `chassis_move_mm(1000.0f, 100.0f)`；没有该入口时，不得把一次 500 mm 自检写成“1 m 精度已通过”。

**立即停止：** 车辆偏转失控、轮胎打滑、无法自动停车、触发堵转或按中键后未立即锁定停车。

---

## 阶段 6：`+90°/-90°` 初始转向

### 前置条件

- [ ] 500 mm 已安全完成，车辆停在 `PRESS FOR +90`。
- [ ] 车体中心周围有足够旋转空间。
- [ ] 地面已标出初始朝向和 ±90° 基准线。

### 操作

1. 对齐并记录当前车头方向，按中键启动 `ROTATE +90`。
2. 当前实车软件约定中正角从车顶向下看为顺时针，停止后测量实际角度。
3. OLED 进入 `PRESS FOR -90` 后重新记录起始方向，按中键启动 `ROTATE -90`。
4. 负角应为逆时针，停止后测量实际角度。
5. 完成后 OLED 应显示 `TEST COMPLETE`，电机保持停止。

| 命令 | 预期方向 | 实际方向 | 实际角度 ° | 绝对误差 ° | 自动停车 | 备注 |
| --- | --- | --- | ---: | ---: | --- | --- |
| +90° | 顺时针 |  |  |  | [ ] |  |
| −90° | 逆时针 |  |  |  | [ ] |  |

### 通过门槛

- [ ] 正负方向正确，转动中心和机械行为稳定。
- [ ] 两次均自动减速停车，无持续摆动或故障。
- [ ] 初始误差已记录；完成轮距标定后的最终 90° 误差必须不超过 2°。

**立即停止：** 正负方向相反、单轮不动、明显平移、持续打滑、机械卡滞或无法停车。

---

## 阶段 7：左右轮 `mm_per_tick` 标定

### 当前入口边界

本阶段使用不驱动电机的OLED滚动标定页完成，不需要连接电脑。UART仍不记录停车手推tick；Keil Watch只作为备用核对方式。左右轮可以在同一次直线手推中同时计算，但结果必须分别保存。

### 操作

1. 断开 12 V，只保持 5 V 逻辑供电。
2. 开机前按住中键并保持静止；IMU校准完成、出现 `ROLL C=ZERO` 后松开中键。
3. 在实际轮胎、载荷和比赛地面上画出 1–2 m 直线。
4. 将相同车体基准对准起点，中键清零后沿直线缓慢滚动到终点，记录左右有符号相对tick和相对 `BAD`。
5. 前进至少 5 次、后退至少 5 次；每次使用相同车体测量基准。
6. 分别计算：

   ```text
   左轮 mm_per_tick = 实际距离 / |左相对tick|
   右轮 mm_per_tick = 实际距离 / |右相对tick|
   ```

| 次数 | 方向 | 距离 mm | 左相对tick | 左 mm/tick | 右相对tick | 右 mm/tick | BAD增量 | 采用/剔除原因 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- | --- |
| 1 | 前进 | 1000 | +7037 | 0.142106 | +6951 | 0.143864 | 0/0 | 采用，首轮样本 |
| 2 | 前进 |  |  |  |  |  |  |  |
| 3 | 前进 |  |  |  |  |  |  |  |
| 4 | 前进 |  |  |  |  |  |  |  |
| 5 | 前进 |  |  |  |  |  |  |  |
| 6 | 后退 | 1000 | -7110 | 0.140647 | -6987 | 0.143123 | 0/0 | 采用，首轮样本 |
| 7 | 后退 |  |  |  |  |  |  |  |
| 8 | 后退 |  |  |  |  |  |  |  |
| 9 | 后退 |  |  |  |  |  |  |  |
| 10 | 后退 |  |  |  |  |  |  |  |

| 汇总 | 左轮 | 右轮 |
| --- | ---: | ---: |
| 有效样本数 | 2个方向样本 | 2个方向样本 |
| 合并 `mm_per_tick` | 0.1413727 | 0.1434926 |
| 最小值 | 0.140647 | 0.143123 |
| 最大值 | 0.142106 | 0.143864 |
| 暂定写入值 | 0.1413727 | 0.1434926 |

### 写入与复验

1. 只修改 `code/chassis_config.c` 中左右轮各自的 `mm_per_tick`。
2. 运行`tests/run_host_tests.ps1`，确认`18/18 passed`。
3. 在 Keil 中全量 Rebuild，确认 `0 Error(s), 0 Warning(s)`。
4. 重新烧录，从阶段 1 重新执行安全门槛。
5. 当前先执行现有500 mm直线回归，放行范围为495–505 mm；该单组正反样本不能单独证明最终1%精度。

**立即停止：** 同方向样本离散很大、左右 tick 突然跳变、地面打滑或机械回差导致无法稳定复现。不要通过提高 PWM 掩盖机械问题。

---

## 阶段 8：`effective_track_mm` 标定

### 当前入口边界

默认自检只提供一次 `+90°` 和一次 `−90°`。360°和多次重复需要标定/任务入口在底盘空闲时调用：

```c
chassis_rotate_deg(90.0f, 60.0f);
chassis_rotate_deg(-90.0f, 60.0f);
chassis_rotate_deg(360.0f, 60.0f);
chassis_rotate_deg(-360.0f, 60.0f);
```

不要在另一个轨迹运行时发送命令；接口返回 `ML_STATUS_BUSY` 时不得强行继续。

### 操作与公式

1. 确认左右 `mm_per_tick` 已标定并写入。
2. 使用外部角度基准测量车体实际转角，不使用编码器自身计算角度作为唯一标尺。
3. 正负 90°和正负 360°各重复至少 5 次。
4. 对每个稳定样本计算：

   ```text
   新轮距 = 旧轮距 × |命令角度| / |实际角度|
   ```

5. 实际转角小于命令角度时增大轮距，实际转角大于命令角度时减小轮距。
6. 去掉明确打滑样本，对稳定结果取平均。正负方向差异明显时，先排查轮径差、机械阻力、重心和地面打滑，不要只靠一个轮距值硬补偿。

本车首轮记录为旧轮距 `128.6 mm`、双向 90°实际均约 `54°`，且机械中心距实测 `214.2 mm`。因此本轮先写入 `214.2 mm`，重新构建和烧录后再用外部角度基准复验。

| 命令角度 | 次数 | 实际角度 ° | 误差 ° | 旧轮距 mm | 建议新轮距 mm | 打滑/采用 |
| ---: | ---: | ---: | ---: | ---: | ---: | --- |
| +90 | 1 |  |  |  |  |  |
| +90 | 2 |  |  |  |  |  |
| +90 | 3 |  |  |  |  |  |
| +90 | 4 |  |  |  |  |  |
| +90 | 5 |  |  |  |  |  |
| −90 | 1 |  |  |  |  |  |
| −90 | 2 |  |  |  |  |  |
| −90 | 3 |  |  |  |  |  |
| −90 | 4 |  |  |  |  |  |
| −90 | 5 |  |  |  |  |  |
| +360 | 1 |  |  |  |  |  |
| +360 | 2 |  |  |  |  |  |
| +360 | 3 |  |  |  |  |  |
| +360 | 4 |  |  |  |  |  |
| +360 | 5 |  |  |  |  |  |
| −360 | 1 |  |  |  |  |  |
| −360 | 2 |  |  |  |  |  |
| −360 | 3 |  |  |  |  |  |
| −360 | 4 |  |  |  |  |  |
| −360 | 5 |  |  |  |  |  |

| 轮距汇总 | 数值 |
| --- | ---: |
| 旧 `effective_track_mm` |  |
| 稳定样本建议值平均 |  |
| 最终写入值 |  |
| 最终 +90° 最大误差 |  |
| 最终 −90° 最大误差 |  |

### 通过门槛

- [ ] 参数修改后重新完成主机测试、Keil Rebuild 和烧录。
- [ ] 双向 90°各至少 5 次，所有有效样本误差不超过 2°。
- [ ] 360°测试没有持续累积的异常方向偏差。

---

## 阶段 9：UART0 遥测导出与分析

### 离线记录前置条件

- [ ] 开发板使用可随车移动且不会中断的独立 5 V 逻辑电源。
- [ ] 12 V 电机电源可以单独断开，不会同时切断 5 V。
- [ ] 本次上电后没有复位或重新烧录；`chassis_init()`已经自动清空 RAM，地面现场不需要发送 `C`。
- [ ] 已知架空五段也会保存在同一缓冲区，导出后将按时间戳间隔和运动特征筛选地面三段。

### 地面离线记录

1. 保持 5 V 供电，按正常安全顺序完成 IMU 静止校准和架空五段自检。
2. 将车放到地面，在 `GROUND+PRESS KEY` 页面完成对线和测量准备后再停留数秒，按中键执行 500 mm并记录外部距离和横向偏移。
3. 在 `PRESS FOR +90` 页面重新对准外部角度基准，停留数秒后按中键；停车后记录实际角度。
4. 在 `PRESS FOR -90` 页面重复相同步骤。各等待间隔会反映在 CSV 的 `time_ms` 跳变中，便于分段。
5. OLED 显示 `TEST COMPLETE` 且车辆完全停车后，先断开 12 V；不要断开 5 V，不要复位、重新烧录或切换可能造成瞬时掉电的电源。
6. 持续保持 5 V，将车搬回电脑旁。确认未处于`FUSION F/S STOP`后，停车搬运不会增加记录或污染已保存样本。

> **RAM 断电即失：** 最后一段测试完成后，如果 5 V 中断、MCU复位或重新烧录，尚未导出的全部遥测都无法恢复。导出并核验前禁止发送 `C`。

### 回到电脑后连接与导出

1. USB 转串口必须为 3.3 V TTL，串口设置为 115200、8N1、无流控。
2. 先连接 GND，再交叉连接信号：适配器 RX 接 PA10/UART0 TX，适配器 TX 接 PA11/UART0 RX。
3. 不连接 USB 转串口的 VCC/3.3 V/5 V 脚，开发板继续使用自己的 5 V 逻辑电源。
4. 完全停车状态下首先发送单个 ASCII `D`，将全部输出保存为 `.csv`。
5. 核对文件包含正确表头、完整数据行和地面三段记录；只有保存和备份确认无误后，才允许发送 `C` 清空 RAM。
6. 竞速运行或制动期间发送 `C` 或 `D` 不会执行，固件每秒最多回复一次短`BUSY`；完全停车后单个`D`才导出纯CSV。

单字符命令必须独立生效，不要发送 `D0`、连续多个 `D`或额外换行来触发导出。若空缓存冒烟测试曾通过、此处却无输出，先确认仍是同一个COM3和115200文本模式，并保持5 V不断电。

CSV 表头应为：

```text
time_ms,left_ticks,right_ticks,x_mm,y_mm,encoder_heading_deg,fused_heading_deg,imu_yaw_deg,fused_yaw_rate_dps,fusion_active,elapsed_ms,lap_total_ms,target_center_mm_s,actual_center_mm_s,left_pwm_count,right_pwm_count,line_bits,line_correction_mm_s,line_usable,line_recovering,line_pattern_invalid,mission_progress_mm,expected_heading_deg,route_feedforward_bias_mm_s,heading_feedback_bias_mm_s,line_weight_pct,final_steering_bias_mm_s
```

记录频率为10 Hz，公开记录结构为52字节、CSV为27列，最多600条，约60秒运动数据；RAM内部使用44字节紧凑记录保存同等字段，其中tick和航向保持原精度，x/y量化分辨率为0.25 mm。缓冲满后停止新增且不覆盖旧记录。同时间戳立即快照覆盖上一条，故导出时间戳严格递增。串口工具必须关闭“发→/收←”方向标记、时间标签、定时发送和自动换行，这些界面文字不是固件CSV内容。

### 分析规则

- 直线前进时左右 tick 应同时增加且为正，差值应稳定。
- 正转命令时编码器航向变化方向应与 `+90°` 约定一致，负转同理。
- 先根据时间戳间隔和左右轮运动特征区分架空五段、500 mm、`+90°`和`-90°`，再取每个地面段首末样本计算变化量。
- IMU Yaw 是上电后的相对值，应比较首末变化量，不把原始起点当绝对北向。
- 500 mm直线段的编码器航向、融合航向和IMU Yaw变化都应接近0。当前软件`+90°`从上方观察为顺时针：编码器/融合航向应为正，原始IMU Yaw应为负；`-90°`相反。比较幅值时先将原始IMU Yaw乘以-1。
- 未启用`F`调试时，停车搬运造成的姿态变化不会写入遥测，也不会改变已经保存的运动样本。
- ICM42688映射且扣除零偏后的Z轴角速度参与航向融合和底盘运动层角速度反馈；原始Yaw继续只用于诊断。单轮速度PID仍只使用编码器。
- `target_center_mm_s`与`actual_center_mm_s`用于判断稳态速度偏差；`left/right_pwm_count`是0–20000的绝对PWM计数，`line_bits`为0–15的LF04位型，`line_correction_mm_s`为有符号红外纠偏量。末三列中B1-B15对应`line_usable=1`，B0对应`line_recovering=1`，兼容列`line_pattern_invalid`固定为0。编码器/融合航向是累计值，原始IMU Yaw仍会回绕。默认自检记录的`lap_total_ms`为0，竞速或纯LF04会话完成后该列为本次总用时。
- 编码器航向与 IMU Yaw 短时明显分离通常提示轮胎打滑、机械偏差、IMU 安装/轴映射问题或振动。

| 文件名 | 测试 | 起止时间 ms | 左 tick变化 | 右 tick变化 | 编码器角变化 ° | 融合角变化 ° | 原始IMU变化 ° | 结论 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
|  | 500 mm |  |  |  |  |  |  |  |
|  | +90° |  |  |  |  |  |  |  |
|  | −90° |  |  |  |  |  |  |  |
|  | +360° |  |  |  |  |  |  |  |
|  | −360° |  |  |  |  |  |  |  |

### 通过门槛

- [ ] CSV表头和27列数据完整，时间戳严格递增，累计融合航向可连续越过180°并达到约360°。
- [ ] 导出前全程保持 5 V，且已按“先 `D` 并核验、后 `C`”完成数据保存。
- [ ] 已利用时间戳间隔正确标出500 mm、`+90°`和`-90°`三个地面运动段。
- [ ] 左右 tick 符号与运动方向一致，没有突跳或长时间零反馈。
- [ ] 编码器航向与融合航向同号；原始IMU Yaw按`-1`转换后同号。
- [ ] IMU短暂异常时融合航向没有跳变，系统能够退化为编码器继续运行。
- [ ] 对明显差值已经区分系统比例误差、打滑和IMU漂移，没有把原始Yaw绝对值当作真值。

---

## 阶段 10：低速前馈/PID 调整与最终回归

### 当前入口边界

默认自检的架空速度固定为 60/80 mm/s，地面直线固定为 100 mm/s。逐轮、多速度调参需要标定/任务入口调用 `chassis_set_wheel_speed()`，并持续执行 `chassis_poll()`。

### 调整顺序

1. 先完成电机方向、编码器符号、左右 `mm_per_tick` 和有效轮距标定。
2. 架空测试单轮 60、80、100 mm/s 的正反转，再测试双轮。
3. 先调整前馈，使低速能够可靠启动且不过度冲击。
4. 再调整 KP，观察响应和振荡；随后少量调整 KI 消除稳定偏差。
5. KD 默认保持 0，只有在有明确噪声和响应证据时才评估。
6. 每次只改变一个参数，记录旧值、新值和结果；左右轮分别调整。
7. 使用 Keil Watch 比较目标速度和滤波测量速度；如需确认 PWM，占用示波器测量，不猜测内部占空比。

禁止改变以下安全基线：

- 默认自检PID与前馈合成输出不超过约`±17500/50000`，即35%。
- 底层绝对PWM硬限制为`±20000/50000`，即40%，只允许独立竞速配置达到。
- PID 输出限制 `±11500`。
- PID 积分限制 `±5750`。
- 堵转阈值 8 个 20 ms 周期，约 160 ms。

| 次数 | 车轮/方向 | 目标 mm/s | 前馈 | KP | KI | KD | 启动情况 | 稳态误差 | 振荡/噪声 | 结论 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- | --- | --- | --- |
| 1 |  | 60 |  |  |  | 0 |  |  |  |  |
| 2 |  | 80 |  |  |  | 0 |  |  |  |  |
| 3 |  | 100 |  |  |  | 0 |  |  |  |  |
| 4 |  | −60 |  |  |  | 0 |  |  |  |  |
| 5 |  | −80 |  |  |  | 0 |  |  |  |  |
| 6 |  | −100 |  |  |  | 0 |  |  |  |  |

### 每次参数修改后的强制流程

- [x] 运行`tests/run_host_tests.ps1`，结果为`18/18 passed`。
- [ ] Keil 全量 Rebuild 为 `0 Error(s), 0 Warning(s)`。
- [ ] 烧录后先断开 12 V 检查 IMU 和五键。
- [ ] 重新完成中键急停与五段架空测试。
- [x] 默认自检仍为35%，竞速目标绝对限幅按方案提高到40%。

### 最终回归签字

- [ ] 两轮正反方向和编码器符号正确。
- [ ] 单圈计数在 1456 tick ±5% 范围内。
- [ ] 非法转换率低于 0.1%，静止 `BAD` 不持续增加。
- [ ] 中键运行急停立即生效并锁定到重新上电。
- [ ] 1 m 直线有效样本误差不超过 1%。
- [ ] 双向 90°有效样本误差不超过 2°。
- [ ] 60–100 mm/s 正反向均可可靠启动、稳定运行和停车。
- [ ] UART CSV 已归档，编码器航向、融合航向与原始IMU Yaw差异已解释。
- [ ] TB6612、电机和线缆无异常发热、异味或机械损伤。

| 签字项目 | 记录 |
| --- | --- |
| 最终固件提交/工作区 |  |
| 最终构建时间 |  |
| 最终左/右 `mm_per_tick` |  /  |
| 最终 `effective_track_mm` |  |
| 最终左轮 PID/前馈 |  |
| 最终右轮 PID/前馈 |  |
| 未通过项及处置 |  |
| 操作人签字 |  |
| 复核人签字 |  |

---

## 顺时针胶囊赛道竞速验收

本项只在默认五段架空自检、500 mm、双向90°、中键急停和堵转保护全部通过后执行。使用独立`user/project_track.uvprojx`全量Rebuild并确认`0 Error(s), 0 Warning(s)`，不要把竞速宏写入默认自检工程。

### 纯LF04诊断先行验收

1. 烧录竞速工程后把车放白底，上电前按住上键；完成白底/IMU校准后松开，必须进入`LF ONLY READY`。不按上键重新上电必须进入正式`RACE FUSION`。
2. READY页确认上/下键只能在60、120、200、280、350 mm/s之间选择，中键以外的按键不能启动；运行中按上/下键目标速度不得改变。
3. `LF ONLY READY`不得显示`ALIGN BOTH`或位型许可计数。分别在B0、四个单探头及多探头位型下按中键，都必须立即开始；初始B0按质心误差0直行并开始累计保持时间。
4. 先在直线段选择60 mm/s并按中键。确认目标每20 ms最多增加8 mm/s、IMU转动不产生额外轮差、编码器闭环/堵转/中键急停均有效；超过1000 mm和6141.6 mm都不得自动制动，回到起点后由中键锁定急停。
5. 让位型在`RE B1/RD B2/R9 B6/RB B4/R7 B8`之间切换：B1/B8在60 mm/s仍立即产生约`±21 mm/s`增强纠偏，B2/B4稳态约为`±2.85 mm/s`，B2/B4回中时允许出现小于1 mm/s的单周期反向D阻尼。`B2→B4`直接反向的首周期纠偏必须为0，连续第二周期才接受B4；`B2→B6→B4`则不延迟。从B1/B8进入B0时持续保持外侧增强；随后出现对侧有效位型时，首个反向周期为0，第二周期必须立即切换到对侧普通PD，不能继续输出旧方向的89–120 mm/s弯道偏置。纯LF04和正式竞速均不得启用请求行程弯道锁存。
6. 中键锁停后保持5 V，导出并保存27列CSV，确认含目标/实际速度、PWM、位型、有符号纠偏量和三列循迹状态；纯LF04下六个正式融合新增列应为0，末条应为`line_correction_mm_s=0`且`lap_total_ms`为本次运行时间。确认保存后才清空记录。下一次测试必须断电重启并重新进入纯LF04模式，READY仍不要求双组同时有效。
7. 依次完成60、120、200、280、350 mm/s的短距离方向确认和整圈测试，280和350 mm/s各连续完成两圈并分别保存CSV。对比修改前后的直线段`fused_yaw_rate_dps`摆幅、`line_correction_mm_s`正负切换次数和B6占比；整圈能力不得下降。重点确认两个弯道之间存在稳定的低纠偏直线段，单圈累计融合航向不应超过约420°，不得再次出现从首次B1起持续十几秒的同向89–120 mm/s纠偏。任何明显偏离、振荡、堵转或完全脱线都必须人工中键急停并停止升级。

> RAM遥测只保留前约60秒且满后不覆盖。60 mm/s整圈标称约102秒，CSV可能在回到起点前停止新增，但车辆不得因此停车；120 mm/s及以上的标称整圈时间在缓冲容量内。

| 路段 | 速度 mm/s | 完成距离 mm | 最大B0 ms | 位型记录 | CSV已保存 | 结论 |
| --- | ---: | ---: | ---: | --- | --- | --- |
| 直线 | 60 |  |  |  |  |  |
| 直线 | 120 |  |  |  |  |  |
| 直线 | 200 |  |  |  |  |  |
| 直线 | 280 |  |  |  |  |  |
| 直线 | 350 |  |  |  |  |  |
| 弯道 | 60 |  |  |  |  |  |
| 弯道 | 120 |  |  |  |  |  |
| 弯道 | 200 |  |  |  |  |  |
| 弯道 | 280 |  |  |  |  |  |
| 弯道 | 350 |  |  |  |  |  |

1. 上电时放在白底并保持静止；LF04原始值必须连续10个20 ms样本为`RF`且IMU校准完成后，才允许进入READY。IMU先完成但白底未稳定时必须显示`LF WHITE WAIT`，电机保持锁定；稳定计数中任何非`RF`样本必须清零。
2. READY页白底必须显示`RF WF B0`。LF04模块横向垂直于车身中心线安装；人工从左向右横移黑线，记录完整位型序列。B1-B15全部按`-40.25/-7.25/+7.25/+40.25 mm`物理质心计算，B6/B9/B15应居中；随后把车放在A点并沿AB顺时针对准，任何LF04位型均不参与竞速启动许可。
3. 烧录stage 0竞速固件后先保持12 V断开，上电时不得按住上键。进入`RACE FUSION`后按中键，允许短暂进入RUN；因无编码器反馈，约160 ms后应锁停并显示`TRACK STALL`，不得被`TRACK HW FAULT/STATUS 05`覆盖。若显示`LF ONLY READY`必须断电重启，不得继续正式验收。RUN期间第四行每秒交替显示`Rr Ww Bb`和`LFb Ln DnHn`。架空后分别在白底和黑线上运行电机；显示`LF GPIO FAULT`时必须立即停止并检查输入配置、3.3 V供电、共地和接线。
4. 重新上电并架空车轮、接通12 V。确认白底/IMU校准期间电机为零，进入`RACE FUSION`后按中键应立即清零并启动；上、左、下、右不得启动。
5. 架空逐一制造B1、B2、B4、B8，确认左侧探头使左轮慢、右轮快，右侧完全对称。普通位型总轮差不超过90 mm/s，B1/B8立即允许120 mm/s且不随持续时间降额。再遍历多探头位型确认物理质心，B6/B9/B15稳态必须回正。`B2→B4`首周期纠偏为0、第二周期转右；经过B6居中的反向不延迟。注入与LF04同向的路线/航向辅助时允许叠加到位型上限；注入反向辅助时最终轮差必须保持LF04方向，不能被抵消或反转。重点回放本次失控条件：B8原始纠偏`-120 mm/s`、路线前馈约`+77 mm/s`、航向反馈约`+37 mm/s`时，最终轮差必须为`-120 mm/s`，不得再出现`+65～+84 mm/s`。任一轮目标不超过500 mm/s；中键急停、约160 ms堵转和PWM 20000限制必须有效。
6. 分别从B1、B2、B6、B4、B8进入B0：正式竞速与纯LF04都必须无限保持最后确认误差和增强状态，`line_weight_pct`在正式模式固定为100。`B2→B4→B0`由B0确认并保持右转；`B2→B4→B2`必须取消单周期反向。上电后尚无有效样本的初始B0按误差0且不增强处理，此时正式竞速允许路线辅助接管。
7. 地面先按60、120、200 mm/s验证，再使用stage 0完成直线/弯道360 mm/s整圈。stage 0通过后依次把`CHASSIS_TRACK_SPEED_STAGE`设为1和2，仅将直线放行到380、400 mm/s，弯道保持360 mm/s；每级记录轨迹偏差、`BAD`、温升和急停。
8. 依次完成四分之一圈、半圈和完整一圈。AB期望航向为0°，BC从0°升至180°，CD保持180°，DA从180°升至360°；BC、DA必须为从车顶观察的顺时针半圆。第二弯从约4571 mm开始；若B2在约5456 mm、294°附近进入B0并保持约`+16 mm/s`红外纠偏，融合角速度仍须持续为正且通常不低于约25°/s，不得像纯LF04数据一样降到约6°/s。
9. 在终点前检查第四行距离门`D`和航向门`H`：`approach_distance_mm=190`的速度上限必须让任务请求在中心里程`6141.6-36 mm`前达到100 mm/s；LF04只改变轮差，不得提高任务中心速度。融合航向连续3个20 ms周期进入`360°±5°`后航向门才成立，两门同时满足才制动；376°必须拒绝制动，只有一项满足时保持不高于100 mm/s继续收敛。
10. 分别模拟距离不足和航向窗口未满足；任一单门不得停车。到`route+50 mm`仍未同时满足时必须显示`FAULT LAP CHECK`并停车，不得无限前进。
11. 完整一圈从有效中键按压边沿开始计时，两轮实测速度连续3个20 ms周期低于20 mm/s时结束。外部秒表、OLED和CSV的`lap_total_ms`必须一致，总时间≤20.00 s。
12. 以A点外部基准测量车辆约定停车基准到A线的有符号距离，绝对值必须≤20 mm。OLED`EST ERR`仅作编码器估计，不替代外部测量；按实测低速制动距离微调`finish_stop_lead_mm`。
13. 完全停车后先断12 V并保持5 V，SSCOM关闭“发→/收←”方向标记、时间标签、定时发送和自动换行，只发一次ASCII `D`或十六进制`44`并等待纯27列CSV；确认时间戳严格递增、累计融合航向约360°，并含六个融合诊断字段及PWM/纠偏归零样本后，才发送`C`清空RAM。运行和制动期间`D/C`不得导出或清空，每秒最多返回一次`BUSY`。

| 速度 mm/s | 圈段 | 时间 s | A点偏差 mm | LF04丢线 | BAD增量 L/R | 最高PWM L/R | 温升/振荡 | 结论 |
| ---: | --- | ---: | ---: | --- | --- | --- | --- | --- |
| 60 | 1/4圈 |  |  |  |  |  |  |  |
| 120 | 1/4圈 |  |  |  |  |  |  |  |
| 200 | 1/2圈 |  |  |  |  |  |  |  |
| 360（stage 0） | 1圈 |  |  |  |  |  |  |  |
| 380/360（stage 1） | 1圈 |  |  |  |  |  |  |  |
| 400/360（stage 2） | 1圈 |  |  |  |  |  |  |  |

- [ ] 路线总长参数为6141.6 mm，AB/CD各1500 mm，BC/DA半径500 mm。
- [ ] 360 mm/s完整圈外部计时≤20.00 s。
- [ ] A点外部停车偏差绝对值≤20 mm。
- [ ] LF04的B1-B15均按四路物理质心工作；普通90 mm/s、B1/B8立即120 mm/s、两周期反向确认、反向LF04优先及B0无限保持符合要求，纯LF04和正式竞速的弯道记忆均关闭。
- [ ] 第一半圆不再出现B8要求负纠偏而最终轮差仍为正的反向夺权；终点仅在`route-36 mm`距离门和360°±5°三周期航向门同时满足时制动，`route+50 mm`不满足时锁停。
- [ ] 中键急停、堵转与40%绝对限幅均通过。
- [ ] CSV包含27列、累计约360°航向、六个融合字段以及PWM/纠偏归零样本，时间戳严格递增，OLED/CSV/外部计时一致。

---

## OLED 与故障速查

| 显示 | 含义 | 现场动作 |
| --- | --- | --- |
| `KEEP STILL` | IMU 正在采集 300 个静止样本 | 不触碰车体 |
| `MOVE RETRY` | 校准期间检测到移动 | 保持静止，查看 G/A 模长 |
| `IMU READ ERR` | ICM42688 暂时读取失败 | 检查 3.3 V、I²C、地址与接线 |
| `IMU DATA ERR` | 姿态更新数据无效 | 保持停车并检查传感器数据 |
| `IMU LOOP SLOW` | IMU 服务时序重置 | 连续出现应停止排查时基/主循环 |
| `ROLL C=ZERO` | OLED滚动标定模式 | 12 V保持断开；记录相对tick，中键仅清零基线 |
| `L/R ±xxxxxxx` | 左右相对最近基线的有符号tick | 前推应为正、后推应为负 |
| `LIFT+PRESS KEY` | 等待架空确认 | 架空后只按垂直中键 |
| `LEFT/RIGHT FORWARD/REVERSE` | 单轮 60 mm/s 架空阶段 | 观察方向、目标轮和 tick |
| `BOTH CLOSED LOOP` | 双轮 80 mm/s 架空阶段 | 两轮都应向前 |
| `GROUND+PRESS KEY` | 等待地面 500 mm | 对线、清场后按中键 |
| `RUN 500MM` | 正在执行 500 mm/100 mm/s | 随时准备急停 |
| `PRESS/ROTATE +90` | 等待/执行顺时针 90° | 清场、测量外部实际角度 |
| `PRESS/ROTATE -90` | 等待/执行逆时针 90° | 清场、测量外部实际角度 |
| `TEST COMPLETE` | 默认自检完成 | 保持停车，可导出 UART |
| `WHITE + IMU CAL` | 竞速白底稳定门与IMU校准 | 车辆离开黑线并保持静止，等待`RF`连续10次 |
| `LF WHITE WAIT` | IMU已完成但LF04尚未稳定为`RF` | 保持停车和白底；查看当前R值与稳定计数 |
| `RACE FUSION` | 正式编码器/IMU融合竞速等待页 | 确认未按上键、对准顺时针AB方向；按中键清零并启动 |
| `LF ONLY READY` | 纯LF04诊断等待页 | 仅用于红外诊断；要跑正式竞速须断电后不按上键重启 |
| `RUN AB/BC/CD/DA` | 编码器–融合航向胶囊轨迹运行 | 监控期望/融合航向、轨迹、温升和BAD |
| `FINAL APPROACH` | A点前低速接近 | 不进入赛道，准备外部停车测量 |
| `ENC+IMU CHECK` | 等待`route-36 mm`与360°±5°三周期双门 | 查看第四行`D/H`，LF按正式置信度参与有限幅融合 |
| `LAP COMPLETE` | 两轮已连续3周期低于20 mm/s | 记录TIME与EST ERR并外部复核 |
| `FAULT LAP CHECK` | 到`route+50 mm`仍未满足距离/航向双门 | 已停车，检查轮距、轮径、融合航向和打滑 |
| `LF GPIO FAULT` | LF04输入重申失败 | 已锁停；断12 V，记录R/W/B并检查软件映射后再查硬件 |
| `TRACK STALL` | 竞速时约160 ms无编码器反馈 | 已锁停；断12 V测试中属预期，带电时检查堵转、接线和编码器 |
| `EMERGENCY STOP` | 中键锁定急停 | 先断 12 V，重新上电前不再运动 |
| `SELF TEST ERROR` | 初始化、命令或运动失败 | 断 12 V，记录 `STATUS:xx` |
| `L A0B0 R A0B0` | 四路编码器实时 GPIO 电平 | 转轮时 A/B 应交替变化 |
| `BAD Lxxxx Rxxxx` | 左右非法正交转换累计值 | 持续增加时停止并查电气问题 |
| `K U1L1D1C1R1` | 上/左/下/中/右原始电平 | 低有效，只允许 C 控制运动 |

`STATUS:xx` 使用 `ml_status_t`：

| 状态 | 含义 |
| ---: | --- |
| 00 | OK |
| 01 | 参数无效 |
| 02 | 功能不支持 |
| 03 | 未初始化 |
| 04 | 超时 |
| 05 | 资源忙 |
| 06 | I²C 无 ACK |
| 07 | 缓冲区空 |
| 08 | 缓冲区满 |
| 09 | 设备未找到 |

当前自检将多数运行期底盘故障统一显示为 `STATUS:04`，因此它可能表示架空 tick 不足、堵转、编码器故障或运动未完成，不能只根据 04 判断根因。应同时记录发生页面、轮子动作、AB 电平、`BAD`、电源和机械状态。
